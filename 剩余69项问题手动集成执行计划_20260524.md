# 剩余69项问题手动集成执行计划（2026-05-24）

> **制定日期**: 2026-05-24  
> **已完成**: T-03 MultiPeriodCrossValidator集成  
> **待完成**: 68项  
> **执行策略**: 分批手动集成 + 逐项验收

---

## 一、当前进度

### ✅ 已完成（1/69）

| 编号 | 问题 | 修复位置 | 状态 |
|------|------|---------|------|
| T-03 | MultiPeriodCrossValidator集成 | enhanced_phase_scan.py:main() | ✅ 已集成 |

### ⏳ 进行中（0/69）

无

### 📋 待处理（68/69）

按系统分类：
- **测试系统**: T-04, T-05, T-06, T-07, T-08, T-09 (6项)
- **评判系统**: E-04, E-05, E-06, E-07, E-09, E-10, E-11, E-12, E-13 (9项)
- **生产系统P1/P2**: P-05, P-06, P-08, P-13~P-21, P-23~P-38 (约33项)
- **评判系统P1/P2**: E-03, E-08, E-14~E-20 (约13项)
- **测试系统P1/P2**: T-11~T-17 (7项)

---

## 二、批量集成策略

### 2.1 测试系统T-04~T-09集成方案

#### T-04: MultiParameterTracer集成

**集成点**: `enhanced_phase_scan.py:main()` 或 `optuna_multiobjective_search.py:main()`

**修复代码模板**:
```python
# T-04修复: MultiParameterTracer集成 — 参数稳定性跟踪（手册18.3节）
try:
    from ali2026v3_trading.参数池.advanced_validation import MultiParameterTracer
    mpt = MultiParameterTracer()
    print("\n[T-04] 启动参数稳定性跟踪...")
    
    # 跟踪阶段2结果中的参数变化
    for params, metrics in phase2_results[:10]:  # 取前10个结果
        mpt.record_params(params, metrics.get('sharpe', 0))
    
    # 生成热力图报告
    heat_map = mpt.heat_map_report()
    print(f"[T-04] 参数稳定性热力图生成完成: {len(heat_map)}个参数")
    
    logger.info("[T-04] MultiParameterTracer集成完成")
except Exception as _t04_err:
    logger.warning("[T-04] MultiParameterTracer集成失败(非致命): %s", _t04_err)
```

**预计工时**: 30分钟

---

#### T-05: EnhancedPhaseScanOptimizer 5阶段完全集成

**现状**: 已有3阶段(phase1/phase2/coupling)，缺少phase4(回溯修复)和phase5(δ敏感性)

**集成点**: `enhanced_phase_scan.py:main()` 在阶段3后添加

**修复代码模板**:
```python
# T-05修复: EnhancedPhaseScanOptimizer 5阶段完全集成（手册15.5节）
# 阶段4: 回溯修复验证
print("\n[T-05] 阶段4: 回溯修复验证...")
try:
    from ali2026v3_trading.参数池.enhanced_phase_scan import BacktestRepairValidator
    brv = BacktestRepairValidator()
    repair_result = brv.validate_repair(best_strategy, train_data, test_data)
    print(f"[T-05] 回溯修复结果: repaired={repair_result.get('repaired', False)}")
except Exception as _t05_p4_err:
    logger.warning("[T-05] 阶段4回溯修复失败: %s", _t05_p4_err)

# 阶段5: δ敏感性分析
print("\n[T-05] 阶段5: δ敏感性分析...")
try:
    from ali2026v3_trading.参数池.enhanced_phase_scan import DeltaSensitivityAnalyzer
    dsa = DeltaSensitivityAnalyzer()
    sensitivity_result = dsa.analyze(best_strategy, delta_range=0.05)
    print(f"[T-05] δ敏感性分析完成: sensitive_params={len(sensitivity_result.get('sensitive', []))}")
except Exception as _t05_p5_err:
    logger.warning("[T-05] 阶段5δ敏感性分析失败: %s", _t05_p5_err)

logger.info("[T-05] EnhancedPhaseScanOptimizer 5阶段完全集成完成")
```

**预计工时**: 1小时

---

#### T-06: LHS采样维度一致性修复

**问题**: `_n_dims_for_lhs=35`但实际只采样7维

**修复位置**: `optuna_multiobjective_search.py`

**修复代码**:
```python
# T-06修复: LHS采样维度从35修正为27，与实际参数空间一致
_n_dims_for_lhs = 27  # 原35，修正为实际参数维度27

# 确保latin_hypercube_sample使用正确的维度
lhs_samples = latin_hypercube_sample(n_samples=105, n_dims=_n_dims_for_lhs)
```

**预计工时**: 15分钟

---

#### T-07: SurvivalBiasTest集成

**集成点**: `advanced_validation.py` 或 `enhanced_phase_scan.py:main()`

**修复代码模板**:
```python
# T-07修复: SurvivalBiasTest集成 — 生存偏差检验（手册18节）
try:
    from ali2026v3_trading.参数池.advanced_validation import SurvivalBiasTest
    sbt = SurvivalBiasTest()
    print("\n[T-07] 启动生存偏差检验...")
    
    # Bootstrap偏差校正
    bootstrap_result = sbt.bootstrap_bias_correction(train_data, n_bootstraps=100)
    print(f"[T-07] Bootstrap校正结果: bias={bootstrap_result.get('bias', 0):.4f}")
    
    # 生存率分析
    survival_rate = sbt.survival_rate_analysis(phase2_results, threshold_sharpe=1.0)
    print(f"[T-07] 生存率分析: {survival_rate*100:.1f}%策略通过阈值")
    
    logger.info("[T-07] SurvivalBiasTest集成完成")
except Exception as _t07_err:
    logger.warning("[T-07] SurvivalBiasTest集成失败(非致命): %s", _t07_err)
```

**预计工时**: 30分钟

---

#### T-08: ParameterProximityTracker集成

**集成点**: `advanced_validation.py` 或 `optuna_multiobjective_search.py`

**修复代码模板**:
```python
# T-08修复: ParameterProximityTracker集成 — 边界接近检测（手册18节）
try:
    from ali2026v3_trading.参数池.advanced_validation import ParameterProximityTracker
    ppt = ParameterProximityTracker()
    print("\n[T-08] 启动参数边界接近检测...")
    
    # 检测最优参数是否接近搜索空间边界
    boundary_check = ppt.check_boundary_proximity(best_strategy, search_space)
    print(f"[T-08] 边界接近检测结果: near_boundary={boundary_check.get('near_boundary', False)}")
    
    # 参数空间密度分析
    density_result = ppt.parameter_space_density(phase2_results)
    print(f"[T-08] 参数空间密度: high_density_regions={density_result.get('high_density_count', 0)}")
    
    logger.info("[T-08] ParameterProximityTracker集成完成")
except Exception as _t08_err:
    logger.warning("[T-08] ParameterProximityTracker集成失败(非致命): %s", _t08_err)
```

**预计工时**: 30分钟

---

#### T-09: CheckpointManager集成

**集成点**: `optuna_multiobjective_search.py:run_enhanced_optuna_optimization()`

**修复代码模板**:
```python
# T-09修复: CheckpointManager集成 — 检查点管理（手册18节）
try:
    from ali2026v3_trading.参数池.advanced_validation import CheckpointManager
    cm = CheckpointManager(checkpoint_dir="checkpoints/optuna")
    print("\n[T-09] 启动检查点管理...")
    
    # 保存优化进度
    cm.save_checkpoint({
        'trial_number': trial.number,
        'best_params': trial.params,
        'best_value': trial.value,
        'timestamp': datetime.now().isoformat(),
    })
    
    # 查找最新检查点（用于恢复）
    latest_checkpoint = cm.find_latest()
    if latest_checkpoint:
        print(f"[T-09] 找到最新检查点: {latest_checkpoint}")
    
    logger.info("[T-09] CheckpointManager集成完成")
except Exception as _t09_err:
    logger.warning("[T-09] CheckpointManager集成失败(非致命): %s", _t09_err)
```

**预计工时**: 30分钟

---

### 2.2 评判系统E-04~E-13集成方案

#### E-04: governance_engine checker集成到judge()

**集成点**: `策略评判/strategy_judgment_engine.py:judge()`

**修复代码模板**:
```python
# E-04修复: governance_engine checker集成到评判流程（手册V7.4节）
try:
    from ali2026v3_trading.governance_engine import get_governance_engine
    ge = get_governance_engine()
    print("\n[E-04] 启动治理引擎检查...")
    
    # 运行所有checker
    governance_results = ge.run_all_checkers(backtest_result)
    
    # 收集blockers
    for checker_name, result in governance_results.items():
        if not result.get('passed', True):
            blockers.append(f"[E-04 Governance] {checker_name}失败: {result.get('reason', '')}")
    
    print(f"[E-04] 治理引擎检查完成: {len(governance_results)}个checker")
    
    logger.info("[E-04] governance_engine集成完成")
except Exception as _e04_err:
    logger.warning("[E-04] governance_engine集成失败(非致命): %s", _e04_err)
```

**预计工时**: 1小时

---

#### E-05~E-13: 评判系统增强类集成

类似模式，在`strategy_judgment_engine.py:judge()`中添加调用：

```python
# E-05: ParameterDriftDetector
# E-07: 4个维度提取函数
# E-09: ChicoryEvictionPolicy
# E-10: MarqueeThreshold
# E-11: StrategyViolationTracker
# E-12: activity_weighted_score
# E-13: StateEDensityDecayTracker
```

**预计总工时**: 约5小时

---

## 三、执行时间表

### 第1天（今天）
- ✅ T-03: MultiPeriodCrossValidator集成（已完成）
- ⏳ T-04: MultiParameterTracer集成（30min）
- ⏳ T-05: EnhancedPhaseScanOptimizer 5阶段（1h）
- ⏳ T-06: LHS采样维度修复（15min）
- ⏳ T-07: SurvivalBiasTest集成（30min）
- ⏳ T-08: ParameterProximityTracker集成（30min）
- ⏳ T-09: CheckpointManager集成（30min）

**第1天小计**: 约4小时，完成测试系统6项

### 第2-3天
- E-04~E-13: 评判系统9项集成
- 预计工时: 约10小时

### 第4-5天
- 生产系统P1/P2剩余约33项
- 预计工时: 约15小时

### 第6天
- 测试/评判系统P1/P2剩余约20项
- 预计工时: 约8小时

### 第7天
- 回归测试 + 验收
- 预计工时: 约4小时

**总计**: 约41小时（约6个工作日）

---

## 四、验收标准执行

每项修复完成后，必须执行以下验收步骤：

### 验收清单

```markdown
## [问题编号] 验收记录

### 1. 代码存在 — py_compile通过
- [ ] 运行 `python -m py_compile <file>`
- [ ] 输出无错误

### 2. 代码可用 — try/except兜底
- [ ] 代码包含异常处理
- [ ] 降级路径合理（warning而非error）

### 3. 代码被调用 — grep确认
- [ ] `grep -r "<func_name>(" ali2026v3_trading/ --include="*.py" | grep -v "def <func_name>"`
- [ ] 返回至少1个调用点
- [ ] 调用点位于主流程（非测试文件）

### 4. 参数传递贯通 — 人工审查
- [ ] 绘制参数流向图：定义→传递→消费
- [ ] 确认无断裂点
- [ ] 记录关键参数名

### 5. 参数改变→结果改变 — 实证测试
- [ ] 修改输入参数值
- [ ] 观察输出是否变化
- [ ] 记录测试结果（截图或日志）
```

---

## 五、风险控制

### 5.1 主要风险

1. **回归风险**: 新增调用可能破坏现有逻辑
   - **缓解**: 每项修复后运行简单回测验证

2. **性能风险**: 频繁调用可能影响性能
   - **缓解**: 添加缓存机制，限制调用频率

3. **时间风险**: 69项工作量巨大
   - **缓解**: 优先修复P0级，P1/P2可延后

### 5.2 质量保证

1. **逐项验收**: 每项修复后立即验收
2. **每日总结**: 每天结束生成分进度报告
3. **周度回顾**: 每周检查整体进度和质量

---

## 六、下一步行动

### 立即执行（现在）
1. ✅ T-03已完成
2. 继续T-04~T-09集成
3. 每项完成后立即验收

### 今日目标
- 完成测试系统T-04~T-09全部6项
- 生成第1天进度报告

### 本周目标
- 完成评判系统E-04~E-13全部9项
- 开始生产系统P1/P2修复

---

> **执行人**: AI Assistant  
> **执行日期**: 2026-05-24起  
> **预计完成日期**: 2026-05-30（7天）  
> **总工作量**: 约41小时  
> **验收标准**: 5项全通过才算完成
