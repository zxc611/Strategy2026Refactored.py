# API Reference

> Auto-generated by `tools/doc_sync.py`. Do not edit manually.

## config

### `__init__.py`

config/子系统(P2-S1)

### `_constants.py`

配置层无依赖常量 — R1-2修复: 提取CACHE_TTL到无依赖模块，打破config_params↔strategy_config循环导入

### `_params_attribute_matrix.py`

参数服务 - 属性矩阵验证 (AttributeMatrixMixin)

#### class `AttributeMatrixService`

参数服务 - 属性矩阵验证（从AttributeMatrixMixin重构为独立Service）

- **`load_attribute_matrix`** — 加载 parameter_attribute_matrix.yaml 并校验当前参数

YAML为主源，JSON仅作fallback。计划统一到YAML单一源

Returns:
    校验报告 dict，含 violations/warnings/checked_count
- **`validate_with_attribute_matrix`** — 基于 attribute_matrix 校验当前 _params 中的所有参数

校验项:
1. 类型校验 (type)
2. 范围校验 (range)
3. 依赖约束 (dependencies)
4. 互斥规则 (mutual_exclusion)
5. L1_safety 层运行时只读校验 (editable=runtime_readonly)

Returns:
    {'violations': [...], 'warnings': [...], 'checked_count': int}
- **`eval_dynamic_param`** — 根据动态表达式计算参数值

当参数的 attribute_matrix 中定义了 `dynamic_expr` 时，
根据运行时 context（如实时波动率、时间、持仓比例等）计算参数值。
若计算失败或表达式不存在，降级返回静态 default 值。

dynamic_expr 示例（在 YAML 中定义）：
    signal_cooldown_sec:
      dynamic_expr: "base * (1 + (iv_percentile - 50) / 100)"
      dynamic_depends: [base, iv_percentile]

Args:
    key: 参数键名
    context: 运行时变量字典，如 {'iv_percentile': 65, 'volatility': 0.25}

Returns:
    计算后的参数值；无动态表达式则返回当前静态值

### `_params_canary_env.py`

参数服务 - 金丝雀/环境/归档

- **`func:get_canary_config`** — P2修复: 获取灰度发布配置
- **`func:update_canary_config`** — P2修复: 更新灰度发布配置

Args:
    **kwargs: 要更新的配置项
- **`func:should_apply_canary`** — P2修复: 判断是否应对指定品种应用灰度策略

Args:
    instrument_id: 品种ID

Returns:
    True表示该品种应走灰度路径
- **`func:hot_update_with_canary`** — P2修复: 带灰度能力的热更新

根据灰度配置决定是否将更新应用到参数。

Args:
    params: 参数字典
    updates: 更新内容
    instrument_id: 品种ID（用于灰度判断）

Returns:
    Dict: {applied: bool, canary: bool, updated_keys: list}
- **`func:get_env_profile`** — P2修复: 获取环境配置档案

Args:
    env_name: 环境名称 (development/testing/production)

Returns:
    环境配置字典
- **`func:diff_env_profiles`** — P2修复: 对比两个环境的配置差异

Args:
    env1: 第一个环境名称
    env2: 第二个环境名称

Returns:
    Dict: {only_in_env1, only_in_env2, value_diffs}
- **`func:archive_params`** — P2修复: 归档参数快照到磁盘

Args:
    params: 要归档的参数
    archive_dir: 归档目录（默认为项目logs/param_archives）
    label: 归档标签（如版本号）

Returns:
    归档文件路径，失败返回None
- **`func:list_param_archives`** — P2修复: 列出参数归档文件

Args:
    archive_dir: 归档目录
    limit: 返回数量限制

Returns:
    归档文件路径列表

### `_params_core.py`

参数服务 - 核心类 (ParamsService)

#### class `ParamsService`

参数服务 - 统一的参数配置管理（Facade组合，消灭Mixin继承）

职责：
1. 参数加载与缓存
2. 参数验证与类型转换
3. 输出控制
4. 参数表管理
5. 变更通知
6. 环境变量支持

使用方式：
    service = ParamsService()
    params = service.load_params("param_table.json")

- **`__init__`** — 初始化参数服务

Args:
    logger_func: 日志输出函数（可选）
- **`_on_data_format_changed`** — DFG-P1-08修复: 消费数据格式变更事件，刷新合约缓存

当数据格式发生变更时（如schema版本升级），
触发合约缓存重建，确保运行时数据与数据库一致。
- **`load_params`** — 加载参数表

YAML为主源，JSON仅作fallback。计划统一到YAML单一源

Args:
    path: 参数表文件路径
    force: 是否强制刷新

Returns:
    参数字典
- **`force_refresh`** — 强制刷新参数缓存

Args:
    path: 指定路径，None 表示刷新所有
    
Returns:
    bool: 是否成功刷新
- **`reload_params`** — P1-R9-15修复: 配置热重载机制
- **`save_params`** — 保存参数表

Args:
    path: 参数表文件路径
    params: 参数字典

Returns:
    是否成功
- **`compute_params_md5`** — 手册15节要求: 策略启动时打印参数文件MD5哈希，确保运行时配置与预期一致。
- **`resolve_param_version`** — 手册15节要求: 支持通过环境变量或CLI指定参数版本(如--param-version v1.2)，便于快速回滚。
优先级: CLI > 环境变量 > 默认路径
- **`verify_params_startup`** — 启动时综合验证: MD5哈希 + 参数完整性检查
Returns: {'md5': str, 'param_count': int, 'issues': list}
- **`load_from_env`** — 从环境变量加载参数

注意：环境变量前缀应与config_service区分，避免冲突。
推荐使用"PARAM_"作为前缀，config_service使用"CONFIG_"。

Args:
    prefix: 环境变量前缀

Returns:
    加载的参数数量
- **`get`** — 获取参数值（线程安全）- 委托给__getitem__+默认值
- **`set`** — 设置参数值（线程安全）

Args:
    key: 参数键
    value: 参数值
    notify: 是否通知观察者
- **`get_int`** — 获取整数参数（带范围验证）

Args:
    key: 参数键
    default: 默认值
    min_val: 最小值（可选）
    max_val: 最大值（可选）

Returns:
    整数参数值
- **`get_float`** — 获取浮点数参数（带范围验证）

Args:
    key: 参数键
    default: 默认值
    min_val: 最小值（可选）
    max_val: 最大值（可选）

Returns:
    浮点数参数值
- **`get_bool`** — 获取布尔参数（线程安全）
- **`get_str`** — 获取字符串参数（线程安全）
- **`get_list`** — 获取列表参数（线程安全）
- **`get_dict`** — 获取字典参数（线程安全）
- **`update`** — 批量更新参数

Args:
    params: 参数字典
    notify: 是否通知观察者
- **`hot_update_begin`** — UPG-P1-07修复: 热更新开始，备份当前参数状态

在热更新操作前调用，将当前参数快照保存到_param_backup。
配合hot_update_commit()或hot_update_rollback()使用。

Returns:
    Dict: 备份的参数快照
- **`hot_update_commit`** — UPG-P1-07修复: 热更新提交，清除备份

热更新成功后调用，清除_param_backup备份。
- **`hot_update_rollback`** — UPG-P1-07修复: 热更新回滚，恢复到备份状态

热更新失败时调用，将参数恢复到hot_update_begin()时的快照。

Returns:
    bool: 是否成功回滚
- **`hot_update`** — UPG-P1-07/09修复: 事务性热更新参数

自动执行备份→更新→提交/回滚的完整事务流程。
更新失败时自动回滚到更新前的状态。

Args:
    params: 要更新的参数字典
    notify: 是否通知观察者

Returns:
    Dict: {success: bool, updated_keys: list, rolled_back: bool, error: str}
- **`apply_from_object`** — 从对象属性应用参数

Args:
    obj: 目标对象
    notify: 是否通知观察者
- **`apply_to_object`** — 将对象属性同步到参数（优化版：只遍历参数键）

Args:
    obj: 源对象
- **`add_observer`** — 添加观察者
- **`remove_observer`** — 移除观察者
- **`register_param_change_callback`** — P1-05修复: 注册到EventBus单通道
- **`_fire_param_change_callbacks`** — P1-05修复: 统一通过EventBus发布参数变更事件
- **`_notify_observers`** — P1 Bug #48修复：通知所有观察者（避免在锁内调用外部回调导致死锁）

注意：调用者必须已持有self._lock！
修复方案：复制观察者列表后，在锁外逐个调用回调
- **`get_output_config`** — 获取输出配置
- **`should_output`** — 判断是否应该输出
- **`_log`** — 输出日志
- **`clear_cache`** — 清空缓存
- **`get_all`** — 获取所有参数（线程安全）
- **`keys`** — 获取所有参数键（线程安全）
- **`__contains__`** — 支持 `in` 操作符（线程安全）
- **`__getitem__`** — 支持 `[]` 取值（线程安全）
- **`__setitem__`** — 支持 `[]` 赋值（线程安全）
- **`__len__`** — 支持 `len()` （线程安全）
- **`get_shadow_params`** — 获取影子策略参数子集

返回key以'shadow_'开头的参数以及影子策略核心配置:
- shadow_a/reverse相关参数
- shadow_b/random相关参数
- alpha衰减监测参数

Returns:
    Dict[str, Any]: 影子策略参数字典
- **`get_quantitative_params`** — 获取量化策略参数子集

返回量化策略核心配置:
- 凯利公式参数(kelly_fraction等)
- 风控参数(max_drawdown等)
- 延迟/时间参数
- TVF六维因子参数

Returns:
    Dict[str, Any]: 量化策略参数字典
- **`get_intuition_params`** — 获取直觉策略参数子集

返回箱体/弹簧/共振等直觉判断策略参数:
- box_开头的箱体参数
- spring_开头的弹簧参数
- resonance_开头的共振参数
- 状态参数(correct/incorrect/other)

Returns:
    Dict[str, Any]: 直觉策略参数字典

- **`func:reset_params_service`** — R27-CP-03-FIX: 重置ParamsService单例，用于测试隔离
- **`func:get_params_service`** — 获取全局 ParamsService 单例

Returns:
    ParamsService: 全局参数服务实例
    
Raises:
    RuntimeError: 如果单例初始化失败
- **`func:get_params_by_category`** — 按参数分类获取参数子集

Args:
    category: 参数分类名称，支持 'shadow'/'quantitative'/'intuition'

Returns:
    Dict[str, Any]: 对应分类的参数字典
- **`func:get_params_by_risk_level`** — 按风险等级筛选参数

Args:
    risk_level: 风险等级，支持 'low'/'medium'/'high'/'all'

Returns:
    Dict[str, Any]: 符合风险等级的参数字典
- **`func:get_params_by_strategy_type`** — 按策略类型筛选参数

Args:
    strategy_type: 策略类型，支持 'box_extreme'/'spring'/'trend'/'arbitrage'/'market_making'/'all'

Returns:
    Dict[str, Any]: 符合策略类型的参数字典
- **`func:_read_param`** — 接口唯一修复：统一为对象属性访问，dict先转SimpleNamespace
- **`func:get_param_value`** — 接口唯一修复：统一对象访问入口，dict先转换，alt_keys为降级键名

### `_params_dataclasses.py`

参数服务 - 数据结构定义

#### class `OutputConfig`

输出配置 — 扩展自config_service.OutputConfig，增加交易输出控制

#### class `ParamObserver`

参数观察者接口

- **`on_param_changed`** — 参数变更回调

#### class `ParamAuditObserver`

参数修改审计观察者 —— 金融合规红线

记录结构化审计日志，包含：谁/何时/改了什么/旧值→新值/调用栈摘要。
审计日志写入专用 logger (params_audit)，可与普通日志分离归档。

### `_params_instrument_cache.py`

参数服务 - 合约缓存管理 (InstrumentCacheMixin)

#### class `InstrumentCacheService`

参数服务 - 合约缓存管理（从InstrumentCacheMixin重构为独立Service）

- **`init_instrument_cache`** — 初始化合约缓存容器

✅ ID三层架构 - 第二层：运行时缓存
- 5.1.2 instrument_id_to_internal_id: instrument_id -> internal_id
- 5.1.1 instrument_meta_by_id: internal_id -> InstrumentMeta
- **`_normalize_id_field`** — ID字段标准化：删除旧id字段（统一使用internal_id）
- **`cache_instrument_info`** — 写入缓存并强制 internal_id 全局唯一，禁止静默覆盖

✅ ID三层架构：同时更新两层运行时缓存
- 5.1.2 instrument_id_to_internal_id: instrument_id -> internal_id
- 5.1.1 instrument_meta_by_id: internal_id -> InstrumentMeta

Args:
    instrument_id: 合约代码
    info: 合约信息
    
Returns:
    缓存后的合约信息
    
Raises:
    RuntimeError: 检测到 internal_id 冲突时抛出
- **`get_internal_id`** — ✅ 5.1.2: 通过 instrument_id 获取 internal_id

Args:
    instrument_id: 平台原始合约代码（如 'IH2605'）
    
Returns:
    internal_id: 系统内部代理键，如果不存在则返回 None
- **`get_instrument_meta`** — ✅ 5.1.1: 通过 internal_id 获取 InstrumentMeta

Args:
    internal_id: 系统内部代理键
    
Returns:
    InstrumentMeta: 合约元数据，如果不存在则返回 None
- **`get_instrument_meta_by_id`** — ✅ 便捷方法：通过 instrument_id 直接获取 InstrumentMeta

Args:
    instrument_id: 平台原始合约代码
    
Returns:
    InstrumentMeta: 合约元数据，如果不存在则返回 None
- **`validate_contracts_loaded`** — ✅ 强制验证：确保配置表中的所有合约都已加载到缓存

Args:
    contract_ids: 需要验证的合约ID列表
    contract_type: 合约类型描述（用于错误提示）
    
Returns:
    failed_contracts: 加载失败的合约列表
    
Raises:
    RuntimeError: 如果有合约加载失败
- **`_lazy_load_from_db`** — 惰性从数据库加载合约缓存（仅当缓存为空时执行）
- **`get_id_cache`** — [DEPRECATED] 请使用 get_instrument_meta(internal_id)

UPG-P1-05修复: 添加migration_guide参数
- **`get_product_cache`** — 获取品种信息
- **`clear_instrument_cache`** — BP-9修复：原子替换模式——先构建新缓存，再一次性替换，避免清空窗口期。
如果重新加载失败，保留旧缓存（宁用过期数据不丢数据）。
- **`_rebuild_instrument_cache`** — 构建全新的合约缓存并原子替换（内部方法，复用load_caches_from_db）
- **`get_all_instrument_cache`** — 返回当前合约缓存的深拷贝副本（基于新架构）。
- **`get_all_instrument_ids`** — 返回当前缓存中的全部合约代码（基于新架构）。
- **`load_instrument_list`** — 加载合约列表（从参数缓存或输出文件）

Args:
    params: 参数对象（dict或对象）
    source: 加载来源，'param_cache' 或 'output_files'
    
Returns:
    Dict: {'futures_list': [...], 'options_dict': {...}} 或 None
- **`cache_instrument_list`** — 缓存合约列表到参数对象

Args:
    params: 参数对象（dict或对象）
    futures_list: 期货合约列表
    options_dict: 期权合约字典
    source: 缓存来源标识
- **`cache_column_names`** — 缓存表列名
- **`get_cached_column_names`** — 获取缓存的表列名
- **`load_caches_from_db`** — 从数据库加载合约缓存

✅ 恢复设计：从合约配置表直接加载所有合约品种，创建元数据表

### `_params_migration.py`

参数服务 - 参数迁移

- **`func:migrate_params_rename`** — UPG-P1-02修复: 参数重命名迁移

将旧参数名映射到新参数名，保留旧参数值。
如果新参数名已存在，则不覆盖。

Args:
    params: 参数字典（将被原地修改）
    old_key: 旧参数名
    new_key: 新参数名
    version: 迁移版本号（用于日志记录）

Returns:
    Dict: 迁移结果 {migrated: bool, old_key: str, new_key: str, value: Any}
- **`func:migrate_params_default_change`** — UPG-P1-02修复: 参数默认值变更迁移

当参数的默认值发生变更时，将使用旧默认值的参数更新为新默认值。
如果用户已自定义该参数值（不等于旧默认值），则不覆盖。

Args:
    params: 参数字典（将被原地修改）
    key: 参数名
    old_default: 旧默认值
    new_default: 新默认值
    version: 迁移版本号
    force_update: 是否强制更新（即使值不等于旧默认值也更新）

Returns:
    Dict: 迁移结果 {migrated: bool, key: str, old_value: Any, new_value: Any}
- **`func:migrate_params_type_change`** — UPG-P1-02修复: 参数类型转换迁移

将参数值从旧类型转换为新类型。

Args:
    params: 参数字典（将被原地修改）
    key: 参数名
    old_type: 旧类型名称（如 'str', 'int', 'float', 'bool'）
    new_type: 新类型名称
    type_converter: 自定义类型转换函数，签名 converter(old_value) -> new_value
    version: 迁移版本号

Returns:
    Dict: 迁移结果 {migrated: bool, key: str, old_value: Any, new_value: Any, error: str}
- **`func:apply_param_migration_plan`** — UPG-P1-02修复: 执行参数迁移计划

按顺序执行一组参数迁移操作，支持重命名/默认值变更/类型转换。

Args:
    params: 参数字典（将被原地修改）
    migration_plan: 迁移计划列表，每项格式:
        {
            'action': 'rename'|'default_change'|'type_change',
            'version': '迁移版本号',
            # rename: old_key, new_key
            # default_change: key, old_default, new_default, force_update(可选)
            # type_change: key, old_type, new_type, type_converter(可选)
        }

Returns:
    Dict: 迁移报告 {total: int, migrated: int, skipped: int, failed: int, details: list}
- **`func:_record_param_migration`** — UPG-P1-02修复: 记录参数迁移历史
- **`func:get_param_migration_history`** — UPG-P1-02修复: 获取参数迁移历史记录

Args:
    limit: 返回的最大记录数

Returns:
    List[Dict]: 迁移历史记录列表
- **`func:check_default_value_compatibility`** — UPG-P1-12修复: 检查参数默认值变更的兼容性

在升级参数默认值之前，检查变更是否会导致兼容性问题：
1. 新增参数（old中不存在）- 安全
2. 删除参数（new中不存在）- 需要确认
3. 默认值变更 - 需要评估影响
4. 类型变更 - 需要评估影响

Args:
    old_defaults: 旧默认值字典
    new_defaults: 新默认值字典
    current_params: 当前运行时参数（可选，用于检查用户自定义值）

Returns:
    Dict: 兼容性报告 {
        compatible: bool,
        added_keys: list,       # 新增参数
        removed_keys: list,     # 删除参数
        changed_defaults: list, # 默认值变更
        type_changes: list,     # 类型变更
        warnings: list,         # 警告
        breaking_changes: list, # 破坏性变更
    }
- **`func:test_migration`** — P2修复: 迁移脚本测试函数

在测试参数副本上执行迁移计划，验证迁移是否正确，
不影响实际运行参数。

Args:
    migration_plan: 迁移计划列表
    test_params: 测试参数（默认使用空字典）

Returns:
    Dict: {success: bool, report: dict, original_params: dict, migrated_params: dict}
- **`func:verify_rollback`** — P2修复: 回滚路径验证函数

验证备份参数是否可以正确恢复到原始状态。

Args:
    original_params: 原始参数
    backup_params: 备份参数

Returns:
    Dict: {can_rollback: bool, missing_keys: list, extra_keys: list, value_diffs: list}

### `config_dataclasses.py`

config_dataclasses.py — 配置数据类定义
拆分自 config_service.py (P3大文件拆分方案)

包含:
- PathConfig: 路径配置
- TradingConfig: 交易参数配置
- UIConfig: UI显示配置常量
- OutputConfig: 输出配置
- LoggingConfig: 日志配置
- DatabaseConfig: 数据库配置
- DataPathsConfig: 数据路径配置
- PerformanceConfig: 性能监控配置
- DebugConfig: 调试配置
- StructuredLogEntry: 结构化日志条目
- ConfigField: 参数字段工厂
- LOG_FORMAT / LOG_DATE_FORMAT: 集中日志格式常量

#### class `PathConfig`

路径配置

#### class `TradingConfig`

交易参数配置

#### class `UIConfig`

UI显示配置常量 - 不影响策略逻辑，不作为策略参数暴露

#### class `OutputConfig`

输出配置

#### class `LoggingConfig`

日志配置

- **`setup_async_logging`** — R13-P0-LOG-06修复: 设置QueueHandler异步日志，使用单独线程处理文件写入
- **`stop_async_logging`** — R13-P0-LOG-06修复: 停止异步日志监听器

#### class `DatabaseConfig`

数据库配置

#### class `DataPathsConfig`

数据路径配置

#### class `PerformanceConfig`

性能监控配置

#### class `StructuredLogEntry`

结构化日志条目 — 定义标准字段，确保日志聚合与监控一致

#### class `DebugConfig`

调试配置

### `config_exchange.py`

交易所与合约配置模块 - 从 config_service.py 拆分
职责：交易所配置、品种映射、合约解析工具

#### class `ExchangeConfig`

交易所配置

### `config_exchange_data.py`

Phase3-Sprint8: config_exchange_data.py — 从config_params.py提取的交易所/品种配置数据
包含: month_mapping, delivery_month_rules, tick_size_by_product, exchange_trading_sessions, rollover_days

### `config_facade.py`

ConfigQueryFacade — 配置读操作门面(P0-S3)
ISP遵守：18个纯读消费者通过此Facade访问配置，不含写操作。
所有方法委托给ConfigService单例。

P1-11: 配置管理三层架构说明
==========================
三层优先级和职责边界：

1. config_params（字典缓存）= 权威运行时配置源
   - 模块: config/config_params.py
   - 职责: DEFAULT_PARAM_TABLE + get_cached_params() 提供运行时参数的权威数据
   - 所有参数查询最终回溯到此层

2. config_service（dataclass）= 类型安全查询层
   - 模块: config/config_service_core.py (ConfigService)
   - 职责: 提供类型安全的配置查询接口，结构化配置管理
   - 委托到config_params获取实际参数值

3. config_facade（门面）= 统一入口
   - 模块: config/config_facade.py (ConfigQueryFacade / ConfigCommandFacade)
   - 职责: 对外统一入口，隔离消费者与内部实现细节
   - ISP原则：读操作走ConfigQueryFacade，写操作走ConfigCommandFacade

优先级: config_params > config_service > config_facade（数据流从底向上）

#### class `ConfigQueryFacade`

配置读操作门面 — 所有方法委托给ConfigService单例，禁止写操作

#### class `ConfigCommandFacade`

配置写操作门面 — 所有方法委托给ConfigService单例，禁止自行实现
线程安全：reload()内部_sync_config_data()持锁原子替换_config_data

### `config_loader.py`

config_loader.py — 合并自 config_json_loader / config_yaml_loader / config_option_loader
包含:
  [JSON加载/热重载]
    _resolve_params_json_path, _resolve_runtime_env_name, _apply_env_overrides,
    load_default_params_from_json, check_config_hot_reload
  [YAML加载与迁移]
    _load_yaml_file, load_cascade_threshold_grid, load_plr_thresholds
  [期权参数加载]
    _normalize_option_params_payload, load_option_params_from_file, merge_option_params_to_default

- **`func:_load_yaml_file`** — P2-30修复: 统一使用yaml_safe_load，消除ruamel fallback绕过守卫的风险

### `config_logging.py`

config_logging.py — 日志配置与审计
拆分自 config_service.py (P3大文件拆分方案)

包含:
- FlushHandler: 自定义日志Handler
- AuditLogger: 防篡改审计日志(SHA256哈希链)
- StructuredJsonFormatter: 结构化JSON日志格式化器
- setup_logging: 统一日志初始化
- setup_paths: 路径配置
- get_paths: 获取路径
- check_logging_health: 日志健康检查

#### class `FlushHandler`

自定义 Handler，确保日志定期flush到磁盘
LG-01修复: 移除os.fsync()同步阻塞，改为异步flush+周期性fsync，
避免tick处理路径IO延迟导致风控/下单延迟

#### class `AuditLogger`

R13-P0-LOG-08修复: 防篡改审计日志，每条日志附带前一条的SHA256哈希

计划统一到infra审计工具：本类为日志审计专用，职责边界=防篡改哈希链日志。与risk/safety_meta_audit(风控审计)、param_pool/l1_quantification/meta_audit_engine(参数审计)分工。

- **`_recover_hash`** — 从已有日志文件恢复最后一条的哈希值
- **`log`** — 写入一条审计日志
- **`_delegate_to_structured_audit_log`** — P1-07修复: 委托到risk/_utils.structured_audit_log统一审计入口。
- **`verify_chain`** — 验证审计日志链的完整性

#### class `StructuredJsonFormatter`

结构化JSON日志格式化器 — 替代纯文本格式，便于日志聚合系统消费

- **`func:setup_logging`** — 统一日志初始化
- **`func:setup_paths`** — 路径配置
- **`func:check_logging_health`** — LG-06修复: 日志系统健康检查——检测handler失效、文件不可写等

### `config_params.py`

参数表管理模块- 从config_service.py 拆分
职责：默认参数表、参数缓存、参数加载校验/环境覆盖

#### class `_ParamTableProxyDict`

代理字典 — 对外完全兼容dict，内部委托到ParamTableProvider
启用USE_PARAM_TABLE_PROVIDER FeatureFlag时，DEFAULT_PARAM_TABLE
自动升级为此代理，480处引用方无需任何修改。

- **`func:get_environment_selector`** — R17-P1-CFG-P1-02/07: 环境选择器
- **`func:invalidate_strategy_cache`** — P2-R11-02修复: 清理指定策略的参数缓存 — 委托到strategy_config
- **`func:get_cache_ages`** — [FR-P1-08-FIX] 缓存数据年龄暴露为health指标
- **`func:get_param_age_summary`** — 返回参数年龄监控汇总 — 委托到strategy_config
- **`func:subscribe_param_changes`** — [FR-P1-10-FIX] 参数变更事件订阅 — 委托到strategy_config
- **`func:_notify_param_change`** — [FR-P1-10-FIX] 参数变更事件发布通知 — 委托到strategy_config
- **`func:check_multi_level_cache_consistency`** — [FR-P1-11-FIX] 多级缓存一致性校验 — 委托到strategy_config
- **`func:record_pipeline_latency`** — [FR-P1-12-FIX] 数据管道延迟监控 — 委托到strategy_config

.. deprecated:: R7-2
    请直接从 ali2026v3_trading.strategy.strategy_config 导入 record_pipeline_latency
- **`func:mark_computation_timestamp`** — [FR-P1-13-FIX] 计算结果时间戳标记 — 委托到strategy_config
- **`func:check_indicator_freshness`** — [FR-P1-14-FIX] 指标计算新鲜度校验 — 委托到strategy_config
- **`func:record_indicator_value`** — [FR-P1-14-FIX] 记录指标值和时间戳 — 委托到strategy_config

.. deprecated:: R7-2
    请直接从 ali2026v3_trading.strategy.strategy_config 导入 record_indicator_value
- **`func:check_risk_data_freshness`** — [FR-P1-15-FIX] 风控数据新鲜度校验 — 委托到strategy_config
- **`func:update_risk_data_timestamp`** — [FR-P1-15-FIX] 更新风控数据时间戳 — 委托到strategy_config
- **`func:register_param_change_callback`** — P1-05修复: 统一走EventBus单通道
- **`func:unregister_param_change_callback`** — P1-05修复: 统一走EventBus单通道
- **`func:_notify_param_change`** — P1-05修复: 统一走EventBus单通道发布
- **`func:get_cached_params`** — P1-65修复: 优先委托到ParamsService实例级缓存，消除双写风险
- **`func:update_cached_params`** — P2-R8-07修复: caller_id鉴权，只有受信任调用方可修改参数缓存
- **`func:merge_option_params_to_default`** — 合并期权参数到默认参数表

.. deprecated:: R7-2
    请直接从 ali2026v3_trading.config.config_loader 导入 merge_option_params_to_default
- **`func:_get_config_value_cached`** — 带lru_cache的配置值读取
- **`func:get_param`** — 统一参数读取入口，支持别名解析
- **`func:get_param_default`** — P1-33修复: 从DEFAULT_PARAM_TABLE获取参数默认值（单一数据源）

YAML/JSON配置加载时使用此函数作为fallback，消除三处重复定义。
- **`func:validate_param_range`** — P1-57修复: 统一参数范围校验——全项目唯一权威校验入口

Returns:
    True if value is within valid range, False otherwise.

### `config_service.py`

config_service.py — 合并模块 (原4文件合并)

原拆分为4个子模块:
- config_service.py: re-export门面
- config_service_core.py: 核心配置服务
- config_service_query.py: 配置查询服务
- config_service_reexport.py: 重导出模块

现已合并为单一文件，所有现有import路径零修改。

#### class `ConfigError`

配置错误 — 升A路径T1.3/T2.1: 参数真相源缺失或格式错误时抛出

#### class `ConfigQueryService`

ConfigService只读查询方法（从ConfigServiceQueryMixin重构为独立Service）

- **`_validate_config_schema`** — UPG-10修复: 校验config.json的schema

#### class `ConfigService`

配置服务 - 统一配置管理中心（Facade组合，消灭Mixin继承）

- **`get_param`** — 升A路径T2.1: 统一参数读取接口 — config/params.yaml为唯一真相源

Args:
    key: 点分隔的参数路径，如 "shadow_strategy.absolute_ev_floor"
    default: 键不存在时的默认值

Returns:
    参数值或default

Raises:
    ConfigError: params.yaml缺失时
- **`_load_params`** — 加载config/params.yaml到缓存
- **`reset_cache`** — 重置参数缓存（测试用）

#### class `_ConfigServiceProxy`

- **`func:get_param_provider`** — P2-2-FIX: 返回全局_param_provider单例
- **`func:get_config_service`** — AP-03: 统一单例获取入口
- **`func:get_param`** — 模块级便捷函数 — 升A路径T2.1: 统一参数读取接口

### `config_state_defaults.py`

config_state_defaults.py - 状态参数默认值+配置校验
R27: 从config_params.py提取的状态参数默认值和校验函数

职责：
- 状态参数集默认值 (get_default_state_param_sets)
- 配置schema校验 (validate_config_schema)
- UI参数校验 (validate_ui_params)
- 环境变量文档 (get_documented_env_vars)

- **`func:get_default_state_param_sets`** — 返回状态参数集的默认配置。

供StateParamManager 和ShadowStrategyEngine 统一调用。
避免两处硬编码重复并保持YAML路径一致性。

优先从参数源state_param_sets.yaml 加载。
文件不存在或解析失败时回退到代码内硬编码默认值。

R10-P1-15修复: 添加_yaml_cache缓存机制，仅在文件mtime变化时重新解析。
- **`func:validate_config_schema`** — R13-P1-CFG-09修复: 验证配置字典是否包含必需的key

SER-P1-16修复: 增强类型校验 — 对每个配置项检查类型
(int/float/str/bool/list/dict)，类型不匹配则记录WARNING。

Args:
    config: 待验证的配置字典

Returns:
    Dict: 验证结果 {
        'valid': bool,
        'missing_required': List[str],
        'missing_recommended': List[str],
        'total_keys': int,
        'type_warnings': List[str],
    }
- **`func:get_documented_env_vars`** — R13-P1-CFG-10修复: 返回系统使用的所有环境变量文档

环境变量命名规则: PARAM_OVERRIDE_<KEY> (大写)
优先级：环境变量 > JSON配置 > YAML参数 > 代码默认值

Returns:
    Dict: 环境变量键-> {description, default, type}
- **`func:validate_ui_params`** — 后端UI参数校验层

Args:
    params: 前端传入的参数字典

Returns:
    (is_valid, errors): 校验结果和错误列表

### `config_sync.py`

Phase3-Sprint8: config_sync.py — 从config_params.py提取的配置同步/校验逻辑
包含: _sync_defaults_from_attribute_matrix, verify_param_pool_sync

- **`func:verify_param_pool_sync`** — NEW-R6-10修复: 验证param_pool/与参数池/目录关键文件同步性

### `config_version_tracker.py`

Phase3-Sprint8: config_version_tracker.py — 从config_params.py提取的参数版本追踪逻辑
包含: _record_param_version_snapshot, get_param_version, list_param_version_history, rollback_param_version

### `params_service.py`

参数服务 - 门面模块 (re-export)

### `product_initializer.py`

product_initializer.py — 向后兼容重导出门面

实际实现已迁移至 ali2026v3_trading.lifecycle.product_initializer。
此文件仅保留重导出，确保现有 import 路径不中断。

### `state_param.py`

state_param.py - 状态参数管理器（合并版）

合并自：
- state_param_core.py: StateParamManager类 + 模块级常量 + five_to_three_state + get_state_param_manager
- state_param_manager.py: re-export门面（含state_transition/analytics的re-export）

包含：
- 模块级常量(_STATE_MAP, _STATE_PRIORITY, _THREE_STATE_MAP, STRATEGY_MODE_INCORRECT_REVERSAL_DEFENSIVE)
- five_to_three_state函数
- StateParamManager类
- get_state_param_manager单例函数
- _get_state_param_slots辅助函数
- 从state_transition/analytics的re-export

#### class `StateParamManager`

- **`_compute_life_score`** — P1-1修复: 计算生命状态评分，css_premium_threshold控制CSS权利金加权
- **`check_entry_button`** — P1-2修复: 信号入场确认按钮，信号强度需超过entry_button阈值
- **`register_on_state_switch`** — 注册状态切换回调。P1-05修复: 同时注册到EventBus
- **`should_check_state`** — 判断是否到达状态检查间隔（纯检查，不修改时间戳，避免TOCTOU）
- **`get_params_for_state`** — P2-R11-01修复: 获取指定状态的参数，支持策略级参数隔离

优先级: 策略级覆盖 > 资金规模覆盖 > 动态参数覆盖 > 基础参数集

Args:
    state_key: 状态键名(如correct_trending/incorrect_reversal/other等)

Returns:
    Dict[str, Any]: 合并后的参数字典
- **`_migrate_param_format`** — UPG-P1-02修复: 参数格式迁移

处理参数重命名、类型变更等格式迁移。
当前迁移规则：
- close_take_profit_ratio → take_profit_ratio (保留旧键兼容)
- close_stop_loss_ratio → stop_loss_ratio (保留旧键兼容)
- 数值型字符串自动转为float

Args:
    param_sets: 原始参数集

Returns:
    迁移后的参数集
- **`_check_param_compatibility`** — UPG-P1-09修复: 参数兼容性检查

检查新参数集是否与旧参数集兼容：
1. 关键参数不能缺失
2. 数值型参数不能从有效值变为None
3. 安全关键参数不能越界

Args:
    new_param_sets: 新参数集
    old_param_sets: 旧参数集

Returns:
    List[str]: 兼容性问题列表，空列表表示兼容
- **`reload_param_sets`** — UPG-05/UPG-06修复: 热更新参数集

UPG-05: 使用双缓冲模式，先加载到临时变量，再原子交换，
避免其他线程在reload期间读到空参数。

UPG-06: 加载失败时恢复当前有效值，而非回退到硬编码默认值。

UPG-P1-02: 加载后执行参数格式迁移。

UPG-P1-07/12: 加载后执行兼容性检查，不兼容时自动回滚。

- **`func:five_to_three_state`** — P0-R9-09修复: 五态到三态映射
手册3.1节要求:
{correct_rise, correct_fall} -> correct_trending
{wrong_rise, wrong_fall} -> incorrect_reversal
{other} -> other
- **`func:_get_state_param_slots`** — 已集成: __slots__已在类定义中声明

### `state_transition.py`

state_transition.py - 状态转换（合并模块）

从 state_transition_analytics.py 和 state_transition_capture.py 合并而来，包含：
- StateTransitionEvent: 状态转换事件枚举
- TransitionPoint: 转换点数据类
- StateTransitionCapture: 状态转换捕捉器
- StateTransitionAnalytics: 状态转换分析类

#### class `StateTransitionEvent`

#### class `TransitionPoint`

#### class `StateTransitionCapture`

#### class `StateTransitionAnalytics`

状态转换分析：首次切换记录+驻留时间+转换概率矩阵

首次切换：记录每个品种首次从other→correct_trending的时刻，
这是高频策略最有利可图的入场窗口。
驻留时间：每个状态的停留时长分布，用于优化信号确认窗口。
转换概率矩阵：P(state_t+1 | state_t)，用于预测状态切换概率。

### `tvf_param_loader.py`

TVF参数加载器 — 从参数池配置文件加载六维仓位调整因子参数

支持:
  - 从YAML配置文件加载参数
  - 参数校验 (权重和检查、阈值范围检查)
  - 动态重载 (运行时更新参数)
  - 多模式支持 (small/medium/large可配置不同参数)

#### class `TVFParamLoader`

- **`load`** — 加载TVF参数配置文件

Args:
    config_path: 配置文件路径，默认使用参数池目录下的tvf_params.yaml

Returns:
    加载的参数字典
- **`reload`** — 重新加载参数（运行时热更新）
- **`get_params`** — 获取当前加载的参数
- **`apply_to_mode_config`** — 将TVF参数应用到ModeConfig

注意: ModeConfig是frozen的dataclass，需要创建新实例
- **`_validate_params`** — 校验参数合法性

- **`func:get_tvf_param_loader`** — 获取TVF参数加载器单例
- **`func:load_tvf_params`** — 便捷函数：加载TVF参数

### `ui_mixin.py`

ui_mixin.py - UI逻辑/样式/模式切换 + UI创建/启动 服务

从ui_service.py拆分而来，合并了原ui_mixin.py和ui_mixin_creation.py的全部内容。

重构说明 (2026-06-11):
- _UIMixinLogic → UILogicService（服务提取，消灭Mixin）
- _UIMixinCreation → UICreationService（服务提取，消灭Mixin）
- UIMixin门面改为组合持有UILogicService+UICreationService，不再继承Mixin

#### class `UIEvent`

UI事件记录

#### class `UILogicService`

UI逻辑/样式/模式切换 服务

包含：__init__, _get_ui_lock, 类属性, _refresh_output_mode_ui_styles,
_schedule_output_mode_ui_refresh, set_output_mode, set_auto_trading_mode,
_on_param_modify_click, _on_backtest_click, _call_method_by_priority,
_log_output, _log_info, _log_error, _log_warning,
_destroy_output_mode_ui, _release_runtime_caches, _log_tick_summary

- **`_get_ui_lock`** — 获取UI锁（确保线程安全）
- **`_refresh_output_mode_ui_styles`** — 刷新UI样式
- **`_schedule_output_mode_ui_refresh`** — 调度UI刷新
- **`set_output_mode`** — 设置输出模式
- **`set_auto_trading_mode`** — 设置自动交易模式（统一状态源：以auto_trading_enabled为准）
- **`_on_param_modify_click`** — 打开简易参数编辑器
- **`_on_backtest_click`** — 打开回测参数编辑器
- **`_call_method_by_priority`** — 按优先级尝试调用方法，首个可用者执行

Args:
    method_names: 方法名列表（按优先级从高到低）
    *args, **kwargs: 传递给方法的参数

Returns:
    方法返回值，或None（无可用方法时）
- **`_log_output`** — 统一日志输出入口（按级别分发）
- **`_log_info`** — 记录信息日志
- **`_log_error`** — 记录错误日志
- **`_log_warning`** — 记录警告日志
- **`_destroy_output_mode_ui`** — 销毁输出模式UI
- **`_release_runtime_caches`** — 释放运行时缓存
- **`_log_tick_summary`** — 记录Tick汇总日志

#### class `UICreationService`

UI创建/启动 服务

包含：_create_ui_in_main_thread, _start_output_mode_ui, _schedule_bring_output_mode_ui_front

- **`_create_ui_in_main_thread`** — P2 Bug #80修复：在主线程中创建UI界面

Args:
    root: Tk根窗口
- **`_start_output_mode_ui`** — 启动简易输出模式界面
- **`_schedule_bring_output_mode_ui_front`** — 调度窗口前置

- **`func:safe_getattr_int`** — 安全获取整数属性

### `ui_service.py`

ui_service.py - UI服务模块（门面）

合并来源：16_ui.py
合并策略：提取UI控制核心功能，简化界面逻辑

核心功能：
1. UIMixin - 策略UI混入类(26个方法) — 由_UIMixinLogic + _UIMixinCreation组合
2. StrategyUI - 独立UI控制面板
3. UIDiagnosisTool - UI诊断工具

作者：CodeArts 代码智能体
版本：v2.0
生成时间：2026-03-21

#### class `UIMixin`

策略UI混入类 - 提供Tkinter界面控制功能（Facade组合，消灭Mixin继承）

[P1-2归属] 被Strategy2026(BaseStrategy, UIMixin)继承 | ServiceContainer可选注册为'ui'服务
| 与StrategyCoreService无直接关系(StrategyCoreService仅继承4核心Mixin)
| 重构阶段: UI重构阶段处理(Phase 3+)

组合自:
- UILogicService: UI逻辑/样式/模式切换方法
- UICreationService: UI创建/启动方法

#### class `StrategyUI`

策略UI界面 - 独立运行的控制面板

- **`start`** — 启动UI（非阻塞）
- **`stop`** — 关闭UI
- **`is_alive`** — 检查UI是否运行中
- **`_run_ui`** — UI主线程
- **`_build_control_panel`** — 构建控制面板
- **`_build_status_panel`** — 构建状态面板
- **`_build_log_panel`** — 构建日志面板
- **`_update_loop`** — 更新循环
- **`_update_status`** — 更新状态显示
- **`_process_messages`** — 处理消息队列
- **`_append_log`** — 追加日志
- **`_record_event`** — 记录事件
- **`log`** — 记录日志
- **`update_status`** — 更新状态
- **`_on_pause`** — 暂停回调
- **`_on_resume`** — 恢复回调
- **`_on_flatten`** — 平仓回调
- **`_on_window_close`** — 窗口关闭回调

#### class `UIDiagnosisTool`

UI诊断工具

- **`check_tkinter`** — 检查Tkinter可用性
- **`check_thread_safety`** — 检查线程安全性
- **`run_diagnosis`** — 运行完整诊断

## evaluation

### `activity_weighted_scorer.py`

#### class `ActivityWeightedScorer`

### `cascade_judge.py`

三层瀑布式评判引擎 — 生产级实现

R2-2: 手册V7.5架构对齐命名
  L1 规则引擎  ← 盈亏比门控(发动机)     → gate_l1_rule_engine()
  L2 ML模型    ← 三角验证+统计检验(安全壳) → gate_l2_ml_model()
  L3 影子隐形  ← 小资金约束+影子EV(驾驶座) → gate_l3_shadow_stealth()

与参数池优化脚本的集成方式：
  from ali2026v3_trading.evaluation.cascade_judge import CascadeJudge, adapt_backtest_result
  judge = CascadeJudge()
  adapted = adapt_backtest_result(train_result, test_result, params)
  report = judge.judge(adapted)

设计约束：
  - sortino_ratio: 优先取回测引擎直接返回值，兜底从returns序列计算下行标准差，最保守取sharpe*1.2
  - max_flat_period_days: 优先从nav_curve直接计算，无NAV时安全降级设999避免误判
  - peak_margin_used: 优先取回测引擎直接返回值，兜底从max_risk_ratio估算

#### class `GateResult`

#### class `GateReport`

#### class `BacktestMetrics`

瀑布式评判所需的标准指标集

R4-J-01修复: 补全13维度字段，确保与评判模板一致

#### class `CascadeReport`

R4-J-05修复: 补全标准模板必填字段，确保下游消费不失败

#### class `CascadeJudge`

三层瀑布式评判引擎
盈亏比(发动机) → 三角验证(安全壳) → 小资金约束(驾驶座)

V7.5 三层命名映射表:
┌───────────────────┬──────────────────┬──────────────────────┐
│ 代码命名          │ 手册V7.5命名     │ 等价性论证           │
├───────────────────┼──────────────────┼──────────────────────┤
│ 盈亏比(发动机)    │ L1规则引擎       │ 均基于硬阈值规则过滤 │
│ 三角验证(安全壳)  │ L2 ML模型        │ 均进行多维度交叉验证 │
│ 小资金约束(驾驶座)│ L3影子策略隐形   │ 均在最后阶段施加现实约束│
└───────────────────┴──────────────────┴──────────────────────┘

R14-P1-DOC-P1-12修复: 三层评判阈值手册交叉引用
L1 盈亏比阈值(min_profit_ratio=1.8): V7.0手册§4.2 盈亏比门控线
L2 三角验证阈值(min_sortino=1.5, min_calmar=0.8, min_sharpe=1.2): V7.0手册§4.3 三轴门控线
L3 小资金约束(max_consecutive_losses=5, max_flat_days=20, max_margin_ratio=0.50):
    V7.0手册§5.1 连亏约束 + §5.2 横盘约束 + §7.1 保证金约束
Sigmoid映射参数(center/scale): V7.0手册§6.2 评分映射参数

R21-MEM-P1-16修复: 添加评判历史回收机制，防止长期运行时报告对象累积。

- **`_sync_cascade_defaults`** — P1-R11-24修复: YAML为唯一源，DEFAULT_THRESHOLDS仅作回退

YAML值覆盖DEFAULT_THRESHOLDS（以YAML为准），
YAML缺失的键使用DEFAULT_THRESHOLDS回退。
差异时记录info日志（YAML为准，非warning）。

Args:
    yaml_cfg: 从cascade_config.yaml加载的配置字典

Returns:
    Dict[str, Any]: 补全缺失值后的配置字典
- **`from_config`** — 从cascade_config.yaml加载三系统统一配置

P1-R11-24修复: YAML为唯一源，DEFAULT_THRESHOLDS仅作回退。
启动时YAML值覆盖DEFAULT_THRESHOLDS，缺失值使用DEFAULT_THRESHOLDS回退，
差异以YAML为准并记录info日志。

capital_scale='small'时使用scoring_small权重
params: 可选参数字典，当包含scoring_*_weight键时优先使用(支持参数池扫描覆盖)
- **`_record_report`** — R21-MEM-P1-16修复: 记录评判报告到历史，超限时回收
- **`get_otm_sync_penalty`** — P1-9修复: 消费option_sync_otm_stats，计算OTM同步率惩罚因子

Returns:
    float: 惩罚因子 0.0~1.0，OTM同步率越低惩罚越大
- **`gate_l1_rule_engine`** — L1规则引擎 ← 盈亏比门控(发动机)
- **`gate_l2_ml_model`** — L2 ML模型 ← 三角验证+统计检验(安全壳)
- **`gate_l3_shadow_stealth`** — L3影子隐形 ← 小资金约束+影子EV(驾驶座)
- **`_safe_sigmoid`** — R4-J-02/J-04修复: 安全Sigmoid归一化，防护scale=0导致NaN/除零
R21-MATH-P2-04修复: 已有z裁剪[-500,500]防止exp溢出，确认保护完整
R7-M-13修复: 全局sigmoid_alpha/sigmoid_beta统一参数读取（手册5.2.3节）
- **`_calculate_score`** — 13维度Sigmoid归一化综合评分

R4-J-01修复(本轮补全): 扩展为13维度加权评分
R4-J-02修复: 统一使用_safe_sigmoid防护除零
R4-J-04修复: 分母为零时不再返回NaN

- **`func:gate_result_to_risk_level`** — GateResult→RiskLevel映射函数，确保评判结果可直接映射到生产风控等级
- **`func:_compute_max_flat_from_nav`** — 从净值曲线直接计算最大横盘天数

横盘定义：NAV连续未创新高(tolerance以上)的最大天数
Args:
    nav_curve: 净值序列
    tolerance: 创新高阈值(0.001=0.1%, 日频适用; 分钟频可放宽至0.005)
- **`func:_estimate_l2_confidence`** — P0-21-FIX: 基于回测指标启发式估算ML置信度，替代硬编码0.0

估算逻辑:
- sharpe>=1.2且win_rate>=0.5且plr>=1.5 → 0.75 (高置信)
- sharpe>=0.8且win_rate>=0.45且plr>=1.0 → 0.60 (中置信)
- 否则 → 0.30 (低置信)
- **`func:adapt_backtest_result`** — 将task_scheduler.run_backtest的dict返回值适配为BacktestMetrics

字段推导规则:
- profit_loss_ratio: 直接取train_result
- sharpe_ratio: 直接取train_result
- calmar_ratio: 直接取train_result
- sortino_ratio: 优先取回测引擎直接值，次选从returns序列计算下行标准差，最保守取sharpe*1.2
- max_consecutive_losses: 直接取train_result
- max_flat_period_days: 优先从nav_curve直接计算，无NAV时安全降级999
- peak_margin_used: 优先取回测引擎直接值，兜底从max_risk_ratio估算
- test_profit_loss_ratio: 从test_result提取，用于交叉验证
- overfit_flag: 训练/测试盈亏比差异>50%时标记过拟合

### `chicory_eviction.py`

菊苣驱逐策略 — 策略淘汰评估与生命周期管理

LC-02修复: 增加淘汰编码、复活策略、冷静期、审计日志

#### class `EvictionCode`

LC-02修复: 淘汰编码枚举 — 不同编码对应不同复活条件

#### class `EvictionDecision`

LC-02修复: 淘汰决策数据类

#### class `ChicoryEvictionPolicy`

菊苣驱逐策略 — 三档权重驱逐评分

W0_BASE = 0.60 (基础策略评分权重)
W1_DECAY = 0.25 (活跃度衰减权重, 半衰期30天)
W2_VIOLATION = 0.15 (违反惩罚权重, 每次违反扣0.1)

R21-MATH-P2-04修复: exp参数裁剪防溢出
LC-02修复: 增加淘汰编码、复活策略、冷静期、审计日志

- **`set_audit_log_path`** — LC-02修复: 设置淘汰审计日志路径（WAL模式）
- **`eviction_score`** — 计算淘汰评分

Args:
    base_score: 基础策略评分 [0,1]
    age_days: 策略运行天数
    violation_count: 违规次数

Returns:
    float: 淘汰评分 [0,1]，低于阈值则应淘汰
- **`_determine_eviction_code`** — LC-02修复: 根据评分组成确定淘汰编码
- **`_write_audit_log`** — LC-02修复: 写入淘汰审计日志（WAL模式，不可篡改）
- **`evaluate`** — 评估策略是否应被淘汰

Args:
    strategy_id: 策略ID
    overall_score: 综合评分 [0,1]
    dimensions: 维度评分字典，可包含:
        - age_days: 策略运行天数
        - violation_count: 违规次数
        - base_score: 基础评分（默认使用overall_score）

Returns:
    Dict: 淘汰决策结果

### `marquee_threshold.py`

#### class `MarqueeThreshold`

### `parameter_drift_detector.py`

#### class `ParameterDriftDetector`

### `state_density_decay.py`

#### class `StateEDensityDecayTracker`

### `violation_tracker.py`

#### class `StrategyViolationTracker`

## governance

### `__init__.py`

governance/子系统(P2-S1)

### `compliance_checker.py`

R27-SML-FIX: 合规检查子模块 — 从SafetyMetaLayer提取的监管合规检查职责

#### class `ComplianceChecker`

合规检查器

负责监管合规性检查，包括持仓限额、撤单率、自成交防护、涨跌停、交易时间等。
从SafetyMetaLayer中提取，实现职责分离。

### `governance_engine.py`

#### class `E12ReverseStrategyPseudoIndependenceDetector`

R14-P1-DOC-P1-06修复: E12反向策略伪独立性检测器 — 检测主策略与反向策略PnL相关性过高，判定伪独立

#### class `E13ShadowStrategyCollusionDetector`

R14-P1-DOC-P1-06修复: E13影子策略共谋检测器 — 检测影子策略参数差异过小或信号同步率过高，判定共谋

#### class `MultiStateSwitchBacktestScenario`

#### class `WF6ToWF10EliminationChecker`

R14-P1-DOC-P1-06修复: WF6-WF10淘汰条件检查器 — 检查Sharpe单调递降/参数脆弱性/负EV/Alpha衰减/绝对EV突破

- **`_check_wf10_absolute_ev_breach`** — WF10: 绝对EV阈值突破 — 与WF8区分：WF8检查EV<0，WF10检查EV低于绝对阈值

#### class `E8E9E10EliminationChecker`

R14-P1-DOC-P1-06修复: E8/E9/E10淘汰检查器 — E8尾部风险/E9 Minsky杠杆趋势/E10状态依赖性检测

- **`check_e9_minsky`** — P1-R8-13修复: E9 Minsky时刻检测对齐手册定义
手册定义: "低波动仓位在模拟波动率跳升2倍时回撤>20%"
检测步骤:
  1. 计算滚动波动率（最近N个窗口）
  2. 检测波动率跳升（当前波动率 > 2 × 滚动均值）
  3. 在跳升窗口中检查回撤是否超过阈值

#### class `E7UnexplainedReturnChecker`

R14-P1-DOC-P1-06修复: E7未解释收益检测器 — 检测Greeks归因残差占比是否超过阈值，判定存在未解释收益

- **`check_with_significance`** — 裂缝30修复：统计显著且相对阈值双重条件。

E7触发条件: t检验p<alpha 且 mean(|residual|) > threshold_pct% * mean(|pnl|)

#### class `E11QuantitativeSourceChecker`

R14-P1-DOC-P1-06修复: E11量化来源检查器 — 检测参数来源是否为直觉/猜测/手动设定，要求全部量化来源

#### class `GovernanceEngine`

治理引擎: 聚合所有checker并执行检查/反馈

R4-J-06修复: 检测器参数可配置化 —
通过 config 字典传入各检测器的阈值参数，替代硬编码。

- **`func:create_governance_feedback_channel`** — 聚合所有checker的反馈通道，传递给strategy_judgment_engine
- **`func:get_governance_engine`** — P2-R3-D-20: governance检查器统一调度入口 — 已修复。

get_governance_engine()自动注册所有checker(E7/E8E9E10/E11/E12/E13/WF6-WF10)，
消除了原先各checker独立调用无统一调度的问题。

R4-J-06修复: 支持通过 config 传入检测器阈值参数

### `greeks_calculator.py`

Greeks Calculator - Independent option risk management module.（门面文件）

拆分说明：
- greeks_math.py: _norm_cdf, _norm_pdf, _normalize_option_type, _bs_price, _bs_greeks 数学函数
- greeks_pricers.py: IVCalculator, BinomialTreePricer, MonteCarloPricer, TradingCalendar 定价模型类
- greeks_calculator.py（本文件）: GreeksCalculator类 + validate_greeks_calendar_iv_noise + re-export所有公共API

Features:
- Black-Scholes Greeks calculation (Delta, Gamma, Theta, Vega)
- Implied Volatility (IV) calculation (Newton iteration)
- Binomial tree pricing model (CRR, European/American options)
- Monte Carlo pricing model (GBM simulation, antithetic variance reduction)
- Smart update strategy (time + price change + IV change)
- Portfolio risk aggregation (position-weighted)
- Config file loading
- Trading calendar support

#### class `GreeksCalculator`

Independent Greeks calculator supporting:
- Implied volatility calculation (Newton iteration)
- Hybrid update strategy based on time, price change, and IV change
- Position-level portfolio risk aggregation
- Config file parameter loading
- Comprehensive exception handling and logging

- **`__init__`** — Args:
    config_path: JSON config file path for loading rate, multiplier, dividend yield, update strategy params.
- **`__repr__`** — Debug string representation.
- **`__str__`** — Friendly string representation.
- **`_set_defaults`** — Set default multipliers and dividend yields.
- **`_load_config`** — Load configuration from JSON file.
- **`set_contract_multiplier`** — Set contract multiplier.
- **`set_dividend_yield`** — Set dividend yield.
- **`set_risk_free_rate`** — Set risk-free rate.
- **`set_update_strategy`** — Set update strategy parameters.

Args:
    time_interval_sec: Time interval (seconds)
    price_change_pct: Price change percentage
    iv_change_pct: IV change percentage
    min_tick_interval: Minimum tick interval
- **`set_trading_calendar`** — Set trading calendar.
- **`use_trading_days`** — Enable/disable trading day counting.
- **`cache_option_info`** — Cache option basic info to avoid passing on every tick.

Args:
    instrument_id: Option contract ID
    info: {'strike', 'option_type', 'expiry_date', 'future_product', ...}
- **`_time_to_expiry`** — Calculate annualized time to expiry, supporting trading days.
- **`_parse_strike_from_id`** — Parse strike price from contract ID (fallback method).

Delegates to SubscriptionManager.parse_option to extract strike_price.
- **`_should_update`** — Determine whether Greeks need updating using a hybrid strategy.

Conditions (any one triggers update):
1. Time interval exceeds threshold
2. Price change exceeds threshold
3. IV change exceeds threshold (if current_iv provided)
4. Tick count reaches minimum interval
- **`_should_update_with_cached_iv`** — Delegate to _should_update with cached IV.
- **`calculate_and_update`** — Calculate and update Greeks (full pipeline).
- **`update_greeks_from_tick`** — Update Greeks from option tick data (smart decision on whether to recalculate).
- **`get_iv`** — Get implied volatility for a single contract.
- **`set_iv`** — Manually set IV (from external data source).
- **`get_portfolio_greeks`** — Calculate portfolio Greeks (position-weighted).

Args:
    positions: {instrument_id: position_size}
               Positive = long, negative = short

Returns:
    Dict: {'delta', 'gamma', 'theta', 'vega'} portfolio totals
- **`get_product_summary`** — Summarize Greeks for a specified product.
- **`check_risk_limits`** — Check risk limits.
- **`get_stats`** — Get performance statistics.
- **`clear_cache`** — clear_cache: R10-P1-13 fix

- **`func:validate_greeks_calendar_iv_noise`** — P2-GR-004修复: 验证Greeks日历效应和IV噪声稳定性。

检查:
1. 日历效应: 周末/假日前后IV是否有异常跳变
2. IV噪声: 对IV序列添加高斯噪声(sigma=iv_noise_sigma)后Greeks是否稳定

Args:
    greeks_calculator: GreeksCalculator实例，从中提取缓存的IV数据
    iv_noise_sigma: IV噪声标准差
    n_simulations: 噪声模拟次数
    calendar: TradingCalendar实例（可选，为空则自动创建）

Returns:
    Dict with keys: 'passed' (bool), 'calendar_anomaly_count' (int),
    'iv_noise_max_delta_change' (float), 'details' (str)

### `greeks_math.py`

Greeks数学函数模块 - 正态分布、BS定价、BS Greeks计算

拆分自 greeks_calculator.py，确保所有现有 import 路径零修改。

- **`func:_norm_cdf`** — Standard normal CDF (precision ~1e-5).
- **`func:_norm_pdf`** — 标准正态分布概率密度函数
- **`func:_normalize_option_type`** — ID唯一: 委托shared_utils.normalize_option_type, 不再独立实现
- **`func:_bs_price`** — Black-Scholes option pricing (for IV calculation)

R16-P0-PERF-05: added lru_cache for IV newton iteration
Note: option_type should be normalized before calling
- **`func:_bs_greeks`** — Calculate option Greeks.

Args:
    S: Underlying price
    K: Strike price
    T: Time to expiry (years)
    r: Risk-free rate
    q: Dividend yield (continuous)
    sigma: Implied volatility
    option_type: 'CALL' or 'PUT' (should be normalized before calling)

Returns:
    Dict: {'delta', 'gamma', 'theta', 'vega'}

### `greeks_pricers.py`

Greeks定价模型模块 - IVCalculator、BinomialTreePricer、MonteCarloPricer、TradingCalendar

拆分自 greeks_calculator.py，确保所有现有 import 路径零修改。

#### class `IVCalculator`

Implied volatility calculator using Newton iteration.

- **`implied_volatility`** — Calculate implied volatility.

Args:
    market_price: Market option price
    S: Underlying price
    K: Strike price
    T: Time to expiry (years)
    r: Risk-free rate
    q: Dividend yield
    option_type: 'CALL' or 'PUT'
    initial_guess: Initial guess (default 0.2)
    max_iter: Maximum iterations
    tol: Convergence tolerance

Returns:
    float: Implied volatility, returns 0.2 on failure

#### class `BinomialTreePricer`

Binomial tree option pricing model (CRR)

#### class `MonteCarloPricer`

Monte Carlo option pricing model using GBM with antithetic variance reduction.

- **`price`** — Monte Carlo pricing with optional historical bootstrap distribution.

#### class `TradingCalendar`

Trading calendar for calculating remaining trading days.

- **`__init__`** — Initialize TradingCalendar with optional holidays and MarketTimeService.

Args:
    holidays: List of holiday dates
    market_time_service: MarketTimeService instance (optional)
- **`add_holiday`** — Add a holiday.
- **`is_trading_day`** — Check if a date is a trading day.
- **`trading_days_between`** — Return the number of trading days between two dates.
- **`time_to_expiry`** — Return annualized time to expiry (years).

Args:
    expiry_date: Expiry date
    use_trading_days: Whether to use trading days
    bar_date: Current bar date (pass in backtest, None uses system time)

Returns:
    float: Remaining time (years)

### `mode_config.py`

mode_config - Enum + ModeConfig dataclass + configs + YAML/factory

Split from mode_engine.py - P2 split

#### class `CapitalMode`

#### class `TakeProfitMethod`

#### class `StopLossMethod`

#### class `DrawdownAction`

#### class `PyramidingRule`

#### class `ModeConfig`

- **`func:_validate_yaml_modeconfig_mapping`** — P1-R11-23修复: 验证YAML键名与ModeConfig字段名映射一致性

读取tvf_params.yaml中所有键，检查每个键是否在_YAML_TO_MODECONFIG_MAP中有映射条目。
对未映射的键记录警告，防止参数静默丢失。

Args:
    yaml_params: 从tvf_params.yaml加载的参数字典
- **`func:_get_flattened_param`** — 扁平化缓存查询: 支持'a.b.c'路径查询嵌套字典，结果缓存
- **`func:_make_mode_config`** — ModeConfig工厂函数 — 自动合并TVF公共默认参数，消除重复定义

R14-P1-DOC-P1-05修复: 补全参数列表说明
Args:
    mode: 资金模式(SHARPE_MAX/CONSERVATIVE/AGGRESSIVE)
    max_positions: 最大持仓数
    single_position_cap: 单仓上限占比
    take_profit_method: 止盈方法
    stop_loss_method: 止损方法
    pyramiding: 是否加仓
    pyramiding_rule: 加仓规则
    drawdown_action: 回撤动作
    recovery_target: 恢复目标
    min_signal_strength: 最小信号强度
    time_decay_cutoff: 时间衰减截止
    profitability_mode: 盈利模式
    profitability_weights: 盈利权重元组
    win_loss_ratio_full_score_at: 盈亏比满分阈值
    profit_factor_full_score_at: 利润因子满分阈值
    recovery_efficiency_full_score_at: 恢复效率满分阈值
    max_consecutive_losses_full: 连亏满分阈值
    max_consecutive_losses_zero: 连亏零分阈值
    drawdown_recovery_max_hours: 回撤恢复最大小时
    extreme_max_recovery_hours: 极端最大恢复小时
    overall_pass_threshold: 总体通过阈值
    overall_conditional_threshold: 总体条件阈值
    kelly_fraction: Kelly比例
    min_estimated_plr: 最小估计PLR
    plr_filter_enabled: PLR过滤开关
    consecutive_loss_limit: 连亏限制
    recovery_timeout_seconds: 恢复超时秒数
    asymmetric_risk_enabled: 非对称风险开关
    tvf_enabled: TVF开关
    sharpe_full_score_at: Sharpe满分阈值
    calmar_full_score_at: Calmar满分阈值

Returns:
    ModeConfig: 合并TVF参数后的ModeConfig实例

### `mode_engine.py`

模式切换引擎 — Facade + re-export门面

解决:
  G1: 封装 ModeEngine 统一入口类，聚合各组件模式相关调用
  G2: ModeConfig dataclass 替代 CAPITAL_SCALE_CONFIGS 字典
  G3: 原子性模式切换（预检查 + 回滚机制）
  G4: 凯利公式仓位计算
  G5: time_decay_cutoff 期权时间价值衰减过滤
  G6: ExitRuleEngine / DrawdownManager 显式抽象

架构: 实现已拆分至子模块，本文件为Facade+re-export门面
  - mode_config.py: 枚举 + ModeConfig + _make_mode_config + 配置常量
  - mode_exit_rules.py: ExitRuleEngine + DrawdownManager + DefensiveDrawdownChecker
  - mode_position_sizing.py: SixDimPositionAdjustmentFactor + kelly_* + PredictiveStateEngine
  - mode_engine_propagation.py: ModeEnginePropagationMixin (9个方法)

#### class `ModeEngine`

### `mode_engine_propagation.py`

#### class `ModeEnginePropagationService`

- **`_auto_register_known_components`** — 在__init__中尝试自动注册所有已知组件，避免仅注册单个组件
- **`_sync_component_registry`** — 确保组件注册表与import获取的实例同步
- **`check_auto_recovery`** — 定期检查降级后是否满足恢复条件，满足则自动切回原规模
Returns:
    恢复到的规模字符串，或None（未恢复）

### `mode_exit_rules.py`

mode_exit_rules - ExitRuleEngine + DrawdownManager + DefensiveDrawdownChecker

退出规则与回撤管理 - 文档12.3.5拆分方案

Split from mode_engine.py - P2 split

#### class `ExitRuleEngine`

- **`compute_take_profit_levels`** — 计算止盈价位，支持BUY和SELL两个方向
- **`compute_stop_loss`** — 计算止损价位，支持BUY和SELL两个方向

#### class `DrawdownManager`

回撤管理器 — 含VaR/ATR动态升级机制（R7-M-01修复）

手册第268行要求："动态升级采用历史滚动VaR(95%)或ATR为基准"
当VaR或ATR超过阈值时，自动收紧仓位缩减/暂停/停止条件。

P2-12 优先级规则:
    风控层硬停止（DrawdownMonitorService）优先级高于治理层策略性回撤管理（DrawdownManager）。
    DrawdownManager 的决策应先检查 DrawdownMonitorService 的硬停止状态，
    若硬停止已触发，DrawdownManager 的所有决策方法应直接返回最保守结果。

- **`set_daily_hard_stop`** — P2-12: 设置风控层硬停止状态，由外部DrawdownMonitorService同步
- **`_subscribe_daily_hard_stop_event`** — P2-07修复: 订阅EventBus DailyDrawdownHaltEvent，自动同步硬停止状态。
当DrawdownMonitorService触发日回撤硬停止时，DrawdownManager自动收到通知，
确保治理层决策与风控层硬停止状态一致。
- **`_on_daily_hard_stop_event`** — P2-07修复: EventBus回调——日回撤硬停止事件处理
- **`var_upgrade_active`** — R7-M-01修复: VaR动态升级是否激活
- **`atr_upgrade_active`** — R7-M-01修复: ATR动态升级是否激活
- **`last_var_value`** — 最近一次VaR计算值
- **`last_atr_value`** — 最近一次ATR值
- **`update_var_baseline`** — R7-M-01修复: 计算历史滚动VaR并更新升级标志

手册第268行: "动态升级采用历史滚动VaR(95%)为基准"
当VaR超过阈值时，激活动态升级 → 收紧should_reduce_size/halt_new/full_stop的触发阈值

Args:
    returns: 收益率序列
    confidence: VaR置信水平 (默认0.95=95%)

Returns:
    float: VaR值(正值=潜在损失百分比)
- **`update_atr_baseline`** — R7-M-01修复: 更新ATR基准并检查是否激活动态升级

手册第268行: "或ATR为基准"

Args:
    atr_value: 当前ATR值(相对于价格的比例，如0.015=1.5%)
- **`should_reduce_size`** — R7-M-01修复: VaR/ATR升级时阈值更敏感

P2-12: 若风控层硬停止已触发，直接返回True（最保守）
- **`should_halt_new`** — R7-M-01修复: VaR/ATR升级时暂停新开仓阈值收紧

P2-12: 若风控层硬停止已触发，直接返回True（最保守）
- **`should_full_stop`** — R7-M-01修复: VaR/ATR升级时全停阈值从-5%收紧到-2%

P2-12: 若风控层硬停止已触发，直接返回True（最保守）

#### class `DefensiveDrawdownChecker`

防御性减仓检查器 — 当实时风险指标显著低于入场时触发减仓

- **`check`** — 返回减仓因子 [0, 1]
1.0 = 不减仓, 0.5 = 减仓50%, 0.0 = 清仓

### `mode_position_sizing.py`

mode_position_sizing - SixDimPositionAdjustmentFactor + kelly_fraction + kelly_position_size + PredictiveStateEngine

仓位计算与预测状态 - 文档12.3.5拆分方案

Split from mode_engine.py - P2 split

#### class `SixDimPositionAdjustmentFactor`

六维仓位调整因子 — 将风险指标、订单流、希腊字母内化为仓位乘数

六维体系:
L1 风险收益层 (三角验证): Sortino + Calmar + Sharpe
L2 市场微观层 (订单流): OFI + CVD偏离 + 智能资金流向
L3 期权风险层 (希腊字母): Delta暴露 + Gamma风险 + Theta衰减 + Vega波动

- **`compute_adjustment`** — 计算六维仓位调整因子 [0, 1]
- **`_sigmoid_adjust`** — Sigmoid调整: 阈值处=0.5, 2倍阈值处≈0.88

P-28修复: 市场状态自适应scale — 高波动市场scale放大(更保守)，低波动市场scale缩小(更激进)
R21-MATH-P2-04修复: 添加输入裁剪防止exp溢出

#### class `PredictiveStateEngine`

方向化细分预测状态引擎

将传统二元状态(correct/incorrect)细分为四方向:
- correct_rise: 正确预判+价格上涨 → 满仓进攻
- correct_fall: 正确预判+价格下跌 → 满仓进攻
- wrong_rise: 错误预判+价格上涨 → 减仓防守
- wrong_fall: 错误预判+价格下跌 → 减仓防守

用于ModeEngine仓位调整的方向化因子计算。

- **`classify_state`** — 将预测正确性+价格方向分类为四方向状态

Args:
    prediction_correct: 预测是否正确
    price_direction: 价格变化方向 (1=上涨, -1=下跌)

Returns:
    str: correct_rise/correct_fall/wrong_rise/wrong_fall
- **`get_position_multiplier`** — 获取对应方向的仓位调整乘数

Args:
    state: 四方向状态名

Returns:
    float: 仓位乘数 (0.5~1.0)
- **`record_transition`** — 记录状态转换事件

Args:
    from_state: 源状态
    to_state: 目标状态
    pnl: 本次转换对应的盈亏
- **`get_state_stats`** — 获取四方向状态统计

- **`func:kelly_fraction`** — Kelly公式（含交易成本调整）

R17-07修复: 交易成本从赢利中扣除，公式:
  kelly = (win_rate * (win_loss_ratio - cost_ratio) - (1 - win_rate)) / (win_loss_ratio - cost_ratio)

Args:
    win_rate: 胜率 (0~1)
    win_loss_ratio: 盈亏比
    fraction: Kelly比例系数 (0~1)
    cost_ratio: 单位交易成本占赌注比例 (默认0)

Returns:
    float: Kelly仓位比例 [0, 1]
- **`func:kelly_position_size`** — Kelly仓位计算（含交易成本调整 + 期权买方折扣）

R17-07修复: cost_ratio传递给kelly_fraction进行成本扣除
R7-M-02修复: instrument_type + option_discount 期权买方仓位打折(手册第854行要求0.5~0.7)

### `param_table_provider.py`

Phase3-Sprint8: ParamTableProvider — DEFAULT_PARAM_TABLE解耦
引入Protocol接口+依赖注入，消除14文件129次全局字典直接引用

#### class `ParamTableProvider`

参数表提供者协议 — 替代全局DEFAULT_PARAM_TABLE直接引用

#### class `DefaultParamTableProvider`

默认参数表提供者 — 包装原有DEFAULT_PARAM_TABLE，向后兼容

DEFAULT_PARAM_TABLE是扁平字典(key→value)，不是嵌套字典(strategy→{key→value})。
将整个表视为单个策略'_default'的参数集。

#### class `CachedParamTableProvider`

带LRU缓存的参数表提供者 — P1-41修复: 委托到functools.lru_cache统一缓存策略

- **`func:get_param_table_provider`** — 获取全局ParamTableProvider单例
- **`func:set_param_table_provider`** — 设置全局ParamTableProvider(用于依赖注入/测试)

### `regulatory_compliance.py`

合规规则模块 — 从risk_circuit_breaker.py拆分
职责: 监管合规检查规则（自成交、洗盘、算法交易标识）
         合规规则引擎(ComplianceRule Protocol + 3规则类)

#### class `ComplianceRule`

#### class `ComplianceContext`

#### class `ComplianceResult`

#### class `MaxPositionRule`

#### class `ConcentrationRule`

#### class `CrossMarketRule`

#### class `ComplianceEngine`

#### class `RegulatoryCompliance`

### `validation_registry.py`

Phase4-Sprint12: ValidationRegistry — backtest_validation验证器注册模式
将backtest_validation.py(1689行)的硬编码验证链重构为可注册验证器

#### class `BacktestValidator`

#### class `BacktestResult`

#### class `ValidationResult`

#### class `ValidationReport`

#### class `ValidationRegistry`

验证器注册中心 — 替代backtest_validation硬编码验证链

支持动态注册/注销验证器，按注册顺序执行

#### class `SharpeRatioValidator`

#### class `MaxDrawdownValidator`

#### class `WinRateValidator`

#### class `ProfitLossRatioValidator`

#### class `MinTradesValidator`

## infra

### `__init__.py`

infra/子系统(P2-S1)

### `_backup_restore.py`

backup_restore.py - 备份/恢复/值班 (DuckDBBackupService, OnCallManager, DuckDBRestoreService)

#### class `DuckDBBackupService`

DuckDB定期备份服务

DR-P1-01: 每小时备份一次，保留最近24个备份
DR-P1-03: 生成backup_manifest.json记录备份完整性

- **`backup_duckdb`** — 执行DuckDB备份

DR-P1-01: 每小时执行一次，保留最近24个备份
DR-P1-03: 记录backup_manifest.json

Returns:
    备份文件路径，跳过时返回None
- **`_record_backup_manifest`** — DR-P1-03: 记录备份到backup_manifest.json
- **`verify_backup_integrity`** — DR-P1-03: 验证备份文件完整性
- **`_rotate_backups`** — DR-P1-01: 保留最近MAX_BACKUPS个备份，删除旧的

#### class `OnCallManager`

R20-P1-OPS-01修复: On-call轮值管理

#### class `DuckDBRestoreService`

R20-P1-OPS-02修复: DuckDB恢复服务

### `_disk_monitor.py`

disk_monitor.py - 磁盘空间监控 (DiskSpaceMonitor)

#### class `DiskSpaceMonitor`

R15-P0-RES-08修复: 定期监控磁盘空间，空间不足时告警

- **`check`** — 检查磁盘空间，返回各路径状态
- **`_load_history`** — R16-P1-RES-17修复: 启动时加载历史监控数据
- **`_save_history`** — R16-P1-RES-17修复: 持久化历史监控数据
- **`get_history`** — R16-P1-RES-17修复: 获取历史监控数据

### `_helpers.py`

maintenance_service helpers - 模块级辅助函数和常量

- **`func:_normalize_code`** — 标准化代码（交易所/产品），保留原始格式

Args:
    code: 代码字符串（交易所或产品）

Returns:
    str: 原始格式的代码

### `_ops_automation.py`

ops_automation.py - 自动化函数/压测/SOP

#### class `CapacityStressTestFramework`

OPS-18修复: 容量压测框架

提供系统容量压测能力，验证性能上限。

#### class `StandardOperatingProcedures`

OPS-20修复: 运维标准操作程序(SOP)

定义标准化的运维操作步骤，确保操作一致性和可追溯性。

- **`func:ops_health_check`** — P2修复: 综合运维健康检查

Returns:
    Dict: {overall_status, components: {name: {status, details}}}
- **`func:ops_run_diagnostic`** — P2修复: 运维诊断工具函数

Args:
    category: 诊断类别 (all/disk/eventbus/config/risk)

Returns:
    诊断结果字典
- **`func:ops_auto_repair`** — P2修复: 自动化运维修复函数

Args:
    issue_type: 问题类型 (disk_full/eventbus_stuck/config_stale)

Returns:
    Dict: {repaired: bool, action: str, details: str}
- **`func:generate_ops_report`** — P2修复: 自动生成运维报告

Args:
    report_type: 报告类型 (daily/weekly/incident)

Returns:
    Dict: 运维报告内容
- **`func:estimate_ops_cost`** — P2修复: 运维成本评估函数

估算当前系统的运维资源消耗和成本。

Returns:
    Dict: {storage_cost, compute_cost, estimated_monthly_cost}

### `_ops_framework.py`

ops_framework.py - 运维操作框架 (OpsOperation + OpsOperationManager)

#### class `OpsOperation`

OPS-P1-06~19修复: 运维操作封装对象

每个运维操作封装为一个OpsOperation，包含:
- operation_id: 唯一标识 (OPS-P1-15幂等保证)
- operation_type: 操作类型
- pre_check: 前置条件验证函数 (OPS-P1-18)
- execute: 执行函数
- post_check: 后置条件验证函数 (OPS-P1-19)
- rollback: 回滚函数 (OPS-P1-07回退/OPS-P1-11回滚)
- impact_assess: 影响评估函数 (OPS-P1-08)
- dependency_check: 依赖检查函数 (OPS-P1-17)
- timeout: 超时时间 (OPS-P1-13)
- max_retries: 最大重试次数 (OPS-P1-14)

#### class `OpsOperationManager`

OPS-P1-06~19修复: 统一运维操作框架

提供14项运维操作保障:
- OPS-P1-06: 审批流程 — 操作前需获取审批令牌
- OPS-P1-07: 回退方案 — 记录操作前状态，支持回退
- OPS-P1-08: 影响评估 — 操作前评估影响范围
- OPS-P1-09: 预演机制 — dry_run模式执行
- OPS-P1-10: 结果验证 — 操作后验证预期结果
- OPS-P1-11: 回滚方案 — 操作失败自动回滚
- OPS-P1-12: 通知机制 — 操作开始/完成/失败通知
- OPS-P1-13: 超时控制 — 操作执行超时自动终止
- OPS-P1-14: 重试机制 — 可配置重试次数和间隔
- OPS-P1-15: 幂等保证 — 操作ID去重
- OPS-P1-16: 并发控制 — 操作锁
- OPS-P1-17: 依赖检查 — 前置依赖验证
- OPS-P1-18: 前置条件验证 — pre_condition检查
- OPS-P1-19: 后置条件验证 — post_condition检查

执行流程: pre_check -> dry_run(可选) -> execute -> post_check
失败时: rollback -> 通知
所有操作通过EventBus发布事件

- **`request_approval`** — 请求运维操作审批

Args:
    operation_id: 操作唯一ID
    operation_type: 操作类型
    approver: 审批人
    reason: 审批理由

Returns:
    审批令牌(token)
- **`check_approval`** — 验证审批令牌
- **`assess_impact`** — 评估操作影响范围

Args:
    operation: 运维操作对象

Returns:
    影响评估结果
- **`dry_run`** — 预演模式执行（不实际修改状态）

Args:
    operation: 运维操作对象

Returns:
    预演结果
- **`execute_operation`** — 执行运维操作（完整生命周期）

执行流程: 审批验证 -> 幂等检查 -> 并发控制 -> 依赖检查 ->
         前置验证 -> 影响评估 -> (预演|执行) -> 后置验证 -> 通知

Args:
    operation: 运维操作对象
    approval_token: 审批令牌 (OPS-P1-06)
    dry_run: 是否预演模式 (OPS-P1-09)

Returns:
    执行结果
- **`_get_type_lock`** — OPS-P1-16: 获取操作类型锁
- **`_execute_with_retry_and_timeout`** — OPS-P1-13: 超时控制 + OPS-P1-14: 重试机制

P2-12修复: 委托到 TimeoutGuard + BoundedRetry。
语义: 超时不重试(直接返回失败), 异常才重试。
- **`_capture_snapshot`** — OPS-P1-07: 捕获操作前状态快照
- **`_rollback_operation`** — OPS-P1-07/OPS-P1-11: 执行回滚
- **`_publish_event`** — OPS-P1-12: 通过EventBus发布运维操作事件
- **`_write_audit_log`** — 写入运维审计日志
- **`get_operation_status`** — 查询操作执行状态
- **`get_audit_log`** — 获取最近运维审计日志

- **`func:get_ops_operation_manager`** — 获取全局运维操作管理器单例

### `_ops_knowledge_metrics.py`

ops_knowledge_metrics.py - 知识库/指标/SLA

#### class `OpsMetrics`

P2修复: 运维指标标准化定义

#### class `OpsSLA`

P2修复: 运维SLA常量定义

- **`check_sla_compliance`** — P2修复: 检查SLA合规性

Args:
    metric_name: 指标名称
    actual_value: 实际值

Returns:
    Dict: {compliant: bool, metric: str, actual: float, target: float}

- **`func:search_ops_knowledge`** — P2修复: 搜索运维知识库

Args:
    keyword: 搜索关键词

Returns:
    匹配的知识条目列表
- **`func:get_ops_knowledge`** — P2修复: 获取指定运维知识条目

Args:
    issue_id: 问题ID

Returns:
    知识条目字典，不存在返回None

### `_storage.py`

_storage/统一存储模块 — 合并自3个子模块

#### class `StorageCatalogService`

存储服务 - 目录维护与归档部分（从StorageCatalogMixin重构为独立Service）

- **`backfill_metadata_exchange`** — 回填元数据交易所信息

P2 Bug #111修复：明确返回值含义为"待更新行数"而非"已更新行数"
DuckDB不支持changes()，因此先COUNT待更新行数，再执行UPDATE
- **`repair_option_underlying_product_references`** — 修复 option_instruments.underlying_product 指向旧期货品种或缺失占位符的问题。

P2 Bug #112修复：明确返回值含义为"待更新行数"而非"已更新行数"
DuckDB不支持changes()，因此先COUNT待更新行数，再执行UPDATE
- **`_get_option_format_template`** — 按交易所返回期权合约格式模板。
- **`ensure_option_product_catalog`** — 补齐并激活 option_products 中缺失的真实期权品种配置。
- **`drop_empty_instrument_tables`** — 删除无数据的合约记录（统一表版本：DELETE而非DROP TABLE）
- **`_diagnose_contract`** — 诊断单个合约的状态 (DuckDB 版本)

Returns:
    dict: {
        'subscribed': bool,
        'subscribe_error': str,
        'enqueued': bool,
        'enqueue_error': str,
        'received': bool,
        'receive_error': str,
        'stored': bool,
        'store_error': str,
        'kline_table': str,
        'tick_table': str,
        'kline_count': int,
        'tick_count': int,
    }
- **`backup_database`** — OPS-09修复: 执行DuckDB数据库备份

将DuckDB文件复制到备份目录，文件名带时间戳。
委托给DuckDBBackupService执行实际备份。

Args:
    force: 是否强制执行（忽略时间间隔限制）
    caller_id: 操作人标识

Returns:
    dict: {backup_path, file_size_mb, timestamp, success, error}
- **`archive_old_data`** — P2-项20修复: 数据归档策略

将指定表中超过archive_before_days天的数据归档到历史表，
减小活跃表体积，提升查询性能。

Args:
    table_name: 要归档的表名
    archive_before_days: 归档多少天前的数据
    dry_run: 仅统计不实际执行

Returns:
    Dict: {rows_archived, archive_table, dry_run, success}

#### class `StorageChecksService`

存储服务 - 数据自愈能力（从StorageChecksMixin重构为独立Service）

职责:
- 启动时执行元数据表检查
- 孤儿记录清理
- 空表检测与清理
- 订阅标准化检查
- **新增：2605 合约日志诊断（每 30 秒输出）**

设计原则:
- 轻量级：只执行必要的检查
- 非侵入：不影响正常业务流程
- 自动化：启动时自动执行

- **`__init__`** — 初始化维护服务
- **`run_startup_checks_fast_path`** — P1-3修复：startup checks fast path。

正常重启时，如果 maintenance version 已是最新，只执行轻量健康检查
（序列同步 + 缺表修复），跳过重维护逻辑。
只有 maintenance version 变化时，才执行全量 startup checks。

设计约束（修改必看十原则）：
1. fast path 必须包含序列同步（轻量但关键，防止 ID 冲突）
2. fast path 必须包含缺表修复（轻量但关键，防止表缺失）
3. 重维护逻辑（orphan 恢复、FK 修复、订阅去重等）只在 version 变化时执行
4. 新增迁移脚本、缺表修复、索引补建时，必须声明是否允许在启动热路径执行
- **`run_startup_checks`** — 启动时执行必要检查 (DuckDB 版本)

Args:
    conn: 数据库连接（已废弃，保留参数用于兼容）

注意：不抛出异常，避免影响系统启动
- **`run_periodic_reconciliation`** — R26-P0-DI-01补全: 定时持仓对账——从maintenance_service调用reconcile_positions_with_exchange

Args:
    position_service: PositionService实例, 用于获取本地持仓
    exchange_query_fn: 查询交易所持仓的回调函数

Returns:
    Dict: 对账结果 {is_matched, diffs, local_only, exchange_only}
- **`run_periodic_diagnostic`** — 运行 2605 合约日志诊断（手动调用，每 30 秒）(DuckDB 版本)

Args:
    conn: 数据库连接（已废弃，保留参数用于兼容）
- **`_log_contract_diagnosis`** — 输出单个合约诊断日志
- **`_log_status_field`** — 输出状态字段日志
- **`_log_receive_status`** — 输出接收状态日志
- **`_log_store_status`** — 输出落库状态日志
- **`ensure_instrument_id_sequence`** — 确保全局 instrument_id_sequence 存在并返回当前 next_id (DuckDB 版本)
- **`reserve_next_global_id`** — 从统一序列中预留一个新的全局 internal_id (DuckDB 版本)

线程安全：通过 _sequence_lock 保证读-改原子性。
异常安全：失败时抛出 RuntimeError 而非返回默认值，避免 ID 冲突。
- **`sync_instrument_id_sequence`** — 把全局序列推进到当前所有 instrument.id 的上界之后 (DuckDB 版本)
- **`get_kv_value`** — 获取 KV 存储值 (DuckDB 版本)
- **`set_kv_value`** — 设置 KV 存储值 (DuckDB 版本)

#### class `StorageMaintenanceService`

存储维护服务 - Facade组合（消灭Mixin继承）

通过组合持有StorageChecksService和StorageCatalogService实例，
__getattr__委托实现零破坏性变更。

### `_sub_config.py`

订阅管理器 - 配置与工具函数

#### class `SubscriptionConfig`

订阅管理器配置 - 借鉴 OrderFlowConfig 设计

- **`func:_acquire_file_lock`** — EC-P1-05修复: 跨进程文件锁
- **`func:_release_file_lock`** — EC-P1-05修复: 释放跨进程文件锁
- **`func:classify_registered_instruments`** — 按 underlying_future_id 对已注册合约做轻量分组，避免初始化阶段逐合约阻塞等待。
- **`func:inst_get`** — 兼容 dict / object 两种返回结构读取字段

### `_sub_core.py`

订阅管理器 - 核心订阅逻辑 (_SubscriptionCoreMixin)

#### class `SubscriptionCoreService`

- **`_do_subscribe`** — 统一订阅方法（内部使用）

✅ 序号122修复：统一为data_manager.subscribe，删除_platform_subscribe回退

Args:
    instrument_id: 合约ID
    data_type: 数据类型 ('tick', 'kline'等)
- **`set_t_type_service`** — 注入TTypeService
- **`_ensure_instruments_loaded`** — 确保合约数据已从配置文件加载到数据库（启动时必须完成）

⚠️ 关键设计：
- 合约元数据必须在 Storage 初始化前从配置文件完整加载
- 这个检查只验证配置是否已加载，不负责实际加载
- 实际加载由 strategy_core_service.on_init() 调用 ensure_products_with_retry 完成
- 如果配置未加载，返回 False 触发上层重试
- 只有策略正常运行后，才可能从平台进行增量更新

Returns:
    bool: 配置是否已加载完成
- **`subscribe_all_instruments`** — 全量订阅 (增强版 - 带重试和WAL保护)

Returns:
    bool: 是否全部成功
- **`unsubscribe_all`** — 取消所有订阅
- **`get_subscription_stats`** — 获取订阅统计
- **`on_tick`** — Tick 路由入口：先查 metadata 决定期权/期货，再转发到 TTypeService。

改造后不再每笔 Tick 先 parse_option/parse_future 再路由，
而是先查 instrument_id -> internal_id -> meta，
由 metadata 中的 type 字段决定走哪条路径。

R23-ID-04-FIX: 添加is_replay参数，回放tick直接跳过处理
- **`_on_tick_impl`** — on_tick内部实现（在_tick_lock内执行）
- **`_extract_product`** — 从合约ID提取品种代码

Examples:
    au2605C1032 -> AU
    al2605C25200 -> AL
    cu2605 -> CU
    HO2605-C-2800 -> HO
- **`record_subscription`** — 记录订阅合约列表（分母）
- **`record_kline_received`** — 记录收到K线（分子：平台返回过K线的合约）
- **`record_tick_received`** — 记录收到Tick（分子：平台推送过Tick的合约）
- **`get_subscription_success_rate`** — 获取订阅成功率统计

Returns:
    {
        'total_subscribed': 分母（合约数）,
        'total_products': 分母（品种数）,
        'kline_received': K线分子（合约数）,
        'tick_received': Tick分子（合约数）,
        'kline_products': K线分子（品种数）,
        'tick_products': Tick分子（品种数）,
        'kline_rate': K线成功率（合约维度）,
        'tick_rate': Tick成功率（合约维度）,
        'kline_product_rate': K线成功率（品种维度）,
        'tick_product_rate': Tick成功率（品种维度）,
    }
- **`log_subscription_success_summary`** — 输出订阅成功率摘要日志

### `_sub_instrument.py`

订阅管理器 - 合约解析 (_SubscriptionInstrumentMixin)

#### class `SubscriptionInstrumentService`

- **`_strip_exchange_prefix`** — 剥离交易所前缀（统一使用正则）

Args:
    instrument_id: 合约ID（可能带交易所前缀，如 'CFFEX.IF2603'）

Returns:
    str: 纯净的合约ID（如 'IF2603'）
- **`_normalize_product_code`** — 标准化产品代码，用于内部比较和分类

Args:
    product: 产品代码字符串

Returns:
    str: 标准化的产品代码（SHFE品种小写，其他大写）
- **`parse_future`** — 解析期货合约
- **`is_option`** — 判断是否为期权
- **`parse_option`** — 解析期权合约(单一正则同时匹配连字符和紧凑格式, 品种ID直通)

修复标准#125: 一种正则解析所有格式, 原样ID直通不改变格式.
统一正则 r'^([A-Za-z]+)(\d{3,4})-?([CP])-?(\d+(?:\.\d+)?)$'
同时匹配连字符格式(CU2603-C-5000)和紧凑格式(CU2603C5000).
- **`_normalize_option_year_month`** — 归一化期权年月
- **`classify_instruments`** — 分类合约

options_dict 的 key 统一为 canonical underlying 标识 (product+year_month)，
如 'IO2605', 'al2605'，与 ParamsService._extract_canonical_underlying 输出一致。

### `_sub_wal.py`

订阅管理器 - WAL管理 (_SubscriptionWALMixin)

#### class `SubscriptionWALService`

- **`_init_wal`** — 初始化WAL文件
- **`_rotate_wal`** — WAL文件轮转
- **`_wal_heartbeat`** — R15-P0-RES-10修复: 定期写入心跳记录，重启时检测异常退出
- **`_check_last_heartbeat`** — R15-P0-RES-10修复: 重启时检测最后心跳时间判断异常退出
- **`_write_wal_async`** — 异步写入WAL - 使用队列缓冲

EC-P1-05: 已有进程级锁保护 - self._wal_lock (threading.Lock)，
在_write_wal_async和_wal_writer_loop中均使用with self._wal_lock保护，
满足进程内线程安全；跨进程场景需依赖文件系统锁(待规划)。
- **`_recover_from_wal`** — 从WAL恢复订阅状态
- **`_close_wal`** — P1 Bug #36修复：关闭WAL文件，防止进程退出时文件泄漏
- **`start`** — 显式启动后台线程（构造函数不再自动启动）
- **`_start_background_threads`** — 启动后台线程
- **`ensure_background_threads`** — 确保后台线程处于运行状态，供 stop 后重启复用。
- **`stop_background_threads`** — 优雅停止所有后台线程

Args:
    join_timeout: 线程等待超时（秒）
    silent: 为True时抑制日志输出（用于atexit清理）
- **`_wal_writer_loop`** — WAL写入线程 - 批量异步落盘
- **`_retry_loop`** — 重试线程 - 定期处理失败任务
- **`_tick_watchdog_loop`** — 定期检查所有已订阅合约的tick是否断流，超时则触发告警
- **`_atexit_cleanup`** — 进程退出清理 - 静默模式避免测试退出时I/O错误
- **`close`** — 关闭所有资源

Args:
    silent: 为True时抑制日志输出（用于atexit清理）
- **`_calc_backoff_delay`** — 计算退避延迟
- **`_enqueue_for_retry`** — 将失败任务加入重试队列
- **`_process_retry_queue`** — 处理重试队列 (已移除过期检查,由独立清理线程负责)
- **`_cleanup_expired_events`** — 清理重试队列中超过最大存活时间的事件
- **`_cleanup_loop`** — 定期清理过期事件 (独立线程,不阻塞重试逻辑)
- **`_check_alert`** — 检查是否触发告警（✅ P1修复：加锁保护共享计数器）  # R17-P2-DOC-05

### `callback_registry.py`

P1-05修复: CallbackRegistry/CallbackGroup保留API兼容，invoke内部委托到EventBus

#### class `CallbackRegistry`

#### class `CallbackGroup`

### `concurrent_utils.py`

R16-P1-RES-05修复: 线程池拒绝策略枚举

#### class `RejectionPolicy`

线程池任务队列满时的拒绝策略

#### class `ThreadPoolExecutorWithPolicy`

带拒绝策略的线程池包装器

R16-P1-RES-05修复: 为queue.Full提供显式拒绝策略

- **`submit`** — 提交任务，队列满时按拒绝策略处理
- **`submit_to_queue`** — 直接提交到队列，满时按策略处理
- **`_handle_rejection`** — 处理线程池拒绝
- **`_handle_queue_rejection`** — 处理队列拒绝
- **`get_stats`** — 获取拒绝统计

### `cross_system.py`

cross_system/统一跨系统模块 — 合并自2个子模块

#### class `UnifiedSlippageModel`

#### class `UnifiedPositionSizer`

#### class `BacktestLatencyController`

#### class `BacktestOrderSplitter`

#### class `CrossSystemExecutionKernel`

#### class `ThreadSafeCounter`

R4-D-09修复: 线程安全计数器

回测引擎并行运行时，共享的equity_series和position状态需加锁。

#### class `ThreadSafeDict`

R4-D-09修复: 线程安全字典包装

用于保护共享的position状态和equity_series。

- **`func:safe_equity_update`** — 安全权益更新 — 防止浮点精度损失累积

连续复利计算中 float64 精度约15-16位有效数字，
当权益>1e8且PnL<1e-2时可能出现精度损失。
- **`func:configure_log_levels`** — R4-D-07修复: 统一日志级别配置

解决不同模块使用不同日志级别导致关键异常被低级别日志淹没的问题。
通过统一的级别配置，确保WARNING及以上级别的日志始终可见。

Args:
    level_map: 模块名到日志级别的映射，覆盖默认配置
- **`func:get_execution_model_differences`** — R4-D-08修复: 返回回测/实盘模型差异清单

明确标注回测与实盘之间的模型差异，为后续对齐提供依据。
- **`func:perf_monitor`** — R4-D-13修复: 性能监控上下文管理器

记录代码块执行时间，超过阈值时输出WARNING。
统计信息可通过 get_perf_stats() 查询。

Args:
    label: 监控标签
    warn_threshold_ms: 超过此时间(ms)输出WARNING
- **`func:get_perf_stats`** — 返回性能监控统计信息
- **`func:log_exception_with_stack`** — R4-D-14修复: 完整记录异常堆栈信息

确保异常信息包含完整的堆栈追踪，便于调试。

Args:
    logger_instance: 日志实例
    message: 错误消息
    level: 日志级别
- **`func:safe_execute`** — 安全执行函数 — 捕获异常并完整记录堆栈

R4-D-14修复: 所有异常都完整记录堆栈信息，避免调试困难。
- **`func:get_spawn_context`** — 获取multiprocessing的spawn context（单例缓存）

统一替代 __import__('multiprocessing').get_context('spawn') 的重复代码。
l2_optimizer.py 和 preprocess_ticks.py 均通过此函数获取spawn context，
避免DRY违反并确保context配置一致性。

Returns:
    multiprocessing.BaseContext: spawn context实例

### `deviation_classifier.py`

deviation_classifier.py — 双重回测围栏偏差分类器 (PA-06 数值稳定性)

定位：生产级偏差分类器，供回测运行器、CI/CD回归守卫和日常重构验证调用。

设计来源：报告13.1.5 "双重回测围栏——重构可信化的最终验证"
核心职责：逐笔对比重构前后信号，将偏差分为4类，其中 CRITICAL_DEVIATION
         是PA-06生死线的工程化判定标准。

分类规则：
- MATCH: 完全一致（方向相同 + 价格差<1e-6）
- CORRECT_DEVIATION: 未来函数消除导致的信号消失（baseline有信号，actual为None）
- NUMERICAL_NOISE: 浮点累加顺序差异（价格差<0.01元）
- CRITICAL_DEVIATION: 方向性差异（需人工审查，PA-06阻断项）
- NEEDS_REVIEW: 其他差异（需溯源确认）

Usage:
    from ali2026v3_trading.infra.deviation_classifier import (
        Signal, DeviationResult, classify_deviation, classify_signal_sequence,
    )

    # 单笔对比
    result = classify_deviation(baseline_signal, actual_signal)
    if result.category == 'CRITICAL_DEVIATION':
        raise PA06Violation(result.description)

    # 批量对比
    report = classify_signal_sequence(baseline_list, actual_list)
    assert report['CRITICAL_DEVIATION'] == 0

#### class `Signal`

统一信号表示 — 回测围栏对比的基本单位

#### class `DeviationResult`

偏差分类结果

- **`func:classify_deviation`** — 偏差分类器 — 逐笔对比重构前后信号

Args:
    baseline: 重构前（BEFORE）信号
    actual: 重构后（AFTER）信号
    noise_tol: 浮点噪声绝对容差
    acceptable_diff: 可接受价格差异阈值

Returns:
    DeviationResult with category and description
- **`func:classify_signal_sequence`** — 批量信号序列偏差分类

Args:
    baseline_signals: 重构前信号序列
    actual_signals: 重构后信号序列
    noise_tol: 浮点噪声容差
    acceptable_diff: 可接受价格差异

Returns:
    报告字典，包含:
    - total: 总对比笔数
    - category_counts: 各分类计数
    - critical_deviations: CRITICAL_DEVIATION详情列表
    - needs_review: NEEDS_REVIEW详情列表
    - pass_pa06: 是否通过PA-06（CRITICAL_DEVIATION=0）

### `diagnosis.py`

diagnosis/统一诊断模块 — 合并自3个子模块

#### class `DiagnosisProbeManager`

12环节诊断探针统一管理器

- **`register_online_monitor_callback`** — 注册上线监控回调函数，签名: callback(event_type: str, detail: Dict)

#### class `ResourceOwnershipScanner`

资源所有权诊断扫描器

#### class `ControlActionLogger`

控制动作日志记录器

#### class `DiagnoserConfig`

诊断器配置类

#### class `StrategyProtocol`

策略协议接口（类型约束）

#### class `DiagnosisReport`

诊断报告数据类

#### class `StrategyDiagnoser`

策略诊断器 - Core 层辅助服务

职责:
- 全链路诊断（数据源、合约、订阅、K 线）
- 断点检测
- 问题排查
- 健康评分
- 自动修复建议

设计原则:
- Composition over Inheritance
- 单一职责
- 线程安全
- 非侵入式诊断

- **`_on_filter_stats_event`** — DFG-P1-10修复: 消费信号过滤统计事件，持久化到诊断服务

当信号过滤统计更新时，缓存到诊断服务，
供诊断报告和健康检查使用。
- **`_persist_filter_stats`** — P2修复: 将信号过滤统计持久化到磁盘

每次统计更新时追加写入JSONL文件，确保重启后可恢复历史统计。

Args:
    stats: 信号过滤统计数据
- **`load_persisted_filter_stats`** — P2修复: 加载持久化的信号过滤统计

Args:
    days: 加载最近N天的统计

Returns:
    统计记录列表

### `diagnosis_periodic.py`

周期性诊断模块 - 从 diagnosis_service.py 拆分
职责：周期性14合约诊断、宽限期管理、资源快照、资源扫描器、控制动作日志

(已合并至 diagnosis.py，本文件仅做向后兼容重导出)

### `diagnosis_probe.py`

诊断探针管理模块 - 从 diagnosis_service.py 拆分
职责：12环节诊断探针、监控合约定义、合约格式验证

(已合并至 diagnosis.py，本文件仅做向后兼容重导出)

### `diagnosis_service.py`

策略诊断服务 - CQRS 架构 Diagnosis 层
来源：分散的诊断逻辑（strategy_lifecycle_mixin.py 中的诊断函数）
功能：策略运行诊断、断点检测、问题排查、健康报告

(已合并至 diagnosis.py，本文件仅做向后兼容重导出)

### `event_bus.py`

event_bus/统一事件总线模块 — 合并自event_bus_dataflow + event_bus_events + event_bus_core re-export

拆分说明：
- event_bus_core.py: EventBus类、RateLimiter、全局单例函数（保持独立）
- event_bus.py（本文件）: 合并了 event_bus_dataflow + event_bus_events 的全部代码，
  并 re-export event_bus_core 的公共API
- event_bus_dataflow.py / event_bus_events.py: 向后兼容的薄重导出门面

#### class `BaseEvent`

基础事件类

#### class `TickEvent`

Tick 数据事件

#### class `KLineEvent`

K线更新事件

#### class `SignalEvent`

交易信号事件

#### class `OrderEvent`

订单事件

#### class `PositionEvent`

持仓变更事件

#### class `RiskEvent`

风控事件

#### class `RiskAlertEvent`

P1-05修复: 风控告警事件——统一RiskService._alert_callbacks到EventBus

#### class `SystemEvent`

系统事件

#### class `SubscriptionCompletedEvent`

订阅完成事件

#### class `DataFormatChangedEvent`

UPG-P1-10修复: 数据格式变更事件

当数据格式（schema版本、列定义等）发生变更时发布此事件。
通知订阅者刷新缓存或重新加载数据。

#### class `HealthStatusEvent`

OPS-P1-02修复: 健康状态主动推送事件

定时通过EventBus发布健康状态，替代仅被动查询的模式。

#### class `ParamChangedEvent`

OPS-P1-03修复: 参数变更通知事件

参数变更时通过EventBus发布通知，替代仅日志记录的模式。

#### class `CircuitBreakerTriggeredEvent`

OPS-P1-04修复: 断路器触发告警事件

断路器触发时通过EventBus发布告警，替代仅日志记录的模式。

#### class `DailyDrawdownHaltEvent`

OPS-P1-05修复: 日回撤硬停止告警事件

日回撤硬停止触发时通过EventBus发布告警，替代仅日志记录的模式。

#### class `OpsOperationEvent`

OPS-P1-06~19修复: 运维操作生命周期事件

运维操作开始、完成/失败/回滚时通过EventBus发布通知。

- **`func:producer`** — P2修复: 生产者装饰器 - 声明函数是某事件类型的生产者

用法:
    @producer('SignalEvent')
    def generate_signal(self, ...):
        event_bus.publish(SignalEvent(...))
- **`func:consumer`** — P2修复: 消费者装饰器 - 声明函数是某事件类型的消费者

用法:
    @consumer('SignalEvent')
    def on_signal(self, event):
        ...
- **`func:get_producer_consumer_registry`** — P2修复: 获取生产者/消费者注册表
- **`func:record_dataflow_latency`** — P2修复: 记录数据流延迟

Args:
    event_type: 事件类型
    latency_ms: 延迟毫秒数
- **`func:get_dataflow_perf_stats`** — P2修复: 获取数据流性能统计

Returns:
    Dict: 每种事件类型的性能统计
- **`func:detect_dataflow_anomaly`** — P2修复: 检测数据流异常

Args:
    event_type: 事件类型
    latency_ms: 延迟毫秒数
    success: 事件是否成功处理

Returns:
    异常信息字典，无异常返回None
- **`func:get_dataflow_anomalies`** — P2修复: 获取数据流异常记录

Args:
    limit: 返回数量限制

Returns:
    异常记录列表

### `event_bus_core.py`

事件总线核心模块 - EventBus类、RateLimiter、全局单例

拆分自 event_bus.py，确保所有现有 import 路径零修改。

#### class `EventBus`

线程安全的事件总线（优化版 v2.2）

特性：
- 支持多订阅者
- 异步事件分发（带线程池管理）
- 异常隔离（单个订阅者错误不影响其他订阅者）
- 线程安全
- 背压机制（防止内存溢出）
- 高效事件存储（deque O(1) 操作）
- 事件优先级支持（高优先级优先处理）
- 事件过滤器（条件订阅）
- 速率限制（防 flooding）

#### class `RateLimiter`

令牌桶速率限制器

- **`func:_lazy_repr`** — R15-P1-LOG-17修复: 智能repr截断，保留type/key信息，截断冗余value
- **`func:safe_import_event_bus`** — 接口唯一修复：委托get_global_event_bus，返回EventBus实例而非模块
- **`func:get_global_event_bus`** — 获取全局事件总线单例（线程安全）
- **`func:reset_global_event_bus`** — 重置全局事件总线（主要用于测试）

### `event_publisher.py`

event_publisher.py - EventPublisher
Phase 2 (CC-02+CC-04): 从_LifecycleMixin提取的事件发布Manager

职责：
- 发布策略事件（StrategyInitialized/Started/Stopped等）

Provider接口：
- provider._event_bus: 事件总线
- provider.strategy_id: 策略ID

#### class `EventPublisher`

事件发布Manager

- **`publish`** — 发布事件

### `exception_layers.py`

R3-1: 异常分层策略模块

定义L0-L3四级异常分层策略，用于治理全系统352处`except Exception`软集成问题。

分层策略:
    L0 致命: 核心门控失败必须硬阻断，不允许静默吞没
        - 适用: CascadeJudge、风控检查、交易执行
        - 捕获: (ImportError, AttributeError, TypeError)
        - 处理: raise RuntimeError 硬阻断

    L1 严重: 评判/验证组件失败必须标记结果无效
        - 适用: 评判组件、验证器、checker
        - 捕获: (ValueError, KeyError, RuntimeError)
        - 处理: 记录error + 标记结果无效

    L2 可降级: 辅助指标失败可降级到默认值
        - 适用: 辅助指标、非关键评分、监控采集
        - 捕获: (ValueError, KeyError, TypeError, AttributeError, ImportError)
        - 处理: 记录warning + fallback默认值

    L3 可忽略: 非功能性采集失败可跳过
        - 适用: 日志、遥测、非功能性采集
        - 捕获: Exception (保留宽捕获)
        - 处理: 记录debug + 跳过

#### class `FatalGateError`

L0级致命错误 - 核心门控失败时抛出，替代静默吞没

#### class `SevereCheckError`

L1级严重错误 - 评判/验证组件失败时标记结果无效

- **`func:raise_fatal`** — L0级: 核心门控失败硬阻断

Args:
    component: 组件名称，如"CascadeJudge"、"RiskService"
    original_error: 原始异常

Raises:
    FatalGateError: 始终抛出
- **`func:mark_severe`** — L1级: 评判/验证组件失败标记无效

Args:
    component: 组件名称
    original_error: 原始异常
    warnings_list: 可选的warnings列表，会自动追加

Returns:
    格式化的警告消息
- **`func:degrade`** — L2级: 辅助指标降级到默认值

Args:
    component: 组件名称
    original_error: 原始异常
    logger: 可选的logger，会自动记录warning

Returns:
    格式化的降级消息

### `exceptions.py`

R10-P2-01: 全系统自定义异常类层次

#### class `TradingSystemError`

交易系统基础异常

#### class `RiskCheckError`

风控检查异常

#### class `OrderExecutionError`

订单执行异常

#### class `StateTransitionError`

状态转换异常

#### class `ConfigurationError`

配置异常

#### class `DataIntegrityError`

数据完整性异常

#### class `ParameterValidationError`

参数验证异常

#### class `FutureLeakException`

未来数据泄露异常 - 回测模式下访问超出当前事件时间的数据时抛出。

统一异常类，替代原有的 FutureLeakException（param_pool）和
FutureDataLeakException（strategy）两个语义相同但类名不同的异常。

Attributes:
    access_time: 访问时间
    poison_time: 数据时间（被访问的未来时间）
    stack_trace: 调用栈信息

### `health.py`

health/统一健康检查模块 — 合并自4个子模块

#### class `HealthMonitor`

DR-P1-07修复: 心跳检测 + 自动故障判定

心跳检测：每30秒检查核心服务是否响应
连续3次心跳失败 → 标记 DEGRADED
连续5次心跳失败 → 标记 CRITICAL → 触发自动恢复
故障日志写入 logs/health_events.jsonl

- **`register_service`** — 注册需要心跳检测的核心服务
- **`heartbeat`** — 执行一次心跳检测，返回当前状态

Returns:
    'HEALTHY', 'DEGRADED', 或 'CRITICAL'
- **`_log_health_event`** — 记录健康事件到JSONL日志
- **`get_status`** — 获取当前健康状态
- **`is_critical`** — 是否为CRITICAL状态
- **`is_degraded`** — 是否为DEGRADED状态
- **`reset`** — 重置故障计数

#### class `StructuredJsonlLogger`

- **`_rotate_if_needed`** — LG-P1-02修复: JSONL文件超限时rotate
- **`close`** — 关闭文件句柄 — NEW-P1-01修复: 添加异常安全保护
- **`log_risk_decision`** — R24-P1-TR-12修复: 记录风控决策事件
- **`log_strategy_switch`** — R24-P1-TR-12修复: 记录策略切换事件
- **`log_diagnostic`** — R24-P1-TR-12修复: 记录诊断事件

#### class `HealthCheckAPI`

- **`_probe_component_status`** — R30-P0-03修复: 对unknown组件执行存活探测
- **`_refresh_unknown_components`** — R30-P0-03修复: 刷新所有unknown组件的状态
- **`_on_greeks_dashboard_updated`** — DFG-P1-04修复: 消费Greeks仪表盘更新事件，缓存最新数据
- **`_push_health_status`** — 定时推送健康状态而非仅被动查询
- **`start_push`** — 启动健康状态主动推送
- **`_delegate_to_aggregator`** — P1-01修复: 委托HealthCheckAggregator获取核心健康状态。
若聚合器可用且有strategy_service引用，返回聚合结果；
否则返回None，回退到HealthCheckAPI原有逻辑。
- **`get_greeks_dashboard`** — DFG-P1-04修复: 返回当前Greeks仪表盘数据
- **`greeks_dashboard_data`** — OPS-P1-01修复: 返回Greeks仪表盘结构化数据
- **`register_health_change_callback`** — OPS-P1-02修复/P1-05修复: 注册到EventBus单通道
- **`_fire_health_change_callbacks`** — OPS-P1-02修复/P1-05修复: 统一通过EventBus发布
- **`get_log_stats`** — 返回最近日志统计信息，用于日志聚合与监控
- **`check_capacity_limits`** — OPS-11修复: 检查系统容量是否接近上限

#### class `HealthCheckAggregator`

健康检查聚合器

_CHECKS定义6个独立检查项:
1. _check_connection_state: 连接状态检查
2. _check_heartbeat_status: 心跳状态检查
3. _check_resource_usage: 资源使用检查
4. _check_shadow_engine: 影子引擎检查
5. _check_strategy_ecosystem: 策略生态系统检查
6. _check_safety_meta_layer: 安全元层检查

- **`aggregate`** — 编排器: 聚合所有检查项，≤20行，圈复杂度≤3
- **`_check_connection_state`** — 检查1: 连接状态(平台/交易所连接)
- **`_check_heartbeat_status`** — 检查2: 心跳状态(最近心跳时间+延迟)
P1-01修复: 优先委托 HealthMonitor.heartbeat() 获取心跳状态，失败则回退到原有逻辑
- **`_check_resource_usage`** — 检查3: 资源使用(线程池/内存/订单数)
- **`_check_shadow_engine`** — 检查4: 影子引擎状态(Alpha衰减/暂停)
- **`_check_strategy_ecosystem`** — 检查5: 策略生态系统(策略活跃度/资金分配)
P1-01修复: 优先委托 LifecycleMonitor.health_check() 获取策略状态，失败则回退到原有逻辑
- **`_check_safety_meta_layer`** — 检查6: 安全元层(熔断器/合规/资金充足性)
P1-01修复: 优先委托 HealthCheckAPI._get_circuit_breaker_status() 获取熔断器状态，失败则回退到原有逻辑
- **`_determine_overall`** — 根据各检查项确定整体状态

#### class `CodeQualityMetrics`

运行时代码质量指标采集

- **`_check_dead_imports`** — 检查死代码导入数量
- **`_check_whitelist`** — 检查API白名单违规数量
- **`_get_component_failure_rate`** — 获取组件失败率
- **`_count_suppressed_errors`** — 统计try/except抑制的错误数量

- **`func:check_disk_space`** — DR-P1-14: 磁盘空间预警检查

使用 shutil.disk_usage 检查磁盘使用率
磁盘 < 10% → WARNING
磁盘 < 5%  → CRITICAL

Args:
    path: 要检查的路径，默认当前目录

Returns:
    dict: {total_gb, used_gb, free_gb, usage_percent, status}

### `logging_utils.py`

logging_utils — 统一日志工厂 (R9-5)

提供 ``get_logger`` 统一入口，替代分散在代码各处的
``logging.getLogger(__name__)`` 模式。

优势:
- 单点控制: 未来切换日志框架/格式只需修改此模块
- 一致性: 所有 logger 通过同一入口创建
- 可扩展: 可注入模块上下文、默认 handler 等

- **`func:get_logger`** — 获取统一 logger 实例。

Args:
    name: logger 名称，通常传 ``__name__``。
    level: 可选日志级别，如 ``logging.DEBUG``。
           不传时使用 logger 自身或 root 的默认级别。

Returns:
    配置好的 ``logging.Logger`` 实例。

### `maintenance_service.py`

maintenance_service.py - 存储维护服务 (门面模块)

从 query_service.py 迁移的 StorageMaintenanceService 类
职责：存储服务的数据自愈能力（启动检查、孤儿清理、空表检测等）

作者：CodeArts 代码智能体
迁移时间：2026-05-04

拆分时间：2026-06-09
拆分为子模块：
  - _helpers.py: 模块级辅助函数和常量
  - _storage_checks_mixin.py: StorageChecksMixin (启动检查、诊断、序列、KV)
  - _storage_catalog_mixin.py: StorageCatalogMixin (目录维护、备份、归档)
  - _storage_maintenance.py: StorageMaintenanceService 组合类
  - _ops_framework.py: OpsOperation + OpsOperationManager
  - _disk_monitor.py: DiskSpaceMonitor
  - _backup_restore.py: DuckDBBackupService + OnCallManager + DuckDBRestoreService
  - _ops_knowledge_metrics.py: 知识库/指标/SLA
  - _ops_automation.py: 自动化函数/压测/SOP

### `market_time_service.py`

market_time_service.py - 市场时间服务与时钟同步检测

从 scheduler_service.py 提取，解决 P1-08 双调度器问题。
职责：市场开收盘判断、交易日历、时钟同步检测

作者：CodeArts 代码智能体
版本：v1.0

#### class `TimeSyncChecker`

DR-P1-12: 使用简单时间源比对检测时钟偏差

偏差 > 5秒  → WARNING
偏差 > 30秒 → CRITICAL → 触发暂停交易

- **`check_time_sync`** — DR-P1-12: 检测系统时钟与外部时间源的偏差

Returns:
    dict: 包含 deviation_sec, status, message
- **`_measure_time_deviation`** — 测量本地时钟与外部时间源的偏差

#### class `MarketTimeService`

- **`add_holiday`** — 添加节假日
- **`add_default_holidays`** — [R22-TIME-P1-14] 添加2026年默认中国法定节假日
- **`is_trading_day`** — 判断是否为交易日

Args:
    target_date: 目标日期
    holiday_dates: 额外的节假日集合（可选）

Returns:
    bool: 是否为交易日

- **`func:is_market_open`** — 市场是否开盘（委托给MarketTimeService）

### `metrics_registry.py`

Phase0增强: MetricsRegistry — 可观测性设计(Prometheus风格指标)
SLI/SLO/SLA定义 + 结构化指标 + 告警规则

#### class `SLIDefinition`

#### class `MetricsRegistry`

可观测性指标注册中心

Prometheus风格指标:
- Counter: 单调递增计数器(请求量/错误数/WAL写入量)
- Histogram: 延迟分布(请求延迟)
- Gauge: 瞬时值(断路器状态/队列深度)

#### class `AlertRule`

告警规则

### `operations_api.py`

#### class `OperationsAPI`

OPS-17修复: 运维操作API — 集中管理常见运维操作

提供统一的运维操作入口，避免所有运维依赖手动命令行。
每个操作自动记录审计日志。

- **`emergency_stop`** — 紧急停止: 暂停策略 + 撤销挂单 + 平所有仓位
- **`emergency_degrade`** — 紧急降级: 减少活跃策略数量
- **`emergency_close_all`** — 紧急平仓: 撤销所有挂单 + 市价平所有持仓
- **`force_backupbackup_database`** — 数据库备份
- **`check_capacity`** — 容量检查
- **`run_stress_test`** — R21-OPS-18: 执行容量压测
- **`get_stress_test_report`** — R21-OPS-18: 获取压测报告
- **`get_health`** — 获取健康状态
- **`confirm_daily_resume`** — 确认恢复日回撤硬停止（需审批）
- **`get_audit_log`** — 获取最近的操作审计日志
- **`get_help`** — P2-项22修复: 运维培训自文档化帮助系统
- **`query_knowledge_base`** — P2-项23修复: 运维知识库查询
- **`run_diagnostics`** — P2-项24修复: 运行诊断工具
- **`list_available_tools`** — P2-项24修复: 列出所有可用运维工具
- **`auto_remediate`** — P2-项25修复: 自动修复常见运维问题
- **`collect_metrics`** — P2-项26修复: 采集标准化运维指标
- **`generate_report`** — P2-项27修复: 自动生成运维报告
- **`estimate_cost`** — P2-项28修复: 运维成本估算
- **`get_sla_definitions`** — P2-项29修复: 获取SLA定义
- **`check_sla_compliance`** — P2-项29修复: 检查SLA合规性
- **`list_sop_procedures`** — R21-OPS-20: 列出所有SOP标准操作程序
- **`get_sop_procedure`** — R21-OPS-20: 获取指定SOP操作程序
- **`execute_sop`** — R21-OPS-20: 执行SOP标准操作程序

### `performance_monitor.py`

performance_monitor.py - 性能监控工具模块

提供路径计数器、函数调用统计等性能监控功能。
从 strategy_core_service.py 迁移而来，实现关注点分离。

职责：
- 函数/方法调用次数统计
- 执行时间监控
- 错误率跟踪
- 性能数据导出

设计原则：
- 单例模式确保全局唯一
- 线程安全
- 低开销（可禁用）

#### class `PathCounter`

路径计数器 - 记录函数/方法调用次数和执行时间

使用示例：
    from ali2026v3_trading.infra.performance_monitor import PathCounter, count_call
    
    @count_call()
    def my_function():
        pass
    
    # 手动记录
    PathCounter.record_call("custom.path", execution_time=0.5)
    
    # 导出报告
    PathCounter.export_to_json("performance_report.json")
    PathCounter.print_summary()

- **`enable`** — 启用性能监控
- **`disable`** — 禁用性能监控（零开销）
- **`is_enabled`** — 检查是否启用
- **`start_auto_persist`** — RES-P1-17修复: 启动自动定期持久化后台线程
- **`stop_auto_persist`** — RES-P1-17修复: 停止自动持久化并执行最后一次保存
- **`record_call`** — 记录一次函数调用

Args:
    path: 调用路径（如 module.function 或 Class.method）
    execution_time: 执行时间（秒）
    exception: 异常信息（如果有）
- **`get_stats`** — 获取性能统计数据（统一接口）

Args:
    output_format: 输出格式 ('dict', 'json', 'console')
           - 'dict': 返回字典（默认）
           - 'json': 导出到JSON文件（需指定filepath参数）
           - 'console': 打印到控制台

Returns:
    dict: 性能统计数据（当output_format='dict'时）

Usage:
    # 获取字典数据
    stats = PathCounter.get_stats()
    
    # 导出到JSON
    PathCounter.get_stats(output_format='json', filepath='report.json')
    
    # 打印到控制台
    PathCounter.get_stats(output_format='console', top_n=20)

#### class `ChaosFaultInjector`

R15-P0-RES-09修复: 轻量故障注入框架，用于验证容错措施是否真正生效

用法:
    injector = ChaosFaultInjector()
    injector.register('duckdb_connection', lambda: (_ for _ in ()).throw(Exception("simulated db failure")))
    injector.inject('duckdb_connection')  # 模拟DuckDB连接失败

- **`inject`** — 注入故障，返回handler结果或抛出异常

- **`func:count_call`** — 统一装饰器：记录函数/方法调用

Args:
    path: 自定义路径名称
          - 对于函数：默认为 module.function
          - 对于方法：默认为 ClassName.method_name

Usage:
    # 函数
    @count_call()
    def my_function():
        pass
    
    # 方法
    class MyClass:
        @count_call()
        def my_method(self):
            pass
    
    # 自定义路径
    @count_call("custom.name")
    def another_function():
        pass

### `phase_feature_flag.py`

Phase0增强: PhaseFeatureFlag — 每Phase回滚/降级策略的Feature Flag控制
灰度发布: 默认关闭，逐步灰度开启，支持即时切换回旧代码路径

#### class `PhaseFeatureFlag`

重构Phase的Feature Flag控制

每个Sprint的Feature Flag默认关闭(False)
灰度发布流程:
1. 代码部署(Feature Flag=False, 使用旧代码路径)
2. 灰度开启(Feature Flag=True, 部分流量走新代码)
3. 全量开启(确认无问题后全量切换)
4. 回滚(Feature Flag=False, 即时切换回旧代码)

### `resilience.py`

resilience/统一容错模块 — 合并自7个子模块

子模块:
  resilience_numeric.py  — 数值稳定
  resilience_circuit.py  — 断路器
  resilience_watchdog.py — 看门狗/心跳
  resilience_state.py    — 状态管理
  resilience_retry.py    — 重试/退避/超时
  resilience_config.py   — 容错配置/校验
  resilience_utils.py    — 资源守卫 + 兼容工具 + re-export

#### class `KahanSummation`

#### class `CircuitBreakerHalfOpen`

- **`opened_at`** — 断路器进入OPEN状态的时间戳（委托给CircuitBreakerService用于计算暂停截止时间）
- **`open_duration`** — OPEN状态持续时长（秒）
- **`force_open`** — 强制将断路器置为OPEN状态（由CircuitBreakerService在触发熔断时调用）

Args:
    open_duration_sec: 新的OPEN持续时长（秒），None则保持原值
    opened_at: OPEN状态起始时间戳，None则使用time.time()

#### class `RateLimitedLogger`

#### class `GracefulDegradation`

#### class `Watchdog`

#### class `HeartbeatMonitor`

#### class `ProcessHealthState`

#### class `AsyncTaskTimeout`

#### class `AtomicConfigRef`

#### class `ConfigSnapshot`

#### class `SharedStateRegistry`

#### class `ParamVersionManager`

#### class `SignalLifecycleManager`

#### class `ExponentialBackoff`

#### class `TimeoutGuard`

#### class `BoundedRetry`

#### class `StrategyRestartGuard`

#### class `DbQueryTimeout`

#### class `ISOLATION_LEVELS`

#### class `ThreadPoolGuard`

#### class `SlowQueryDetector`

#### class `DataStalenessDetector`

#### class `MemoryPressureGuard`

#### class `BoundedQueue`

#### class `ResourceLeakDetector`

#### class `BatchPartitioner`

#### class `SignalExpiryManager`

#### class `SubscriberSnapshotGuard`

#### class `ShadowSyncBarrier`

#### class `StrategyRegistry`

- **`func:compute_sharpe_stable`** — 计算夏普比率（数值稳定版）

Args:
    returns: 收益率序列
    risk_free_rate: 逐期无风险利率（非年化）
    annualize_factor: 年化因子（日频252，分钟频252*240）
    use_sample_std: True使用样本标准差(n-1分母)，False使用总体标准差(n分母)
- **`func:should_trigger_stop_loss`** — P0-03修复: 统一止损触发判断 — 生产/回测共用

is_long=True时(current_price<=stop_loss_price触发), is_long=False时(current_price>=stop_loss_price触发)
rtol使用np.isclose相对容差，与STOP_PRICE_RTOL对齐
- **`func:should_trigger_take_profit`** — P0-03修复: 统一止盈触发判断 — 生产/回测共用
- **`func:deprecated`** — P1-21: deprecated装饰器权威定义(底层模块，避免循环导入)

shared_utils.py从此处re-export，其他模块统一从shared_utils导入。
- **`func:check_multi_level_cache_consistency`** — P2-15修复: 委托到config_params.check_multi_level_cache_consistency
- **`func:check_indicator_freshness`** — P2-15修复: 委托到config_params.check_indicator_freshness
- **`func:check_risk_data_freshness`** — P2-15修复: 委托到config_params.check_risk_data_freshness
- **`func:check_hot_reload_compatibility`** — P1-45修复: 原check_config_hot_reload重命名，消除与config_loader同名混淆。
检查哪些配置项支持热更新（不支持的返回requires_restart=True）。
- **`func:check_config_hot_reload`** — Deprecated: 请使用check_hot_reload_compatibility替代

### `resilience_circuit.py`

Backward-compatible re-export facade — content merged into resilience.py

### `resilience_config.py`

Backward-compatible re-export facade — content merged into resilience.py

### `resilience_numeric.py`

Backward-compatible re-export facade — content merged into resilience.py

### `resilience_retry.py`

Backward-compatible re-export facade — content merged into resilience.py

### `resilience_state.py`

Backward-compatible re-export facade — content merged into resilience.py

### `resilience_utils.py`

Backward-compatible re-export facade — content merged into resilience.py

### `resilience_watchdog.py`

Backward-compatible re-export facade — content merged into resilience.py

### `risk_audit_utils.py`

risk_audit_utils.py — R1-4修复: 清理断裂重导出

原 archive/audit_rounds/ 目录已清空，从该目录的重导出会导致 ImportError。
本模块现在从实际存在的模块导入可用符号，对已删除的符号提供 no-op stub。

#### class `AlertLevel`

告警级别 stub

#### class `AlertDeduplicator`

告警去重器 stub

#### class `SimplifiedSPAN`

SPAN保证金计算 stub

- **`func:alert`** — 告警 stub — 仅记录日志
- **`func:safe_get`** — 安全属性获取

### `risk_rules.py`

infra/risk_rules.py — P0-06: 日回撤硬停止统一检查函数

三处独立实现(safety_meta_equity / risk_check_engine / backtest_safety_checker)
委托到此模块，确保固定阈值路径与5日均值乘数路径逻辑一致。

- **`func:check_daily_drawdown_hard_stop`** — 统一的日回撤硬停止检查。

两条路径（互斥）：
1. 当 prev_5day_avg_profit > 0 且 multiplier > 0 时，
   使用5日均值乘数动态阈值: (prev_5day_avg_profit * multiplier) / daily_start_equity
2. 否则使用固定阈值 hard_stop_pct

Args:
    daily_drawdown_pct: 当前日回撤百分比 (0.0~1.0)
    hard_stop_pct: 固定硬停止阈值 (0.0~1.0)
    prev_5day_avg_profit: 前5日平均盈利（绝对值），<=0 时走固定阈值路径
    multiplier: 5日均值乘数，默认2.0
    daily_start_equity: 日初权益（动态阈值计算分母），默认1.0

Returns:
    (should_stop, reason): 是否应停止及原因描述
- **`func:resolve_and_check_daily_drawdown`** — P0-06完整统一: 参数解析+保护+核心判断一站式入口。

统一处理:
1. hard_stop_pct 未显式传入时从 params_service 回退读取
2. daily_start_equity 无效时将 effective_hard_stop 设为 inf（禁用固定阈值路径）
3. 调用 check_daily_drawdown_hard_stop 执行双路径判断

### `scheduler_manager_proxy.py`

scheduler_manager_proxy.py - SchedulerManagerProxy
Phase 2 (CC-02+CC-04): 从_LifecycleMixin提取的调度器管理Proxy

职责：
- 调度器初始化/停止代理（_init_scheduler, _stop_scheduler）
- 定时任务注册（_add_option_status_diagnosis_job, _add_tick_sync_job等）
- 检查待定订单任务（_ensure_check_pending_orders_job）

注意：调度器核心逻辑已在StrategyScheduler类中，本Proxy仅做方法代理和参数适配。

Provider接口：
- provider._scheduler_manager: StrategyScheduler实例
- provider.strategy_id: 策略ID
- provider._order_service: 订单服务

#### class `SchedulerManagerProxy`

调度器管理Proxy — 初始化/停止/任务注册代理

- **`init_scheduler`** — 初始化调度器
- **`stop_scheduler`** — 停止调度器
- **`add_option_status_diagnosis_job`** — 添加期权5种状态诊断定时任务
- **`add_tick_sync_job`** — 添加缓存刷写定时任务
- **`add_14_contracts_diagnosis_job`** — 添加重点监控合约诊断定时任务
- **`add_trading_jobs`** — 注册交易定时任务
- **`ensure_check_pending_orders_job`** — 确保check_pending_orders任务已注册

### `scheduler_service.py`

scheduler_service.py - 调度服务

合并来源：05_scheduler.py
合并策略：简化调度器实现，提取核心调度功能

重构目标：
- 旧架构：05_scheduler.py (~320行)
- 新架构：scheduler_service.py (~400行)
- 功能增强：超时、重试、优先级、取消

核心改进：
1. 单一职责：专注于定时任务调度
2. 防阻塞设计：所有任务在独立线程执行
3. 线程安全：支持并发操作
4. 增强功能：超时、重试、优先级、取消

作者：CodeArts 代码智能体
版本：v1.1
生成时间：2026-03-16

#### class `JobStatus`

任务状态

#### class `JobInfo`

任务信息

#### class `OnceJobInfo`

一次性任务信息

- **`is_running`** — 检查任务是否正在运行
- **`stop`** — 停止一次性任务

#### class `SchedulerService`

调度服务 - 定时任务管理

职责：
1. 定时任务调度
2. 任务执行管理
3. 防阻塞保护
4. 任务状态监控

职责边界：通用定时任务调度(超时/重试)，与strategy/StrategyScheduler(策略业务调度)分工。
本类负责通用任务调度(超时/重试/优先级/取消)，StrategyScheduler负责策略业务调度(交易循环/持仓风控/缓存刷写)。

使用方式：
    scheduler = SchedulerService()
    scheduler.add_job(my_func, interval=60, job_id="task1", priority=10)
    scheduler.start()

- **`__init__`** — 初始化调度服务

Args:
    logger_func: 日志输出函数（可选）
- **`start`** — 启动调度器
- **`stop`** — 停止调度器
- **`is_running`** — 检查是否运行中
- **`add_job`** — 添加定时任务

Args:
    func: 任务函数
    interval: 执行间隔（秒）
    job_id: 任务ID（可选）
    run_async: 是否异步执行（默认True，防止阻塞）
    priority: 优先级（数值越大优先级越高）
    timeout: 超时时间（秒）
    max_retries: 最大重试次数

Returns:
    任务ID，失败返回None
- **`remove_job`** — 移除任务
- **`get_job`** — 获取任务信息
- **`get_all_jobs`** — 获取所有任务（按优先级排序）
- **`pause_job`** — 暂停任务
- **`resume_job`** — 恢复任务
- **`cancel_job`** — 取消任务（统一入口：同时处理定时任务和一次性任务）
- **`add_once_job`** — 添加一次性延迟任务

Args:
    func: 任务函数
    delay: 延迟时间（秒）
    job_id: 任务ID（可选）

Returns:
    任务ID
- **`_master_loop`** — 主调度循环
- **`_try_run_job`** — 尝试执行任务
- **`_run_job_with_timeout`** — 带超时保护的任务执行

R5-E-11修复: 添加重叠执行防护，防止同一任务并发执行。
R15-P1-RES-14修复: 添加task_timeout_sec默认300秒，超时cancel。
- **`_cleanup_dead_threads`** — 清理已结束的线程
- **`_log`** — 输出日志
- **`get_stats`** — 获取统计信息
- **`clear`** — 清空所有任务

- **`func:get_scheduler_service`** — P1-43修复: SchedulerService单例工厂——生产代码应通过此函数获取实例

### `security.py`

security/统一安全模块 — 合并自2个子模块

#### class `_SecureCredential`

#### class `SecurityEventResponder`

#### class `SecurityProfile`

- **`func:_get_current_security_profile`** — 从环境变量获取当前安全配置
- **`func:_get_security_config`** — 获取当前环境的安全配置
- **`func:apply_security_profile`** — SEC-P2-01/03修复: 应用指定安全配置profile

根据profile设置日志级别、堆栈暴露策略等安全参数。
返回应用的安全配置字典。
- **`func:SANITIZE_ERROR_MSG`** — 消毒错误消息: 替换内部路径为<PATH_REDACTED>
- **`func:_truncate_stack_trace`** — 截断异常堆栈输出

Args:
    exc: 异常对象，None时使用当前异常
    max_lines: 最大行数，None时从安全配置获取

Returns:
    截断后的堆栈字符串
- **`func:_safe_unpickle`** — 安全unpickle: 默认禁止unpickle不可信数据源

Args:
    data: pickle字节流
    allow_untrusted: 是否允许不可信数据unpickle(仅DEV环境)

Raises:
    RuntimeError: 禁止unpickle不可信数据时
- **`func:enable_mlock_if_linux`** — Linux环境下调用mlockall防止内存被swap到磁盘，避免core dump泄露密钥

Returns:
    bool: True表示成功启用mlock，False表示不支持或失败
- **`func:disable_core_dump`** — 禁用core dump（Linux/Unix）

Returns:
    bool: True表示成功禁用

### `serialization_utils.py`

统一序列化工具 - 修复SER-01/SER-P1-01/SER-P1-03

解决审计报告中的序列化问题：
- SER-01: JSON default=str导致datetime不可逆丢失
- SER-P1-01: WAL日志双重default=str导致时间戳冗余丢失
- SER-P1-03: NaN/Infinity在JSON序列化中无防护

修复日期：2026-05-25
审计报告：第二十轮独立审计报告_全系统全链路核查_20260523.md

#### class `SerializationError`

P2-35修复: 友好的序列化错误消息

- **`func:json_default_serializer`** — JSON序列化器 - 修复SER-01/SER-P1-01/SER-P1-03

支持类型：
- datetime.datetime/datetime.date → ISO格式字符串（可逆）
- pandas.Timestamp → ISO格式字符串（可逆）
- numpy.integer → Python int
- numpy.floating → Python float（含NaN/Infinity特殊处理）
- decimal.Decimal → str（精度保留）
- 自定义对象 → __dict__

NaN/Infinity处理（SER-P1-03）：
- NaN → {"__special__": "NaN"}
- Infinity → {"__special__": "Infinity"}
- -Infinity → {"__special__": "-Infinity"}

向后兼容：
- 旧数据（字符串格式datetime）仍可读取
- 新数据使用特殊标记格式
- **`func:json_object_hook`** — JSON反序列化器 - 修复SER-01/SER-P1-01/SER-P1-03

配合json.loads使用：
>>> data = json.loads(json_str, object_hook=json_object_hook)

支持类型：
- {"__datetime__": "..."} → datetime.datetime
- {"__date__": "..."} → datetime.date
- {"__timestamp__": "..."} → pandas.Timestamp
- {"__special__": "NaN/Infinity/-Infinity"} → numpy值
- {"__decimal__": "..."} → decimal.Decimal
- {"__series__": [...]} → pandas.Series
- {"__dataframe__": [...]} → pandas.DataFrame

向后兼容：
- 旧格式字符串datetime仍可正常使用
- **`func:json_dumps`** — 统一JSON序列化接口 - 替换json.dumps(..., default=str)

使用方式：
>>> json_str = json_dumps(data)           # 紧凑输出，适用于JSONL日志
>>> json_str = json_dumps(data, indent=2) # 缩进输出，适用于配置文件

等价于：
>>> json_str = json.dumps(data, default=json_default_serializer, **kwargs)

默认参数：
- indent=None（紧凑输出，适用于JSONL日志；配置文件可传indent=2）
- ensure_ascii=False（支持中文）
- sort_keys=True（一致性）

Raises TypeError on serialization failure.
For non-critical paths where fallback is acceptable, use safe_json_dumps instead.

R21-MEM-P1-14修复: 对相同对象缓存序列化结果，避免高频调用重复序列化。
- **`func:json_loads`** — 统一JSON反序列化接口 - 配合json_dumps使用

使用方式：
>>> data = json_loads(json_str)

等价于：
>>> data = json.loads(json_str, object_hook=json_object_hook, **kwargs)
- **`func:safe_json_dumps`** — 安全JSON序列化 - 失败时返回fallback而非抛异常

解决SER-P1-02（事件持久化截断可能损坏JSON结构）

仅用于日志/非关键路径。关键路径应使用json_dumps（失败时抛TypeError）。

使用方式：
>>> json_str = safe_json_dumps(data, fallback='{}')
- **`func:validate_json_roundtrip`** — 验证JSON roundtrip（往返）保真度

解决SER-P1-06（配置文件无round-trip保障）

使用方式：
>>> is_valid, error = validate_json_roundtrip(config)
>>> if not is_valid:
>>>     print(f"Roundtrip failed: {error}")

返回：
- (True, "") - roundtrip成功
- (False, error_msg) - roundtrip失败，返回错误信息
- **`func:sanitize_for_json`** — 清理对象以便JSON序列化 - 递归处理

解决SER-P1-03（NaN/Infinity无防护）

使用方式：
>>> clean_obj = sanitize_for_json(obj)
>>> json_str = json.dumps(clean_obj)  # 无需default参数
- **`func:yaml_safe_dump`** — 统一YAML序列化 - 修复SER-02（YAML dump/safe_load不匹配）

解决：
- yaml.dump()写入但yaml.safe_load()读取导致失败
- 统一使用safe_dump/safe_load配对

使用方式：
>>> yaml_safe_dump(config, open('config.yaml', 'w', encoding='utf-8'))
- **`func:yaml_safe_load`** — 统一YAML反序列化 - 配合yaml_safe_dump使用

SER-P1-13修复: YAML布尔值守卫 — 预处理yes/no/on/off为显式布尔值，
防止YAML将布尔字符串静默转换为Python bool导致的配置语义错误。

使用方式：
>>> config = yaml_safe_load(open('config.yaml'))
- **`func:safe_pickle_load`** — SER-P1-07/SER-P1-17修复: 安全Pickle加载，防止RCE攻击

警告：Pickle反序列化可执行任意代码，仅加载可信文件！

使用方式：
>>> data = safe_pickle_load('data.pkl')

安全措施：
1. 校验文件路径（防止路径遍历）
2. 记录警告日志（提醒用户风险）
3. 限制文件大小（防止内存炸弹）
- **`func:safe_pickle_dump`** — SER-P1-07修复: 安全Pickle保存

使用方式：
>>> safe_pickle_dump(data, 'data.pkl')
- **`func:safe_dataframe_to_parquet`** — SER-P1-04/SER-P1-08修复: 安全DataFrame保存到Parquet

解决：
- SER-P1-04: 时区处理（保存时区信息）
- SER-P1-08: 索引保留（可选保留索引）
- P2-12: Schema版本元数据（写入schema_version和created_at）

使用方式：
>>> safe_dataframe_to_parquet(df, 'data.parquet', preserve_index=True)
- **`func:safe_dataframe_from_parquet`** — SER-P1-04修复: 安全DataFrame从Parquet加载，恢复时区信息

P2-12: 读取时验证schema_version元数据，版本不匹配时发出WARNING。

使用方式：
>>> df = safe_dataframe_from_parquet('data.parquet')
- **`func:safe_csv_read`** — SER-P1-09修复: 安全CSV读取，指定dtype防止类型推断错误

使用方式：
>>> df = safe_csv_read('data.csv', dtype={'instrument_id': str, 'volume': int})
- **`func:safe_csv_write`** — SER-P1-09修复: 安全CSV写入

使用方式：
>>> safe_csv_write(df, 'data.csv')
- **`func:safe_jsonl_write`** — SER-P1-10修复: 安全JSONL写入，统一转义处理

使用方式：
>>> count = safe_jsonl_write(records, 'data.jsonl')

返回：写入记录数
- **`func:safe_jsonl_append_line`** — P1-16修复: 安全JSONL逐行追加写入，带截断保护

解决：单条记录序列化后可能超长（如含大字段），导致JSONL文件损坏或读取OOM。

使用方式：
>>> with open('data.jsonl', 'a', encoding='utf-8') as f:
...     safe_jsonl_append_line(f, record)
>>> # 或对缓存句柄：
>>> safe_jsonl_append_line(self._log_f, record)

行为：
1. 序列化record为JSON字符串
2. 若行长度超过MAX_JSONL_LINE_LENGTH(1MB)，截断并添加__truncated标记
3. 写入行 + 换行符
- **`func:safe_jsonl_read`** — SER-P1-10修复: 安全JSONL读取

使用方式：
>>> records = safe_jsonl_read('data.jsonl')
- **`func:validate_duckdb_parquet_chain`** — SER-P1-11修复: DuckDB类型一致性检查 — 验证DataFrame→Parquet→DuckDB往返类型一致性

DuckDB读取Parquet时可能发生类型推断偏移（如int64→double、bool→int8），
本函数检测并报告类型不一致。

使用方式：
>>> is_consistent, detail = validate_duckdb_parquet_chain(df, 'data.parquet')
>>> if not is_consistent:
>>>     print(f"类型不一致: {detail}")

返回：
- (True, "") - 类型一致
- (False, error_msg) - 存在类型不一致
- **`func:normalize_parquet_columns`** — SER-P1-15修复: 统一Parquet列名为小写，消除跨环境大小写不一致

DuckDB/Spark等引擎读取Parquet时可能将列名转为小写，
此函数统一为小写，确保round-trip一致性。
- **`func:serialization_perf_monitor`** — P2-28修复: 序列化性能监控装饰器
- **`func:route_data_by_version`** — P2-34修复: 多版本数据格式路由策略
- **`func:rotate_jsonl_if_needed`** — P2-01修复: JSONL文件轮转公共函数 — 统一3个调用点的重复实现

当文件大小超过max_bytes时执行编号轮转:
  - 删除最老备份 .{backup_count}
  - 依次重命名 .{backup_count-1} → .{backup_count}, ..., .1 → .2
  - 当前文件重命名为 .1
使用os.replace()替代os.rename()确保原子性(跨平台安全)
对ENOSPC(磁盘满)记录CRITICAL日志而非静默吞没

Args:
    filepath: JSONL文件路径
    max_bytes: 最大文件大小(字节),默认50MB
    backup_count: 保留备份数,默认3

Returns:
    True if rotation was performed, False otherwise

### `service_container.py`

#### class `ServiceContainer`

服务容器 - 完整的依赖管理

层级规则:
  L0 基础设施: event_bus, config, params
  L1 数据层:   storage, market_data
  L2 业务层:   analytics, risk, order, signal, t_type, diagnosis, ui
  L3 策略层:   strategy_core

  L0 不依赖任何层, L1 仅依赖 L0, L2 仅依赖 L0+L1, L3 依赖 L0+L1+L2
  禁止反向依赖(高层→低层中跳过中间层)

R5-T-01修复: 循环依赖处理策略
  strategy_ecosystem → risk_service → signal_service → strategy_ecosystem
  解决: 使用延迟绑定(lazy binding)，L2层服务初始化时不立即绑定跨层依赖，
  而是在initialize_all()完成后通过bind_cross_layer_dependencies()统一绑定。

R5-T-10修复: 初始化顺序文档
  1. event_bus (L0) - 无依赖
  2. config (L0) - 无依赖
  3. params (L0) - 无依赖
  4. storage (L1) - 依赖event_bus
  5. market_data (L1) - 依赖storage, event_bus
  6. analytics (L2) - 依赖market_data
  7. risk (L2) - 依赖market_data
  8. order (L2) - 依赖risk
  9. signal (L2) - 依赖analytics
  10. t_type (L2) - 依赖signal
  11. diagnosis (L2) - 依赖t_type
  12. ui (L2) - 依赖diagnosis
  13. strategy_core (L3) - 依赖storage, event_bus, config, params

- **`register`** — 注册服务（带层级校验）
- **`get`** — 获取服务
- **`initialize_all`** — 按依赖顺序初始化所有服务
- **`bind_cross_layer_dependencies`** — R5-T-01修复: 绑定跨层依赖（延迟绑定策略）

L2层服务初始化时不立即绑定跨层依赖，
在initialize_all()完成后统一绑定，解决循环依赖问题。
- **`_check_circular_dependencies`** — 检查循环依赖
- **`create_and_register_all_services`** — 创建并注册所有服务（按依赖顺序）

### `service_contracts.py`

P1-34修复: 服务协议定义 — 统一 health_check / get_service_name 接口约束

多个服务类均实现了 health_check() 和 get_service_name()，
但没有一个统一的 ABC 或 Protocol 强制约束。
本模块使用 typing.Protocol 定义 ServiceProtocol，使接口契约显式化。

Protocol 是隐式实现的（structural subtyping），
其他服务类无需修改即可满足协议，只需确保协议定义存在。

#### class `ServiceProtocol`

服务协议 — 所有服务类应满足此接口

方法签名：
- health_check() -> Dict[str, Any]: 返回服务健康状态字典
- get_service_name() -> str: 返回服务名称

- **`health_check`** — 返回服务健康状态字典

Returns:
    Dict[str, Any]: 包含服务健康状态的字典，
        至少应包含 'healthy' (bool) 和 'service_name' (str) 键
- **`get_service_name`** — 返回服务名称

Returns:
    str: 服务名称，通常为类名

### `shared_providers.py`

Shared Provider Interfaces — Phase 1 architectural refactoring.

Defines explicit interfaces for the 6 cross-shared self._ attributes
currently implicitly shared between Mixin classes via self:

  self._state, self._is_running, self._is_paused,
  self._lock, self._stats, self._safety_meta_layer

These protocols allow Mixin→composition migration (Phase 2) by providing
explicit dependency injection interfaces. During Phase 1, existing code
continues to work via self attributes; these interfaces are the target
for Phase 2 composition refactoring.

#### class `StateProvider`

Interface for strategy state access.

#### class `LockProvider`

Interface for thread lock access.

#### class `StatsProvider`

Interface for statistics access.

#### class `SafetyProvider`

Interface for safety meta layer access.

#### class `DefaultStateProvider`

Default implementation of StateProvider for standalone use.

#### class `DefaultLockProvider`

Default implementation of LockProvider for standalone use.

#### class `DefaultStatsProvider`

Default implementation of StatsProvider for standalone use.

#### class `DefaultSafetyProvider`

Default implementation of SafetyProvider (no-op).

### `shared_trading_constants.py`

R27-CP-01-FIX: 生产/离线双向依赖修复 — 共享交易常量模块

原始定义位置:
  - P0_IRON_RULES:       param_pool/backtest_param_grids.py (原line 647)
  - REASON_MULTIPLIERS:  param_pool/backtest_param_grids.py (原line 619)
  - detect_rollover_gaps:  param_pool/backtest_runner_base.py (原line 2965)
  - compute_rollover_cost: param_pool/backtest_runner_base.py (原line 3048)

提取原因: 生产模块(strategy_core_service.py, position_service.py, risk_circuit_breaker.py)
原先从param_pool/task_scheduler.py导入这些常量，导致生产→回测层反向依赖。
现将共享常量提取到此模块，生产层和回测层均从此处导入，消除反向依赖。

- **`func:detect_rollover_gaps`** — P0-裂缝5：检测合约展期数据断裂点

找出合约代码变化的位置，计算展期跳空幅度。
若跳空幅度 > gap_threshold(默认2%)，标记为展期点。

Returns:
    List[Dict]: 每个展期点的详细信息
- **`func:compute_rollover_cost`** — P1-1修复：换月成本建模 — 量化calendar_basis(日历基差) + slippage(滑点)

在换月点计算三部分成本:
  1. gap_cost: 展期跳空价差(已由detect_rollover_gaps检测)
  2. calendar_basis: 近远月合约基差(默认5bps)
  3. rollover_slippage: 换月双边滑点(默认3bps)

Returns:
    Dict: 换月总成本及各分项

### `shared_utils.py`

shared_utils/统一工具模块 — 合并自6个子模块

✅ ID唯一 / 方法唯一 / 传递渠道唯一：跨模块共享的工具函数统一定义在此
禁止在其他模块中重复定义这些函数，必须从此模块导入。

合并来源 (R27-CP-04-FIX):
- shared_utils_sql: SQL安全相关
- shared_utils_types: 类型转换相关
- shared_utils_instrument: 合约工具相关
- shared_utils_contracts: 契约编程相关
- shared_utils_infra: 基础设施相关
- shared_utils (facade): 门面自有定义

#### class `ToDictMixin`

为 dataclass 提供统一的 to_dict 方法

使用 dataclasses.asdict 自动转换，替代各模块中
重复的 `def to_dict(self): return asdict(self)` 实现。

用法:
    @dataclass
    class MyData(ToDictMixin):
        x: int = 0

#### class `RingBuffer`

环形缓冲区，线程安全

R21-MEM-P1-15修复: 添加TTL过期检查，append时记录时间戳，
查询时自动过滤过期条目，防止陈旧信号占用内存。

- **`get_fresh`** — 返回未过期的条目列表（TTL机制），若未启用TTL则返回全部数据

#### class `ShardRouter`

确定性分片路由器：品种ID驱动的自动通道绑定

核心设计：
1. 用 hashlib.md5 替代 Python hash()，保证跨进程/跨重启确定性
2. 品种代码全链路直通，零配置自动绑定
3. 同一品种始终命中固定通道，tick时序天然正确
4. 通道绑定注册表可审计（§8.4 路由规则可审计）
5. 简单取模路由：shard_key % shard_count，2^n时等价位掩码

用法：
    router = ShardRouter(shard_count=16)
    idx = router.route("IF2506")   # 返回确定性shard索引
    router.get_binding_map()        # 返回品种→通道绑定表

- **`_deterministic_hash`** — P2-31修复: 使用compute_content_hash替代直接hashlib.md5

#### class `ThreadLifecycleManager`

管理多服务线程的启停与排水顺序（§8.5 并发安全）

设计原则：
- 不包含业务逻辑，仅管理线程生命周期
- 强制停止顺序：(1) set StopEvent → (2) join Writers → (3) drain → (4) flush
- 部分启动失败自动回滚已启动线程
- 独立模块，不增加核心大模块负担

- **`detect_zombie_threads`** — 检测已停止但未清理的僵尸线程引用

#### class `InitPhase`

#### class `InitializationError`

#### class `InitStateMachine`

#### class `IdempotencyGuard`

OPS-P1-09修复: 幂等性保证

确保操作不会被重复执行。使用操作ID跟踪已完成的操作，
相同ID的操作只执行一次。

用法:
    guard = IdempotencyGuard()
    if guard.try_acquire("order_123"):
        # 执行操作
        guard.mark_completed("order_123", result)
    else:
        result = guard.get_result("order_123")

- **`try_acquire`** — 尝试获取操作执行权

Returns:
    True: 可以执行; False: 操作已在进行或已完成
- **`mark_completed`** — 标记操作完成
- **`get_result`** — 获取已完成操作的结果
- **`is_completed`** — 检查操作是否已完成

#### class `AutoRollbackContext`

UPG-P1-09修复: 自动回滚上下文管理器

在更新操作中使用，确保失败时自动回滚到更新前的状态。

用法:
    with AutoRollbackContext("schema_update") as ctx:
        ctx.checkpoint("step1", state_before_step1)
        do_step1()
        ctx.checkpoint("step2", state_before_step2)
        do_step2()
    # 如果do_step2()抛出异常，自动回滚到step1的状态

- **`__init__`** — 初始化自动回滚上下文

Args:
    operation_name: 操作名称（用于日志）
    rollback_handler: 自定义回滚处理函数，签名: handler(checkpoint_name, checkpoint_state)
- **`checkpoint`** — 设置检查点，保存回滚状态

Args:
    name: 检查点名称
    state: 回滚时恢复的状态
- **`mark_completed`** — 标记操作成功完成，不需要回滚

#### class `ToDictMixin`

P1-58修复: 统一to_dict()实现基类

提供默认的to_dict()方法，基于实例__dict__序列化。
子类可覆盖to_dict()以自定义序列化逻辑。

#### class `PreconditionError`

OPS-P1-06修复: 前置条件验证失败异常

#### class `PostconditionError`

OPS-P1-07修复: 后置条件验证失败异常

#### class `OperationTimeoutError`

OPS-P1-08修复: 操作超时异常

#### class `PendingPullbackSignal`

- **`check_retrace_tick`** — P0-2修复：逐tick实时跟踪模式 — 每个tick即时判断回撤

#### class `PullbackManager`

通用回撤开仓管理器 — 非高频策略共用

- **`process_tick`** — P0-2修复：逐tick实时跟踪模式

#### class `StrategyRegressionTest`

UPG-P1-13修复: 策略逻辑回归测试框架

#### class `ModelVersionManager`

模型版本管理器 — 支持运行时切换和回滚

- **`rollback`** — 回滚到旧版本

#### class `RuntimeMonitor`

运行时监控 — 跟踪关键指标偏差

- **`record_slippage_deviation`** — 记录滑点偏差
- **`record_position_ratio`** — 记录仓位比值
- **`record_execution_delay`** — 记录执行延迟
- **`record_signal_latency`** — 记录信号延迟
- **`get_summary`** — 获取监控摘要

#### class `TradeAction`

交易动作枚举 — 继承str以保持序列化兼容性

#### class `TradeDirection`

交易方向枚举 — 继承str以保持序列化兼容性

#### class `SignalType`

信号类型枚举 — 继承str以保持序列化兼容性

- **`func:sanitize_sql_identifier`** — 清理SQL标识符（表名、列名）以防止注入。

仅允许字母、数字、下划线和点号字符，且必须以字母或下划线开头。

Args:
    identifier: 待清理的SQL标识符

Returns:
    str: 验证通过的标识符

Raises:
    ValueError: 标识符包含非法字符时抛出
- **`func:sanitize_sql_value`** — 清理SQL值，通过转义单引号防止注入。

Args:
    value: 待清理的SQL值，None返回'NULL'

Returns:
    str: 转义后的SQL值字符串（含引号）
- **`func:to_float32`** — 将数值转换为float32精度（唯一实现）

NP-P1-12修复: None/NaN/inf显式告警而非静默返回0.0

Args:
    value: 输入数值

Returns:
    float: float32精度的数值
- **`func:safe_int`** — 安全转换为整数
- **`func:safe_float`** — 安全转换为浮点数
- **`func:safe_price_check`** — R24-P1-IV-10修复: 安全价格检查，NaN/Inf/非正数均返回False
- **`func:to_native_type`** — R5-I-04修复: 将numpy类型转换为Python原生类型

np.int64不是int的子类，np.float64不是float的子类，
在字典操作和JSON序列化时可能失败。此函数递归转换所有numpy类型。

Args:
    obj: 任意Python对象

Returns:
    转换后的Python原生类型对象
- **`func:safe_get_float`** — P1-20: 统一安全浮点获取 — getattr + safe_float
- **`func:safe_get_int`** — P1-20: 统一安全整数获取 — getattr + safe_int
- **`func:normalize_option_type`** — 标准化option_type（唯一实现）

Args:
    opt_type: 期权类型字符串 ('C', 'CALL', 'P', 'PUT'等)

Returns:
    str: 标准化的期权类型 ('CALL' 或 'PUT')
- **`func:normalize_instrument_id`** — 移除交易所前缀和平台前缀，保留合约ID原始格式（品种ID直通，不做大写化）

Args:
    instrument_id: 可能含交易所前缀或平台前缀的合约ID (如 "DCE.m2605" 或 "platform|m2605")

Returns:
    str: 标准化后的合约ID (如 "m2605")，保留交易所原始大小写
- **`func:extract_product_code`** — 从合约ID提取品种代码（唯一实现）

Args:
    instrument_id: 合约ID (如 "IO2506-C-4000" 或 "al2605C18900")

Returns:
    str: 品种代码 (如 "IO" 或 "al")
- **`func:extract_strike_price`** — 从期权合约ID提取行权价（唯一实现，委托SubscriptionManager）

Args:
    instrument_id: 期权合约ID

Returns:
    Optional[float]: 行权价，解析失败返回None
- **`func:normalize_year_month`** — 归一化年月格式（唯一实现）

支持多种输入格式，统一输出为 YYMM 格式：
- '6M05' -> '2605' (6月05年 -> 26年05月)
- '26M05' -> '2605' (2位年份前缀+月份)
- '202605' -> '2605' (完整年份)
- '2605' -> '2605' (已是标准格式，直通)

Args:
    year_month: 年月字符串

Returns:
    str: 归一化后的 YYMM 格式年月
- **`func:get_shard_router`** — 获取全局单例 ShardRouter（方法唯一原则）

全链路只创建一个 ShardRouter 实例，确保第一层和第二层路由完全一致。
- **`func:atomic_replace_file`** — UPG-P1-07修复: 原子替换文件（备份+写入+验证+失败回滚）

实现热更新的事务性保证：
1. 备份原文件
2. 写入新内容到临时文件
3. 验证写入成功
4. 原子替换目标文件
5. 失败时从备份回滚

Args:
    target_path: 目标文件路径
    new_content: 新文件内容
    backup_suffix: 备份文件后缀

Returns:
    Dict: {success: bool, backup_path: str, error: str}
- **`func:cleanup_atomic_backup`** — UPG-P1-07修复: 清理原子替换的备份文件

确认更新成功后调用此函数清理备份。

Args:
    backup_path: 备份文件路径
- **`func:sanitize_filename`** — R2-3/P2-19修复: 文件名安全化处理

将路径分隔符替换为下划线，防止路径遍历攻击。
统一替代分散在 order_persistence/order_wal_state_service 中的
内联 replace('/', '_').replace('\', '_') 实现。

Args:
    name: 原始文件名（如 order_id）

Returns:
    安全的文件名字符串
- **`func:check_version_sync`** — UPG-P1-11修复: 检查代码版本与手册版本是否同步

确保代码实现与操作手册版本一致，防止版本漂移导致操作错误。

Args:
    code_version: 代码版本号（默认使用内置版本）
    manual_version: 手册版本号（默认使用内置版本，可通过环境变量MANUAL_VERSION覆盖）

Returns:
    Dict: {synced: bool, code_version: str, manual_version: str, warning: str}
- **`func:check_shared_utils_invariants`** — P2修复: 检查shared_utils模块中的不变量

将注释中的不变量断言转为运行时检查，
违反时执行恢复策略（使用安全默认值）。

Returns:
    Dict: {all_passed: bool, violations: list, recoveries: list}
- **`func:set_global_seed`** — P1-54修复: 统一随机数种子设置入口

同时设置Python random和numpy random种子，确保可复现性。
返回seed值方便链式调用。
- **`func:get_log_dir`** — P2-21修复: 统一日志目录路径获取

从ConfigService获取日志目录，fallback到项目根/logs。
Args:
    subdir: 子目录名（如'shadow', 'eviction_audit'），空字符串表示根日志目录
Returns:
    日志目录的绝对路径
- **`func:compute_content_hash`** — P1-64修复: 统一内容哈希计算

Args:
    content: 要哈希的字符串内容
    algorithm: 哈希算法，默认md5（非安全场景）；安全场景使用sha256
    truncate: 截断长度，0表示不截断

Returns:
    十六进制哈希字符串
- **`func:generate_prefixed_id`** — 生成带前缀的唯一标识符。

统一替代分散在代码各处的 ``f"{PREFIX}_{uuid.uuid4().hex[:N]}"`` 模式。

Args:
    prefix: ID 前缀，如 ``"ORD"``、``"SIG"``、``"HFT"``。
            为空时仅返回 hex 片段。
    hex_len: 截取 uuid hex 的长度 (1-32)，默认 12。

Returns:
    ``f"{prefix}_{hex}"`` 或 ``hex``（prefix 为空时）。
- **`func:get_tick_field`** — P1-55修复: 统一Tick字段提取函数——全项目唯一权威实现

按候选属性名列表依次尝试getattr，返回第一个非None值。
tick_processing_service._get_tick_field 和 position_service._get_platform_attr
均应委托到此函数。
- **`func:normalize_tick`** — P1-55修复: 平台适配层——统一Tick属性名映射为标准字典
- **`func:require_precondition`** — OPS-P1-06修复: 前置条件验证

在执行关键操作前验证前置条件，不满足时抛出PreconditionError。

Args:
    condition: 前置条件
    message: 失败时的错误信息

Raises:
    PreconditionError: 前置条件不满足
- **`func:ensure_postcondition`** — OPS-P1-07修复: 后置条件验证

在关键操作执行后验证后置条件，不满足时抛出PostconditionError。

Args:
    condition: 后置条件
    message: 失败时的错误信息

Raises:
    PostconditionError: 后置条件不满足
- **`func:with_timeout`** — OPS-P1-08修复: 带超时控制的操作执行

使用线程执行函数，超时后抛出OperationTimeoutError。

Args:
    func: 要执行的函数（无参数）
    timeout_sec: 超时秒数
    logger: 可选的logger实例

Returns:
    func()的返回值

Raises:
    OperationTimeoutError: 操作超时
- **`func:unified_ts_ms`** — [R22-TIME-P1-15] 统一毫秒精度时间戳，用作缓存键，消除毫秒/微秒混用
- **`func:get_annualize_factor`** — 按策略时间框架及采样频率选择Sharpe年化因子
- **`func:retry_with_limit`** — R5-E-05修复: 带最大重试次数和指数退避的重试辅助函数

P2-12修复: 内部委托到 BoundedRetry, 保持签名兼容。
- **`func:api_version`** — UPG-P1-04修复: API版本标记装饰器
- **`func:get_api_version`** — UPG-P1-04修复: 获取函数的API版本号
- **`func:check_api_compatibility`** — UPG-P1-04修复: 检查API版本兼容性
- **`func:compute_execution_delay_slippage_bps`** — 基于波动率缩放的执行延迟滑点（布朗运动理论）
- **`func:compute_slippage_bps`** — 统一滑点计算函数（生产/回测/评判三系统共享）
- **`func:__getattr__`** — 延迟导入 resilience 模块，避免循环依赖

### `shared_utils_contracts.py`

Backward-compatible re-export facade — content merged into shared_utils.py

### `shared_utils_infra.py`

Backward-compatible re-export facade — content merged into shared_utils.py

### `shared_utils_instrument.py`

Backward-compatible re-export facade — content merged into shared_utils.py

### `shared_utils_sql.py`

Backward-compatible re-export facade — content merged into shared_utils.py

### `shared_utils_types.py`

Backward-compatible re-export facade — content merged into shared_utils.py

### `singleton_registry.py`

Singleton namespace isolation — R19-AP-P1-06 fix.

Provides a centralized registry for module-level singletons with namespace
isolation, replacing scattered _instance variables across 10+ files.

Usage:
    from ali2026v3_trading.infra.singleton_registry import SingletonRegistry

    # Register a singleton
    registry = SingletonRegistry.get_registry('risk_service')
    registry.set('risk_service_instance', my_instance)

    # Retrieve a singleton
    instance = registry.get('risk_service_instance')

    # Reset all singletons (for testing)
    SingletonRegistry.reset_all()

#### class `_Namespace`

Thread-safe namespace for a group of related singletons.

#### class `SingletonRegistry`

Centralized singleton registry with namespace isolation.

Each module gets its own namespace, preventing cross-module singleton
name collisions and enabling fine-grained test resets.

- **`get_registry`** — Get or create a namespace for a module.
- **`reset_all`** — Reset all singleton namespaces (for testing).
- **`reset_namespace`** — Reset a specific namespace.
- **`list_namespaces`** — List all registered namespace names.
- **`total_singleton_count`** — Count total singletons across all namespaces.
- **`register_singleton`** — 注册单例实例，可选附带cleanup回调用于资源释放。

Args:
    name: 单例名称（格式: 'namespace.key'，如 'risk_service.instance'）
    instance: 单例实例
    cleanup_fn: 可选清理回调，在cleanup_all_singletons()时调用
- **`cleanup_all_singletons`** — 调用所有已注册的cleanup回调，然后清空所有命名空间。

适用于策略生命周期结束时释放大对象（如DataFrame、线程池等）。

### `state_machine.py`

state_machine.py — 状态机抽象基类

P1-56修复: 提取 LifecycleStateMachine 与 InitStateMachine 的公共抽象，
消除多业务对象状态机重复定义。

#### class `BaseStateMachine`

状态机抽象基类

提供统一的状态转换接口，子类需实现:
- can_transition(target_state) -> bool: 判断是否可转换到目标状态
- transition_to(target_state) -> None: 执行状态转换
- state 属性: 获取当前状态

子类应定义 _LEGAL_TRANSITIONS: Dict[str, Set[str]] 类属性，
描述合法的状态转换图。若子类使用不同的转换逻辑（如顺序阶段），
可不使用 _LEGAL_TRANSITIONS 而自行实现 can_transition。

- **`state`** — 当前状态
- **`can_transition`** — 判断是否可以从当前状态转换到目标状态
- **`transition_to`** — 执行状态转换，非法转换时记录警告并跳过

### `state_store.py`

#### class `StateSnapshot`

#### class `StateStore`

- **`_validate_format_version`** — P0-16修复: 验证加载的状态字典format_version是否兼容

Args:
    loaded_dict: 从持久化加载的状态字典

Returns:
    bool: True=版本兼容, False=版本不兼容
- **`register_callback`** — P1-05修复: 同时注册到EventBus
- **`_notify_callbacks`** — P1-05修复: 统一通过EventBus发布

### `subscription_manager.py`

订阅管理器 - 门面模块（Facade组合，消灭Mixin继承）

#### class `SubscriptionManager`

订阅管理器 - Facade组合（消灭Mixin继承）

通过组合持有3个Service实例，__getattr__委托实现零破坏性变更。

### `system_contracts.py`

Cross-system contracts for production/backtest/judgment alignment.

#### class `RuntimeSystem`

#### class `UnifiedErrorCode`

#### class `UnifiedDataSourceAdapter`

#### class `HealthStatusLevel`

#### class `SignalDiagnosisRecord`

## lifecycle

### `__init__.py`

lifecycle/子系统(P1-R2)

### `lifecycle_bind.py`

lifecycle_bind.py — 平台API绑定/订阅逻辑（从strategy_lifecycle_mixin.py拆分）
职责: bind_platform_apis, 平台订阅/退订, 市场中心提取, 运行时上下文注入, 历史K线异步加载

#### class `LifecycleBind`

### `lifecycle_callbacks.py`

lifecycle_callbacks.py — 生命周期回调逻辑（从strategy_lifecycle_mixin.py拆分）
职责: on_start, on_stop, on_destroy, start/stop/pause/resume/destroy, save_state, _shutdown_runtime_services,
      _log_resource_ownership_table, _publish_event

#### class `LifecycleCallbacks`

### `lifecycle_init.py`

lifecycle_init.py — 初始化逻辑（从strategy_lifecycle_mixin.py拆分）
职责: on_init, analytics初始化, 合约加载, 调度器初始化, 日志初始化, prepare_restart

#### class `LifecycleInit`

### `lifecycle_manager.py`

lifecycle_manager.py - LifecycleManager
Phase 2 (CC-02+CC-04): 从_LifecycleMixin提取的生命周期管理Manager

职责：
- 策略状态管理（_state, _is_running, _is_paused）
- 状态转换（transition_to）
- 生命周期操作（start/pause/resume/stop/destroy/prepare_restart）
- 状态查询（get_state/is_running/is_paused/is_trading/get_uptime）

Provider接口：通过provider（StrategyCoreService实例）获取共享属性：
- provider._state_lock: 状态锁
- provider._lock: 通用锁
- provider._scheduler_manager: 调度器
- provider._stats: 性能统计
- provider._event_bus: 事件总线

设计原则：
- Composition over Inheritance
- 独立可测试，不依赖Mixin继承链
- 通过provider接口显式依赖，替代self._隐式交叉引用

#### class `LifecycleManager`

生命周期管理Manager — 状态/转换/查询

- **`transition_to`** — 线程安全的状态转换方法

### `lifecycle_monitor.py`

lifecycle_monitor.py — 状态查询/性能监控/健康检查（从strategy_lifecycle_mixin.py拆分）
职责: get_state, is_running, is_paused, is_trading, get_uptime, record_tick/trade/signal/error,
      get_stats, health_check, _should_probe_t_type_future, _log_t_type_future_probe

#### class `LifecycleMonitor`

### `lifecycle_parallel.py`

lifecycle_parallel/统一并行操作模块 — 合并自lifecycle_parallel + lifecycle_parallel_ops

#### class `LifecycleParallel`

#### class `LifecycleParallelOps`

### `lifecycle_platform.py`

生命周期平台层 — 从strategy_lifecycle_mixin.py拆分
职责: 平台API绑定、订阅管理、行情推送、API就绪检查、降级处理

#### class `LifecyclePlatform`

### `lifecycle_resource.py`

生命周期资源层 — 从strategy_lifecycle_mixin.py拆分
职责: 资源所有权、线程池、调度器生命周期管理、清理回调注册与执行

#### class `LifecycleResource`

- **`cleanup_all`** — 清理所有注册的资源

Args:
    level: 'normal' — 正常停止，保留可恢复资源（仅shutdown）
           'final'  — 最终销毁，释放所有资源（shutdown + close + clear）

### `lifecycle_state_machine.py`

lifecycle_state_machine/统一状态模块 — 合并自lifecycle_state + lifecycle_state_machine

#### class `StrategyState`

#### class `LifecycleStateMachine`

P1-56修复: 继承 BaseStateMachine 抽象基类

### `lifecycle_transition.py`

lifecycle_transition.py - LifecycleTransition
Phase 2 (CC-P1-04+CC-P1-05): 从strategy_lifecycle_mixin.py提取的状态转换逻辑

职责：
- 线程安全的状态转换 (transition_to)
- 状态转换合法性校验
- 转换事件通知 (通过EventBus)

#### class `LifecycleTransition`

生命周期状态转换 — 独立可测试组件

- **`transition_to`** — 线程安全的状态转换

Returns:
    tuple: (success: bool, old_state: StrategyState, new_state: StrategyState)

### `product_initializer.py`

product_initializer.py — 品种初始化模块

从 config_service.py 提取，解决 SRP 违反问题。
ensure_products_with_retry 处理品种加载和DB建表，不属于配置服务职责。

职责:
- 解析期货/期权配置文件
- 预验证期权->期货映射关系
- 写入DB并验证ID完整性
- 刷新ParamsService缓存

调用方: strategy_lifecycle_mixin.py (on_init)

- **`func:_get_option_underlying_product`** — 期权品种 -> 标的期货品种映射
- **`func:ensure_products_with_retry`** — 三机制根因修复 + 期货建表时同时赋映射ID

流程：
1. 阶段1（纯内存）：解析配置文件，明示式映射，预验证所有期权->期货关系
2. 阶段2（写DB）：期货 upsert 分配 ID -> 同时建立映射 -> 期权 upsert 使用已验证的 underlying_future_id
3. 阶段3（硬断言）：验证 0 个 NULL，否则初始化失败

机制1 - 内存预验证：写DB前验证每个期权的 (underlying_product, year_month) 在期货集合中存在
机制2 - 硬断言替代warning：underlying_future_id 为 NULL 则初始化失败
机制3 - 删除回填SQL：不再需要 underlying_product + year_month 的字符串匹配回填

## risk

### `__init__.py`

risk/子系统(P2-S1)

### `_phase2_pre_reference.py`

risk_service_phase2_pre.py — Phase2重构前旧版单体风控服务参考

本文件为备份参考，记录Phase2重构前的完整单体RiskService实现。
主库已将此文件拆分为risk/子目录下16个模块：
  - risk_service.py (主入口+re-export)
  - risk_check_service.py / risk_check_engine.py (风控检查链)
  - risk_compute_service.py (风控计算)
  - risk_config_provider.py (风控配置)
  - risk_circuit_breaker.py (熔断器Facade)
  - safety_meta_layer.py / safety_meta_equity.py / safety_meta_circuit.py / safety_meta_position.py / safety_meta_audit.py (安全元层)
  - risk_position_bridge.py (持仓桥接)
  - risk_priority_matrix.py (优先级矩阵)
  - _utils.py (内部工具)
  - crack_validation.py (裂缝验证)

经方法级对比验证，旧版所有168个方法均已在拆分后模块中找到等价实现，
包括：
  - _purge_expired_reservations → position/margin_manager.py:71
  - AlertDeduplicator → archive/audit_rounds/risk_audit_utils.py:96
  - SimplifiedSPAN → archive/audit_rounds/risk_audit_utils.py:380
无需从此文件恢复任何功能。

原始备份路径: _backup_p1_20260531_154535/risk_service_phase2_pre.py (6656行)

### `crack_validation.py`

V7实盘前边缘裂缝验证模块

覆盖裂缝14-30的验证函数与缺失功能实现。
裂缝2/5/10/11由其他模块处理。

#### class `CircuitBreakerEvent`

#### class `StateConfirmWindow`

状态确认窗口：连续N次确认才切换。

规则：只有连续且完全相同的新状态才增加计数器，
任何非新状态出现则重置为0。

- **`feed`** — 输入观测状态，返回(是否切换, 切换到什么状态)。

- **`func:validate_circuit_breaker_halts`** — 验证注入熔断事件后生存率是否达标。

通过标准: 注入后equity/初始equity >= survival_threshold
- **`func:validate_shadow_b_stability`** — 验证影子B随机路径的夏普置信区间宽度。

若CI宽度 > 1.0，则应使用影子B的期望夏普（均值）而非单次最大值。
- **`func:apply_tick_rounding`** — 将价格取整到最小变动价位。

direction: 'nearest'四舍五入, 'up'向上取整, 'down'向下取整
- **`func:apply_lot_rounding`** — 手数取整。direction: 'floor'向下, 'ceil'向上
- **`func:validate_tick_rounding_impact`** — 验证取整后策略夏普 >= 取整前夏普 * min_ratio。
- **`func:compute_cascade_slippage`** — 计算状态切换时的级联滑点。P0-20修复: 与回测版对齐使用sqrt冲击模型

新开仓滑点 = min(原滑点 × (1 + sqrt(平仓量/平均成交量)) × cascade_multiplier, cap)
sqrt冲击模型与backtest_state._compute_cascade_slippage_bps一致。
- **`func:validate_cascade_slippage_impact`** — 验证启用级联滑点后夏普衰减是否可接受。
- **`func:compute_discrete_equity`** — 计算离散资金曲线。

每笔期权亏损=全额权利金，资金呈跳变（而非连续）。
- **`func:validate_capital_discretization_gap`** — 验证连续与离散资金曲线的夏普差异 <= max_diff。
- **`func:validate_shadow_order_isolation`** — 验证影子策略执行后主策略资金和持仓完全不变。
- **`func:validate_state_window_boundary`** — 验证5种交替模式下确认窗口行为正确。
- **`func:check_safety_meta_layer`** — 断路器暂停期间仅允许平仓(保护)，禁止新开仓。

返回: (can_open, can_close)
- **`func:validate_circuit_breaker_vs_time_stop`** — 验证4种组合下can_open/can_close的正确性。
- **`func:resample_indicator_ffill`** — 将1分钟指标通过前向填充对齐到目标频率。

target_freq: 目标频率(如5/15/60分钟)
- **`func:validate_indicator_multiscale_consistency`** — 验证原生频率指标下采样 vs 重采样后直接计算的相关系数 > min_corr。
- **`func:compute_sharpe_ci`** — 计算Sharpe比率的近似置信区间。

Sharpe标准误差 ≈ 1/sqrt(n)（简化假设）。
- **`func:validate_alpha_confidence_overlap`** — 验证主-影子Sharpe置信区间重叠比例 <= max_overlap_ratio。

若重叠>50%，标记alpha_unreliable，资金分配回退到基础权重。
- **`func:validate_logic_reversal_timestamp_causality`** — 验证逻辑反转平仓触发时刻，wrong_pct不使用未来数据。

三重验证：
1. 每个signal_timestamp >= 对应的ref_price_timestamp（信号不早于参考价）
2. wrong_pct_bar_timestamps: wrong_pct数据来源bar的时间戳，
   必须 <= signal_timestamp（不使用信号触发后的bar数据）
3. 若wrong_pct_bar_timestamps为None，标记为needs_bar_level_audit
- **`func:compute_kl_divergence`** — 计算两个离散分布的KL散度 KL(P||Q)。

p, q需为概率分布(自动归一化)。
- **`func:validate_shadow_param_orthogonality`** — 验证影子参数分布与主参数分布的KL散度 > min_kl。

将参数向量直方图化为离散分布后计算KL散度。
- **`func:validate_attribution_residual_significance`** — 验证归因残差是否统计显著且超过相对阈值。

触发E7条件: t检验p<alpha 且 mean(|residual|) > residual_pct_threshold * mean(|pnl|)
- **`func:_normal_cdf`** — 标准正态CDF近似（不依赖scipy）。

### `risk_check_engine.py`

Phase4-Sprint11: RiskCheckEngine — risk_check_service规则引擎模式
将risk_check_service.py(1396行)的硬编码检查链重构为可注册规则引擎

#### class `RiskRule`

#### class `RiskContext`

#### class `RiskRuleResult`

#### class `RiskCheckReport`

#### class `RiskCheckEngine`

规则引擎 — 替代risk_check_service硬编码检查链

规则按severity排序: P0 > P1 > P2
P0规则失败立即返回(阻断)，P1/P2规则失败记录但不阻断

- **`check_sharpe_iron_rule`** — P0铁律门控: Sharpe比率铁律检查

当策略Sharpe低于阈值时阻断交易，防止低质量信号开仓。
该方法从risk_check_service._check_sharpe_iron_rule迁移而来。

#### class `PositionLimitRule`

#### class `MarginSufficiencyRule`

#### class `DailyDrawdownRule`

#### class `NearExpiryRule`

#### class `SignalCooldownRule`

### `risk_check_service.py`

#### class `RiskCheckService`

风控检查服务 — 核心逻辑已委托给RiskCheckEngine，LEGACY方法已清理

- **`check_before_trade`** — 交易前风控检查 — 引擎优先，安全回退
- **`detect_abnormal_trading`** — 异常交易检测 — 直接调用AbnormalTradeDetector
- **`_check_abnormal_trading`** — 异常交易行为检查 — 作为风控检查链的一环
- **`_check_safety_meta_layer`** — 检查SafetyMetaLayer的hard_stop和new_open_blocked状态

- **`func:check_snapshot_freshness`** — 风控快照新鲜度检查
- **`func:is_risk_exempt`** — 风控豁免显式字段检查
- **`func:validate_direction`** — direction白名单验证

### `risk_circuit_breaker.py`

#### class `SafetyMetaLayer`

L-1安全元层 — Facade模式(P2拆分后)

通过构造函数注入3个服务实例，以组合替代继承，外部接口不变：
- _circuit_breaker_svc: 断路器核心逻辑
- _drawdown_monitor_svc: 回撤监控逻辑
- _hard_time_stop_svc: 硬时间止损+合规+保证金+展期+健康+统计

三条硬规则（最高权限，不可被策略参数覆盖）：
1. 速率断路器：1分钟内权益回撤超过滚动3σ，暂停交易。
2. 持仓时间硬止损：开仓后max_hold_minutes_hard分钟内浮盈从未达到min_profit_threshold，
   则在max_hold_minutes_hard+30分钟强制平仓
3. 日最大回撤硬停止：当日累计回撤超过前5日平均日收益的daily_drawdown_multiplier倍，
   立即平掉所有仓位，禁止当日任何后续交易，直到下个交易日人工确认后恢复

- **`func:get_risk_circuit_breaker`** — 获取风控熔断器实例（get_safety_meta_layer 的别名）。

### `risk_compute_service.py`

#### class `RiskComputeService`

CC-03/AP-02修复: 风控计算服务

从RiskService提取的B类方法（风控计算/指标），
包含压力测试、PnL归因、风险指标计算、决策评分等。
构造函数接受risk_service引用（用于访问共享状态）。

- **`_compute_stress_test`** — R17-P1-DOC-P1-04修复: 计算压力测试。Args: calc: Greeks计算器; positions_dict: 持仓字典; net_delta/gamma/vega/theta: 净Greeks值。Returns: Dict含jump_1sigma_pnl/jump_2sigma_pnl/L1_trigger_prob
- **`_compute_pnl_attribution`** — R17-P1-DOC-P1-04修复: 计算PnL归因。Args: calc: Greeks计算器; positions_dict: 持仓字典; net_delta/gamma/vega/theta: 净Greeks值。Returns: Dict含delta/gamma/vega/theta贡献
- **`_compute_position_scale`** — R17-P1-DOC-P1-04修复: 计算仓位缩放比例。Args: composite_score: 综合评分0.0~1.0; scores: 各维度评分字典。Returns: float 缩放比例0.0~1.0
- **`_compute_life_score`** — R17-P1-DOC-P1-04修复: 计算生命周期评分。Args: hmm_state: HMM状态字符串。Returns: float 0.0~1.0, 降级时返回0.5(跨策略Greeks聚合降级默认值)
- **`_compute_cycle_resonance_score`** — R17-P1-DOC-P1-04修复: 计算周期共振评分。Args: cr_output: 周期共振输出对象。Returns: float 0.0~1.0, 降级时返回0.5(跨策略Greeks聚合降级默认值)
- **`compute_otm_sync_exposure`** — P1-8修复: 消费ticks_raw.is_otm和future_sync_status，计算OTM期权同步风险敞口
- **`_compute_greeks_usage_score`** — R17-P1-DOC-P1-04修复: 计算Greeks使用率评分。Args: dashboard: Greeks仪表盘字典含portfolio子键。Returns: float 0.0~1.0, 降级时返回0.5(跨策略Greeks聚合降级默认值)
- **`_compute_consecutive_loss_score`** — R17-P1-DOC-P1-04修复: 计算连续亏损评分。Args: consecutive_losses: 连续亏损次数。Returns: float 0.1~1.0, 0次=1.0/1-2次=0.8/3-4次=0.5/5-6次=0.3/7+次=0.1
- **`_compute_alpha_decay_score`** — R17-P1-DOC-P1-04修复: 计算Alpha衰减评分。Args: alpha_ratio: Alpha比率。Returns: float 0.1~1.0, >1.0=1.0/>0.5=0.7/>0.0=0.4/<=0=0.1, 降级时返回0.5
- **`_compute_cross_correlation_score`** — R17-P1-DOC-P1-04修复: 计算跨策略相关性评分。Args: cross_corr: 跨策略相关系数。Returns: float 0.1~1.0, 低相关=1.0/高相关=0.1, 降级时返回0.5
- **`_compute_liquidity_score`** — R17-P1-DOC-P1-04修复: 计算流动性评分。Returns: float 0.0~1.0, 基于买卖价差评估流动性, 降级时返回0.5

### `risk_config_provider.py`

#### class `RiskConfigProvider`

CC-03/AP-02修复: 风控配置参数提供者

从RiskService提取的D类方法（配置/参数管理），
统一管理风控参数的读取和默认值。
构造函数接受risk_service引用（用于访问共享状态）。

- **`_get_max_risk_ratio`** — P1-57修复: 默认值从get_param_default获取
- **`_get_life_estimator`** — 懒加载行情寿命估计器

R4-D-03修复: 懒加载单例防护 —
首次调用返回None时记录WARNING，后续调用不再重复尝试导入。
使用_SENTINEL标记区分「未初始化」和「初始化失败」。
R5-T-04修复: 添加线程安全锁，防止并发初始化。

### `risk_position_bridge.py`

Risk-Position Bridge Interface — Phase 1 architectural refactoring.

Breaks the circular dependency between risk_service.py and position_service.py
by introducing a protocol-based interface layer.

Before: position_service → risk_service (9 direct refs)
        risk_service → position_service (6 lazy imports)
After:  position_service → RiskPositionBridge (protocol)
        risk_service → PositionBridge (protocol)
        Both protocols defined here, no circular import.

#### class `BridgeRiskLevel`

#### class `BridgeRiskResponse`

#### class `BridgePositionLimit`

#### class `RiskPositionBridge`

Interface that position_service uses to communicate with risk_service.

This breaks the direct dependency on RiskService class.
RiskService implements this interface; position_service depends only on this.

#### class `PositionBridge`

Interface that risk_service uses to communicate with position_service.

This breaks the lazy import dependency on PositionService class.
PositionService implements this interface; risk_service depends only on this.

#### class `RiskBridgeAdapter`

Adapter wrapping an existing RiskService instance as a RiskPositionBridge.

#### class `PositionBridgeAdapter`

Adapter wrapping an existing PositionService instance as a PositionBridge.

### `risk_priority_matrix.py`

Phase0增强: RPN风险优先数矩阵 — 定量风险评估
RPN = 概率(1-5) × 影响(1-5) × 可检测性(1-5)
CRITICAL(≥36) > HIGH(≥24) > MEDIUM(≥15) > LOW(<15)

#### class `RiskItem`

#### class `RiskPriorityMatrix`

RPN风险优先数矩阵

10项风险按RPN降序排序，CRITICAL级风险必须先缓解才能进入下一Phase

### `risk_service.py`

risk_service.py - 风控服务

合并来源：08_position.py (PositionManagerMixin) + 10_gate.py (GateLayer3)
合并策略：提取风控检查、限额管理、风险计算等功能

重构目标：
- 旧架构：08_position.py (~600行) + 10_gate.py (~200行风控相关) = ~700行
- 新架构：risk_service.py (~400行)
- 减少：~43%

核心改进：
1. 单一职责：专注于风险控制与限额管理
2. 统一风控接口：整合持仓限额、信号限频、策略状态检查
3. 线程安全：支持并发检查
4. 可配置性：灵活的风控参数配置

作者：CodeArts 代码智能体
版本：v1.0
生成时间：2026-03-16

#### class `RiskLevel`

风险等级

#### class `RiskCheckResult`

风控检查结果

#### class `RiskCheckResponse`

风控检查响应

#### class `RiskMetrics`

风险指标 — 含11维度评分

#### class `RiskService`

风控服务 - 统一的风险控制接口

职责：
1. 持仓限额检查
2. 信号限频控制
3. 策略状态检查
4. 风险指标计算

使用方式：
    service = RiskService(params, position_manager)
    result = service.check_before_trade(signal)

- **`get_dashboard_service`** — 获取RiskDashboardService实例 — 升A路径T1.2: 消除死代码
- **`_validate_and_check`** — P1-42修复: 执行风控校验逻辑，返回结果（不记录不发布）
- **`_publish_risk_event_via_eventbus`** — P1-42修复: 通过EventBus统一发布风控事件，消除独立发布路径
- **`check_regulatory_compliance`** — 合规检查 — R2-3: 统一走_check_service委托路径
- **`check_capital_sufficiency`** — 资金充足性检查 — R2-3: 统一走_check_service委托路径
- **`check_exchange_status`** — 交易所状态检查 — R2-3: 统一走_check_service委托路径

#### class `RiskDashboardService`

风险仪表盘服务 — Greeks聚合、PnL归因、压力测试

独立全局单例，供RiskService和外部仪表盘调用。

- **`aggregate_greeks`** — P1-61修复: Greeks聚合委托到position_greeks.aggregate_greeks_exposure（唯一权威实现）
- **`attribute_pnl`** — PnL归因：按策略/状态/持仓时长分组归因
- **`run_stress_test`** — 压力测试：价格冲击shock_pct下的最坏情景损失

- **`func:_audit_annotation`** — 审计标注装饰器 — 不改变类行为，仅用于H.1.3格式清单审计追踪
- **`func:validate_risk_level_transition`** — [R23-P1-02-FIX] 校验风控级别转移合法性，禁止正常→熔断直接跳跃
- **`func:get_risk_service`** — R13-API-02修复: RiskService单例工厂支持参数更新

首次以params=None初始化后，后续传入真实params会被更新到实例中，
避免风控检查因参数缺失而失效。
- **`func:get_risk_dashboard_service`** — 获取RiskDashboardService全局单例

### `safety_meta_audit.py`

#### class `CircuitBreakerStateStore`

断路器状态持久化服务 — 审计与状态存储

负责：断路器状态的save/load/跨日重置逻辑

- **`save_state`** — 持久化断路器状态到JSON文件
- **`write_compliance_audit`** — 写入合规审批记录到JSONL审计日志

### `safety_meta_circuit.py`

#### class `CircuitBreakerService`

断路器服务 — 从SafetyMetaLayer提取的断路器核心逻辑

负责：断路器触发/评估/重置/状态持久化/冷静期/影子模式/算法熔断

- **`save_state`** — 委托CircuitBreakerStateStore进行状态持久化
- **`load_state`** — 委托CircuitBreakerStateStore进行状态恢复

### `safety_meta_equity.py`

#### class `DrawdownMonitorService`

回撤监控服务 — 从SafetyMetaLayer提取的回撤/权益监控逻辑

负责：权益更新/日回撤检查/单调性检查/负权益紧急处理

### `safety_meta_layer.py`

安全元层模块 — 从risk_circuit_breaker.py拆分
职责: SafetyMetaLayer核心逻辑（日回撤硬停止、断路器、保证金追保）

#### class `SafetyMetaLayer`

### `safety_meta_position.py`

#### class `HardTimeStopAndComplianceService`

硬时间止损+合规+保证金+展期+健康+统计服务

从SafetyMetaLayer提取的非断路器/非回撤逻辑

## strategy

### `__init__.py`

strategy/子系统(P2-S1) — 公共API精确白名单

PA-03 三层物理隔离: __init__.py 仅重导出 Facade 层公共符号。
严格约束: __all__ 长度 ≤ 7
禁止: import *、重导出任何 _ 前缀模块
禁止: 重导出 logger / json / copy 等实现细节

注意: 使用惰性导入（__getattr__）避免循环依赖。
      config/config_params → strategy/strategy_config → strategy/__init__
      如果 __init__ 急切导入 shadow_strategy_facade → shadow_strategy_core → config_params
      会形成循环。惰性导入打破此循环。

Mixin消灭(G2b): StrategyCoreService已退化为纯Facade，零Mixin继承。
      6个Service通过ServiceFactory注入: LifecycleService, KlineDataService,
      TickProcessingService, RecoveryService, CheckpointService, InstrumentService。

- **`func:__getattr__`** — 惰性导入：仅在首次访问时加载，避免循环依赖

### `_box_spring_types.py`

#### class `SpringState`

#### class `BoxRange`

#### class `SpringSignal`

#### class `SpringPosition`

#### class `PendingPullback`

### `_shadow_strategy_core.py`

shadow_strategy_core.py - core lifecycle and param management mixin

Responsibilities:
- ShadowStrategyCoreMixin: init/validate/shutdown/param management/log

Source: split from shadow_strategy_engine.py per doc 12.3.4

#### class `ConfigError`

配置错误 — 升A路径T1.3: 参数真相源缺失或格式错误时抛出

#### class `ShadowStrategyCoreService`

Core lifecycle and param management service (from ShadowStrategyCoreMixin)

- **`_load_absolute_ev_floor`** — R19-UG-01修复: 从config/params.yaml加载ABSOLUTE_EV_FLOOR

升A路径T2.1: 通过ConfigService.get_param统一读取，YAML为唯一参数真相源。
- **`_mask_sensitive_value`** — 对敏感数值进行掩码处理，仅显示首尾部分
- **`validate_dependencies`** — R13-P2-API-11修复: 验证所有必需依赖是否存在
- **`shutdown`** — R10-P0-16修复: 关闭ShadowStrategyEngine，释放线程池和刷盘队列

Args:
    wait: 是否等待未完成的任务
    timeout: 等待超时（秒）
- **`_atexit_shutdown`** — atexit回调：进程退出时自动shutdown线程池，防止资源泄漏
- **`set_shadow_param_override`** — P2-R11-11修复: 设置策略级影子参数覆盖

Args:
    strategy_group: 策略组名（如hft/main/box_extreme等）
    shadow_type: 影子类型（shadow_a/shadow_b）
    overrides: 参数覆盖字典
- **`relock_params`** — P1-R11-19修复: 添加调用方鉴权

### `_shadow_strategy_pnl.py`

shadow_strategy_pnl.py - PnL and evaluation mixin (Facade)

Responsibilities:
- ShadowStrategyPnLMixin: Degradation, health, stats (inherits Metrics from shadow_strategy_pnl_metrics)

Source: split from _shadow_services.py ShadowMetricsService+ShadowDegradationService per doc 12.3.4

#### class `ShadowStrategyPnLService`

PnL and evaluation service (from ShadowStrategyPnLMixin)

- **`set_trades_max_len`** — 设置交易记录上限（运行时可配置）
- **`record_governance_degradation`** — 记录治理引擎触发的降级事件到影子引擎日志。

### `_shadow_strategy_pnl_metrics.py`

shadow_strategy_pnl_metrics.py - PnL Metrics mixin

Responsibilities:
- ShadowStrategyPnLMetricsMixin: Alpha metrics, daily/weekly summary, validation

Source: split from shadow_strategy_pnl.py per doc 12.3.4

#### class `ShadowStrategyPnLMetricsService`

PnL Metrics service (from ShadowStrategyPnLMetricsMixin)

- **`_compute_sharpe`** — P1-47修复: 委托给 infra/resilience_numeric.compute_sharpe_stable

保留原签名以兼容所有调用点，内部委托给权威实现。
use_sample_std=True 保持与原实现(样本方差n-1)一致。
- **`_emit_alpha_decay_alert`** — Alpha衰减时主动推送告警事件到EventBus
- **`validate_shadow_b_stability`** — P1-裂缝15：验证影子B策略夏普的95%置信区间

用n_seeds个不同种子运行影子B，计算夏普的95%CI。
若CI宽度>1.0，则影子B不可用于Alpha扣除，应改用期望收益而非单路径。
- **`alpha_confidence_overlap_test`** — P2-裂缝27：主-影子Sharpe置信区间重叠检验

若主策略与影子策略的Sharpe置信区间重叠比例 > 50%，
则Alpha可能只是抽样误差，标记alpha_unreliable，
资金分配回退到基础权重。
- **`validate_shadow_param_orthogonality`** — P2-裂缝29：影子参数差异度KL散度验证

差异度基于4个参数平均可能不够，参数间交互可能导致联合作用相似。
增加条件：影子策略的绩效分布与主策略绩效分布的KL散度 > 0.5。

### `_shadow_strategy_signal.py`

shadow_strategy_signal.py - signal generation mixin

Responsibilities:
- ShadowStrategySignalMixin: process_shadow_a/b_signal, record_master_trade, etc.

Source: split from _shadow_services.py ShadowPositionService per doc 12.3.4

#### class `ShadowStrategySignalService`

Signal generation service (from ShadowStrategySignalMixin)

- **`process_signals_batch`** — 批量信号处理(优化：单次锁获取处理所有signals)
- **`get_paper_account`** — P0-裂缝11：获取影子策略独立模拟账户状态
- **`update_paper_account`** — P0-裂缝11：更新影子策略模拟账户（不影响主策略）

Args:
    instrument_id: 合约ID
    direction: 'long'/'short'/'close_long'/'close_short'
    quantity: 数量
    price: 成交价格
    commission: 手续费

Returns:
    更新后的paper_account状态
- **`is_shadow_mode`** — P0-裂缝11：确认影子策略处于隔离模式

R13-P0-DEAD-01修复: 不再直接返回True，而是验证隔离性：
1. 影子订单日志中无真实order_id（不经过OrderService）
2. paper_account持仓与主策略不重叠
3. 所有检查通过则返回True，否则返回False并记录隔离性破坏
- **`verify_shadow_isolation`** — P0-裂缝11+R13-DEAD-01：验证影子策略与主策略完全隔离

通过标准：
1. _is_shadow_mode标志为True
2. 影子策略不使用OrderService（检查shadow_order_log无真实order_id）
3. 影子策略使用独立paper_account（检查不与主策略持仓重叠）

### `box_detector.py`

box_detector.py - 箱体检测与极值判断模块

V7次系统2核心组件：箱底/箱顶识别 + 极值子状态

设计原理：
  箱形震荡本质：价格在箱底和箱顶之间反复，方向不延续。
  共振策略在箱体内不追趋势，而是在"转折点"反向操作：
    箱底极值：期货跌到箱底 + 全链看跌共振 → "跌透了" → 反向做多
    箱顶极值：期货涨到箱顶 + 全链看涨共振 → "涨透了" → 反向做空

箱体检测方法：
  1. 价格振幅法：近期高低价差收窄 + ADX低 → 箱体识别
  2. 行权价聚类法：虚值期权行权价在箱体边界处聚集
  3. VWAP带法：VWAP上下轨收窄 → 箱体

极值判断：
  - 价格触及箱底/箱顶
  - 全链期权呈现共振（方向一致性极高）
  - IV处于近期高位（>50分位数）
  - 订单流出现衰竭迹象

调用现有方法：
  - WidthStrengthCache.get_width_strength_summary() → 五态分布
  - TTypeService.compute_decision_score() → 综合决策
  - MicrostructureAnalyzer.get_composite_assessment() → 订单流综合
  - IVCalculator.implied_volatility() → IV计算

新增方法：
  - detect_box() → 箱体识别
  - classify_extreme_state() → 极值子状态判断
  - check_iv_filter() → IV高位过滤
  - check_order_flow_exhaustion() → 订单流衰竭确认

#### class `BoxProfile`

箱体轮廓

#### class `ExtremeState`

极值子状态

#### class `BoxStrategyParams`

箱体策略参数（比趋势策略更苛刻）

#### class `BoxDetector`

箱体检测与极值判断引擎

职责：
1. 基于价格序列识别箱体（箱底/箱顶）
2. 在箱体边界判断极值子状态
3. IV过滤：仅在IV高位时允许箱体买方操作
4. 订单流衰竭确认：大单停手、小单反向
5. 生成箱体交易信号

### `box_spring_detector.py`

box_spring_detector.py - 箱体波动率脉冲策略（弹簧策略）- 检测Mixin

包含箱体识别、IV监控、弹簧信号识别、扫描等检测相关方法。
从BoxSpringStrategy类中提取，作为Mixin使用。

#### class `BoxSpringDetectorService`

### `box_spring_executor.py`

box_spring_executor.py - 箱体波动率脉冲策略（弹簧策略）- 执行Mixin

包含交易执行、平仓、资金计算等执行相关方法。
从BoxSpringStrategy类中提取，作为Mixin使用。

#### class `BoxSpringExecutorService`

- **`compute_equity_based_lots`** — 根据实时资金和权利金自动推算下单手数。
公式: lots = floor(account_equity * max_risk_ratio / (premium_price * 10000))
其中 premium_price * 10000 为单手权利金成本(期权1手=10000张)。
至少1手，受max_position_pct约束。

R13-P2-BIZ-01修复: 计算lots时应减去该合约已有的持仓手数，
避免在已有持仓的情况下重复计算导致超仓。
- **`find_cheaper_strike_same_month`** — 资金不足时，在同月期权中选择权利金较小的行权价品种。
按权利金升序排列同月同类型期权，返回能买1手的最低权利金候选。

### `box_spring_executor_helpers.py`

box_spring_executor_helpers.py - 箱体弹簧策略执行辅助方法 (从box_spring_executor.py拆分)

包含:
  - _compute_hedge_ratio: delta中性对冲比率计算
  - _check_cross_strategy_risk: 跨策略风控检查
  - _record_spring_trade: 弹簧交易记录
  - _find_straddle_pair: 跨式组合配对查找

- **`func:_compute_hedge_ratio`** — 计算delta中性对冲比率

使用greeks_calculator获取当前持仓的net_delta，
计算需要多少期货手数来对冲至delta中性。

Returns:
    float: 对冲比率（需要买入/卖出的期货手数，正=买入，负=卖出）

### `box_spring_scanner.py`

box_spring_scanner.py - 箱体弹簧策略扫描Mixin (从box_spring_detector.py拆分)

包含:
  - scan_springs: 从全量期权中寻找弹簧机会
  - _scan_from_sort_buckets: 从sort_buckets扫描
  - _scan_from_option_info: 从option_info扫描
  - _evaluate_candidate: 评估单个候选

### `box_spring_strategy_impl.py`

box_spring_strategy.py - 箱体波动率脉冲策略（弹簧策略）

策略本质：交易波动率的脉冲，而非价格的方向
利润来源：波动率从极低回归正常 + Gamma在行权价附近的非线性收益

识别条件（弹簧被压紧）：
1. 箱体结构已识别（价格在箱顶箱底之间震荡）
2. 近月期权（2-5天到期），权利金极便宜
3. IV百分位处于近期极低水平（下分位数）
4. 期货价格接近该期权行权价（Gamma最敏感位置）

入场信号（箱内脉冲）：
- 弹簧压紧后，任何波动率回归迹象即可入场
- 订单流出现异动，或全链期权从死寂状态略有反应

平仓纪律（铁律）：
- 止盈：弹簧松开即走，盈亏比达1.5:1或2:1时立刻平仓
- 止损：接受归零（成本极低，占总资金0.5%-1.5%）
- 铁律：绝不平移为趋势策略

期望值模型：
- 胜率 ~20%，盈亏比 ~5:1
- 期望值 = 20%*5 - 80%*1 = +0.2 > 0（正期望）

作者：AI代码助手
版本：v1.0
日期：2026-05-10

#### class `BoxSpringStrategy`

### `checkpoint_service.py`

checkpoint_service.py - CheckpointService (G2b Mixin消除)
从StrategyCheckpointMixin提取的独立检查点/状态同步服务，使用组合替代继承

职责：
- 保存检查点 (save_checkpoint)
- 系统事件检查点回调 (on_system_event_checkpoint)
- 验证服务依赖 (validate_service_dependencies)
- 状态同步到存储 (sync_state_to_store)

#### class `CheckpointService`

独立检查点/状态同步服务 — 通过注入回调访问策略状态，不依赖Mixin继承

- **`save_checkpoint`** — DFG-P1-13修复: 保存当前策略状态到检查点文件

Returns:
    bool: 保存是否成功
- **`on_system_event_checkpoint`** — DFG-P1-13修复: 消费系统启动事件，触发检查点恢复

当系统启动事件（STARTUP）发布时，自动尝试从检查点恢复策略状态，
解决回测checkpoint数据无恢复消费者的数据流断裂问题。
- **`validate_service_dependencies`** — 验证所有服务依赖的回调已正确注入，防止运行时错误。
- **`sync_state_to_store`** — 将策略状态同步到 state_store

使用注入的 get_attr/has_attr 回调访问策略属性，
而非直接通过 self.* 访问。

### `hft_enhancements.py`

HFT增强引擎 - 七大竞争优势统一入口(薄门面)

实际实现已分散到架构归属模块:
1. 信号时序滤波 → signal_service.py (KalmanFilter1D, EMASignalFilter, SignalTimingFilter)
2. 智能订单拆分 → order_service.py (SmartOrderSplitter, OrderSplitStrategy, SplitOrderResult)
3. 动态追击算法 → strategy_tick_handler.py (DynamicPursuitEngine, PursuitPosition)
4. 微观结构套利 → order_flow_bridge.py (MicrostructureArbitrageDetector, ArbitrageOpportunity)
5. 成交量加权订单流 → order_flow_analyzer.py (VolumeWeightedOrderFlow)
6. 状态转换捕捉 → state_param_manager.py (StateTransitionCapture, StateTransitionEvent, TransitionPoint)
7. 做市商防御 → order_flow_bridge.py (MarketMakerDefenseEngine, OrderDefenseType, DefensiveOrder)

本模块仅提供 HFTEnhancementEngine 统一编排入口。

#### class `HFTEnhancementEngine`

HFT增强引擎 - 七大竞争优势统一入口

将7个增强模块统一管理, 提供一站式API。
各模块实现分散在其架构归属模块中, 本类仅做编排调度。

性能优化:
- 模块开关: 每个模块可独立启用/禁用
- 采样频率: 低优先级模块按采样率执行(如套利每5tick执行1次)
- 最小间隔: 模块执行的最小时间间隔(秒)
- 条件前置: 不满足前置条件直接跳过

### `instrument_manager.py`

#### class `InstrumentManager`

### `instrument_service.py`

instrument_service.py — InstrumentService
独立品种服务，替代 _InstrumentHelperMixin，以组合替代继承。

设计原则：
- 零self状态写入（除通过参数传递）
- 可独立测试
- 不依赖 StrategyCoreService 的任何实例属性

#### class `InstrumentService`

独立品种服务 - 替代_InstrumentHelperMixin，以组合替代继承

#### class `_InstrumentHelperMixin`

向后兼容Shim — 已迁移至InstrumentService，请使用InstrumentService替代。

### `kline_data_service.py`

K线数据服务 - 独立组合式服务（替代 HistoricalKlineMixin）

职责：
- 管理历史K线加载的启动、执行和进度跟踪
- 提供合约过滤和提供者解析功能
- 维护历史加载状态和统计信息
- 回测模式下提供时间截止墙（time cutoff wall），防止未来数据泄露

设计原则：
- 组合优于继承：通过构造函数注入 state_store / callback_group，不依赖 Mixin 宿主的 self
- 完全自包含：不使用任何 Mixin 宿主属性
- 显式属性访问：替代 __getattr__ 代理，提供显式 @property

#### class `KlineDataService`

K线数据服务 - 替代 HistoricalKlineMixin 的独立组合式服务。

通过构造函数接收 state_store 和 callback_group，完全自包含，
不依赖 Mixin 宿主的 self 属性。

Args:
    state_store: 状态存储，提供 get/set 方法访问策略全局状态
    callback_group: 回调组，提供 get_registry 方法访问注册的回调
    strategy_id: 策略标识，用于日志
    is_backtest: 是否为回测模式，回测模式下启用时间截止墙

- **`set_time_cutoff`** — 设置时间截止墙。回测模式下，超出此时间的数据请求将触发异常。

Args:
    cutoff_time: 截止时间戳（Unix epoch 秒）
- **`check_time_cutoff`** — 检查请求时间是否超出截止墙。

仅在回测模式且已设置 time_cutoff 时生效。
实盘模式下此方法为空操作。

Args:
    requested_time: 请求的数据时间戳（Unix epoch 秒）

Raises:
    FutureLeakException: 当回测模式下请求时间超出截止墙时
- **`historical_mgr`** — 访问底层 HistoricalDataManager 实例。
- **`load_in_progress`** — 历史K线加载是否正在进行。
- **`kline_result`** — 历史K线加载结果。
- **`kline_progress`** — 历史K线加载进度。
- **`init_historical`** — 初始化历史K线管理器。创建 HistoricalDataManager 并调用 init_historical()。
- **`filter_historical_month_scope`** — 过滤合约月份范围。

Args:
    instrument_ids: 待过滤的合约ID列表

Returns:
    (过滤后列表, 移除数量, 最低年月)
- **`build_historical_instruments`** — 构建历史K线加载的合约列表。
- **`resolve_historical_provider`** — 解析历史K线数据提供者。

Returns:
    (provider, provider_source) 元组
- **`load_historical_klines_once`** — 执行一次历史K线加载。

Args:
    instruments: 合约列表
    provider: 数据提供者
    provider_source: 提供者来源标识
- **`notify_historical_kline_loaded`** — 通知历史K线加载完成。

Args:
    result: 加载结果字典
- **`start_historical_kline_load`** — 启动历史K线加载。

Args:
    blocking: 是否阻塞等待加载完成
- **`shutdown_historical_services`** — 关闭历史K线相关服务，停止加载线程。
- **`reset_historical_state_for_restart`** — 重置历史加载状态，为重启做准备。
- **`emit_historical_kline_diagnostic_on_first_tick`** — 在首个Tick时输出历史K线诊断信息。
- **`check_and_start_historical_load_on_tick`** — 在Tick中检查并启动历史K线加载（兜底逻辑）。
- **`get_historical_kline_summary_lines`** — 获取历史K线摘要信息行。

Returns:
    (kline_summary_line, historical_detail_line) 元组

### `lifecycle_service.py`

lifecycle_service.py — LifecycleService 独立生命周期服务
替代 _LifecycleMixin，以组合替代继承 (G2b Step 4)

设计原则:
- LifecycleService 是独立服务对象，不依赖 Mixin 继承链
- 通过 self._provider 访问策略实例的共享状态 (与 StrategyBusinessLayer 等一致)
- 子模块延迟初始化模式与 _LifecycleMixin 保持一致
- 所有公开方法签名与 _LifecycleMixin 完全兼容，确保平滑迁移

#### class `LifecycleService`

独立生命周期服务 — 替代 _LifecycleMixin，以组合替代继承

与 _LifecycleMixin 的关键区别:
- 不使用 Mixin 继承，而是独立对象
- 通过 self._provider 访问策略实例共享状态 (state_store, _lock, _state 等)
- 可被 StrategyCoreService 以组合方式持有，而非继承

Usage:
    # 在 StrategyCoreService.__init__ 中:
    self._lifecycle_service = LifecycleService(provider=self, strategy_id=self.strategy_id)
    # 调用:
    self._lifecycle_service.transition_to(StrategyState.RUNNING)

- **`__init__`** — Args:
    provider: 策略实例引用（用于访问 _state, _lock, _storage 等共享状态）
              后续可替换为显式依赖注入
    strategy_id: 策略ID
- **`_init_lifecycle_submodules`** — 强制初始化所有子模块

### `recovery_service.py`

recovery_service.py - RecoveryService (G2b Mixin消除)
从StrategyRecoveryMixin提取的独立灾备恢复服务，使用组合替代继承

职责：
- 崩溃后自动化恢复 (auto_recovery_flow)
- 看门狗重启 (watchdog_restart)
- 检查点恢复 (recover_from_checkpoint)

#### class `RecoveryService`

独立灾备恢复服务 — 通过注入回调访问策略状态，不依赖Mixin继承

- **`auto_recovery_flow`** — DR-P1-04修复: 检测是否从崩溃中恢复，自动恢复状态

返回 True 表示正常或恢复成功，False 表示恢复失败（进入SAFE_MODE）。
- **`recover_from_checkpoint`** — DFG-P1-13修复: 从回测检查点恢复策略状态

检查点文件路径: logs/checkpoint_{strategy_id}.json
恢复内容: 策略状态、持仓快照、风控参数、信号冷却等

Returns:
    Dict: 恢复结果 {recovered: bool, fields: [...], errors: [...]}

### `service_factory.py`

service_factory.py - ServiceFactory (G2b Mixin消灭)
策略级服务工厂 — 确保每个策略获得独立的服务实例（PA-03隔离）

职责：
- 创建和缓存策略级服务实例（KlineDataService, TickProcessingService 等）
- 管理命名空间隔离（shadow/normal）
- 提供懒初始化的便捷属性访问

#### class `ServiceFactory`

策略级服务工厂 — 确保每个策略获得独立的服务实例（PA-03隔离）

- **`create_kline_service`** — 创建KlineDataService实例
- **`create_tick_processing_service`** — 创建TickProcessingService实例

Args:
    **fns: 注入回调函数，参见TickProcessingService.__init__参数
- **`create_recovery_service`** — 创建RecoveryService实例

Args:
    **fns: 注入回调函数，参见RecoveryService.__init__参数
- **`create_checkpoint_service`** — 创建CheckpointService实例

Args:
    **fns: 注入回调函数，参见CheckpointService.__init__参数
- **`create_lifecycle_service`** — 创建LifecycleService实例

Args:
    provider: 策略实例引用（用于访问共享状态）
- **`create_instrument_service`** — 创建InstrumentService实例

### `shadow_strategy_facade.py`

shadow_strategy_facade.py - 影子策略子系统唯一公共入口（ARCH-01 Facade模式）

外部消费者只能通过此模块访问影子策略功能。
任何 _shadow_ 前缀的内部模块都不应被外部直接import。

PA-03 三层物理隔离: 第三层（最外层，唯一对外暴露面）

R2-1: 纯组合模式（消除继承），Core通过组合持有+显式委托

公共API（≤7个符号）:
  - ShadowStrategyEngine: 影子策略引擎（Facade，纯组合3个Service）
  - ShadowTradeRecord: 影子策略交易记录
  - AlphaMetrics: Alpha比率监控指标
  - ShadowParamsSnapshot: 影子策略参数快照
  - get_shadow_strategy_engine: 全局单例工厂函数

#### class `ShadowStrategyEngine`

Shadow strategy engine - Facade纯组合（升A路径T2.4: 消灭继承泄漏）

通过组合3个Service实现功能委托，不继承任何Service基类。
Core/Signal/PnL通过__getattr__按优先级委托。

- **`func:get_shadow_strategy_engine`** — 工厂函数：获取影子策略引擎全局单例（唯一构造路径）

线程安全，首次调用时创建实例，后续调用返回同一实例。
单例状态存储在 _shadow_internals 中，本模块不持有可变状态。

### `shadow_strategy_types.py`

#### class `ShadowTradeRecord`

影子策略交易记录

#### class `AlphaMetrics`

Alpha比率监控指标 — 覆盖6策略组×3变体

#### class `ShadowParamsSnapshot`

影子策略参数快照（独立锁定）

### `strategy_2026.py`

Strategy2026 主策略类 — 从strategy_core_service.py拆分

#### class `Strategy2026`

策略 2026 主类 - 直接继承平台基类和 UI 混合类

[P1-2归属] UIMixin被此类继承，属于UI层，在UI重构阶段(Phase 3+)处理
| StrategyCoreService不继承UIMixin(仅继承4核心Mixin: Lifecycle/Instrument/Historical/Tick)

- **`_cancel_all_timers`** — 取消所有已注册的threading.Timer，防止stop后回调仍触发
- **`onTick`** — 平台 Tick 数据回调（必须返回 None）
- **`onOrder`** — 平台订单回调（必须返回 None）
- **`onTrade`** — 平台成交回调（必须返回 None）

#### class `StrategyParams`

策略参数容器 - 替代 type('Params', (), dict)() 匿名类

提供可调试的 __repr__ 和 __dir__，方便排查参数问题。
从strategy_core_service.py迁移至此。

### `strategy_business_layer.py`

StrategyCoreService 业务层 — 从strategy_core_service.py拆分
职责: 交易执行、订单/持仓服务惰性初始化、生态系统互斥、影子策略推送

#### class `StrategyBusinessLayer`

- **`ensure_snapshot_collector`** — 初始化市场快照采集器 — 覆盖18策略(6主策略×3变体)
- **`get_snapshot_collector`** — 获取市场快照采集器（惰性初始化）
- **`_feed_shadow_engine`** — V7新增：将交易信号批量推送到影子策略监控引擎(优化：单次锁获取)

### `strategy_config.py`

策略配置模块 — 从config_params.py拆分
职责: 策略模式常量、策略参数表(策略相关部分)、策略参数缓存管理

- **`func:subscribe_param_changes`** — [FR-P1-10-FIX] 参数变更事件订阅 — P1-05修复: 统一走EventBus单通道
- **`func:_notify_param_change`** — [FR-P1-10-FIX] 参数变更事件发布通知 — P1-05修复: 统一走EventBus单通道
- **`func:record_pipeline_latency`** — [FR-P1-12-FIX] 数据管道延迟监控
- **`func:mark_computation_timestamp`** — [FR-P1-13-FIX] 计算结果时间戳标记
- **`func:check_indicator_freshness`** — [FR-P1-14-FIX] 指标计算新鲜度校验
- **`func:record_indicator_value`** — [FR-P1-14-FIX] 记录指标值和时间戳
- **`func:check_risk_data_freshness`** — [FR-P1-15-FIX] 风控数据新鲜度校验
- **`func:update_risk_data_timestamp`** — [FR-P1-15-FIX] 更新风控数据时间戳
- **`func:get_param_age_summary`** — 返回参数年龄监控汇总
- **`func:check_multi_level_cache_consistency`** — [FR-P1-11-FIX] 多级缓存一致性校验

### `strategy_config_layer.py`

StrategyCoreService 配置层 — 从strategy_core_service.py拆分
职责: 配置初始化、参数解析、依赖检查、优雅关机协调

#### class `StrategyConfigLayer`

- **`init_state`** — 状态初始化 — 从StrategyCoreService._init_state迁移
- **`init_locks`** — 锁与Manager初始化 — 从StrategyCoreService._init_locks迁移
- **`init_services`** — 服务初始化 — 从StrategyCoreService._init_services迁移
- **`check_startup_dependencies`** — R17-P1-CFG-P1-05: 启动时全局依赖检查
- **`coordinated_shutdown`** — R17-P1-CFG-P1-04: 统一优雅关机协调器

### `strategy_core_service.py`

策略核心服务 - 纯Facade (G2b Mixin消灭)
所有逻辑委托到6个独立服务 + 3个Layer子模块，零Mixin继承。服务通过ServiceFactory注入，确保策略级隔离(PA-03)。

#### class `StrategyCoreService`

纯Facade: 零Mixin继承，所有逻辑委托到服务实例

### `strategy_core_service_builder.py`

Phase1-Sprint3: StrategyCoreServiceBuilder — Builder模式拆解__init__(339行)
每个build步骤≤50行，渐进式构建StrategyCoreService
修复: build()不再递归创建新实例，改为apply_to()注入到已有实例

#### class `StrategyCoreServiceBuilder`

渐进式构建StrategyCoreService

原StrategyCoreService.__init__ 339行(圈复杂度>40)拆分为:
- build_state_store: 状态存储初始化
- build_managers: 管理器初始化(合约/持仓/风控/诊断)
- build_thread_pools: 线程池初始化
- build_platform_apis: 平台API绑定
- build_monitoring: 监控/健康检查初始化
- apply_to: 将构建结果注入到已有StrategyCoreService实例(非递归)

- **`apply_to`** — 将构建结果注入到已有StrategyCoreService实例(非递归)
- **`build`** — 创建新StrategyCoreService实例并注入构建结果(非递归: event_bus直传)

### `strategy_ecosystem.py`

strategy_ecosystem.py - 策略生态系统（re-export门面）

原3080行实现已拆分为 strategy_ecosystem/ 包目录下的子模块。
本文件保留为re-export门面，确保所有 `from ali2026v3_trading.strategy.strategy_ecosystem import ...` 导入路径仍然有效。

拆分结构：
  strategy_ecosystem/
  ├── __init__.py              # 包入口re-export
  ├── _models.py               # 数据模型+枚举+常量+装饰器
  ├── _capital_routing.py      # 资金路由Mixin
  ├── _lifecycle.py            # 策略生命周期Mixin
  ├── _trading_ev.py           # 交易记录与EV/PLR Mixin
  ├── _monitoring.py           # 持仓聚合与健康监控Mixin
  ├── _governance.py           # 治理引擎与灰度部署Mixin
  ├── _ops.py                  # 并行运行与运维Mixin
  ├── _core.py                 # StrategyEcosystem主类(组合所有Mixin)
  └── _factory.py              # 工厂函数+模块级单例+性能缓存

### `strategy_historical.py`

历史K线加载Mixin模块

[ACTIVE] 此模块通过 HistoricalKlineMixin 被 StrategyCoreService 继承使用，是当前主链路的组成部分。
    StrategyCoreService(HistoricalKlineMixin, TickHandlerMixin) 是唯一权威链路。

职责：
- 管理历史K线加载的启动、执行和进度跟踪
- 提供合约过滤和提供者解析功能
- 维护历史加载状态和统计信息

设计原则：
- 单一职责：仅处理历史K线相关逻辑
- 无状态依赖：通过self访问所需属性
- 接口稳定：保持与StrategyCoreService的兼容性

#### class `HistoricalKlineMixin`

历史K线加载Mixin

提供完整的历史K线加载功能，包括：
- 合约范围过滤
- 数据提供者解析
- 异步加载管理
- 进度跟踪和诊断

- **`_init_historical_kline_mixin`** — 初始化历史K线Mixin的状态

注意：此方法应在StrategyCoreService.__init__中调用
- **`_filter_historical_month_scope`** — 历史加载仅保留不早于最小年月门槛的合约，降低无效请求。

Args:
    instrument_ids: 待过滤的合约ID列表
    
Returns:
    Tuple[List[str], int, str]: (过滤后的合约列表, 移除数量, 最小年月)
- **`_build_historical_instruments`** — 构建历史K线合约列表

Returns:
    List[str]: 无交易所前缀的合约代码列表（与on_init预注册格式一致）
- **`_resolve_historical_provider`** — 解析历史数据提供者（6级降级链路，按优先级逐级尝试）

降级链路（优先级从高到低）:
  Level 1: params.market_center — 策略参数显式注入
  Level 2: strategy.market_center / strategy.get_kline_data — 策略实例提供
  Level 3: _runtime_market_center — 运行时缓存的市场中心
  Level 4: _runtime_strategy_host提取 — 从宿主策略提取市场中心或get_kline_data
  Level 5: self.get_kline方法包装 — 自身方法包装为Provider
  Level 6: _get_fallback_market_center() — 最终降级方案

Returns:
    tuple: (provider, provider_source) 提供者和来源标识（'params'/'strategy'/'runtime'/'runtime_host'/'get_kline'/'fallback'/'unavailable'）
- **`_load_historical_klines_once`** — 加载历史K线（一次性执行）

Args:
    instruments: 合约列表（带交易所前缀）
    provider: 数据提供者
    provider_source: 提供者来源标识
- **`_notify_historical_kline_loaded`** — 历史K线加载完成通知

发布 HistoricalKlineLoaded 事件，通知策略核心及外部系统历史数据已就绪。
使历史K线加载线程在完成后及时通知，避免策略核心只能在tick回调中被动检查。
- **`_start_historical_kline_load`** — 启动历史K线加载

检查配置和条件，决定是否启动加载任务。
支持异步和阻塞两种模式。

Args:
    blocking: 若为True，启动加载线程后阻塞等待完成。
             用于 on_start 初始化阶段，确保历史数据就绪后才继续。
- **`_reset_historical_state_for_restart`** — 重置历史K线状态以支持重启

在prepare_restart时调用，允许重新触发历史加载。
- **`_emit_historical_kline_diagnostic_on_first_tick`** — 在第一个Tick时输出历史K线加载诊断信息

仅在首次Tick时执行一次，用于验证加载状态。
- **`_check_and_start_historical_load_on_tick`** — ✅ P1修复：在Tick回调中检查并启动历史K线加载（消除无锁快速路径竞态）

所有状态读取都在锁内完成，避免数据可见性问题
- **`_get_historical_kline_summary_lines`** — 获取历史K线汇总行（用于周期性状态输出）

Returns:
    Tuple[str, str]: (K线摘要行, 历史加载详情行)

- **`func:load_historical_klines_with_stop`** — 在历史加载 mixin 内实现可取消加载，避免继续扩张 storage.py。

### `strategy_lifecycle_mixin.py`

strategy_lifecycle_mixin.py — _LifecycleMixin 向后兼容 Shim (G2b Step 4)

原始 _LifecycleMixin 已迁移至 lifecycle_service.py 的 LifecycleService 类。
本文件保留 _LifecycleMixin 作为向后兼容 shim，所有调用委托到 LifecycleService 实例。

迁移路径:
  旧: class Foo(_LifecycleMixin): ...
  新: class Foo: self._lifecycle = LifecycleService(provider=self)

#### class `_LifecycleMixin`

向后兼容 Shim — 所有方法委托到内部 LifecycleService 实例

.. deprecated::
    请直接使用 LifecycleService(provider=self) 组合模式。
    _LifecycleMixin 将在未来版本移除。

- **`_lifecycle_service`** — 延迟创建 LifecycleService 实例 (shim 核心)

- **`func:_lazy`** — 保留 _lazy 装饰器以兼容外部可能的引用

### `strategy_monitoring_layer.py`

StrategyCoreService 监控层 — 从strategy_core_service.py拆分
职责: 健康检查、紧急停止、日回撤硬停止、诊断任务、回归测试

#### class `StrategyMonitoringLayer`

- **`check_position_risk`** — 持仓风控检查 — 从StrategyCoreService.check_position_risk迁移
- **`emergency_stop`** — OPS-03修复: 紧急一键停止 — 暂停策略 + 撤销挂单 + 平所有仓位 + 通知

操作步骤：
1. 暂停策略（停止接收新行情和新交易）
2. 撤销所有未成交挂单
3. 市价平所有持仓
4. 触发通知（告警回调）
5. 记录审计日志

Args:
    caller_id: 操作人标识
    reason: 紧急停止原因

Returns:
    dict: {paused, cancelled_orders, close_result, errors, timestamp}
- **`run_regression_check`** — UPG-P1-13修复: 运行策略逻辑回归测试

使用shared_utils.StrategyRegressionTest框架，验证策略逻辑变更后
关键场景的输出仍然符合预期。

Args:
    test_cases: 自定义测试用例列表，每项格式:
        {
            'name': str,           # 测试用例名称
            'input': Any,          # 输入数据
            'expected': Any,       # 期望输出
            'tolerance': float,    # 浮点数容差(可选)
            'comparator': Callable # 自定义比较函数(可选)
        }
        如果为None，使用内置默认测试用例。
    test_func: 被测试的策略逻辑函数
        如果为None，使用默认的信号生成测试函数。

Returns:
    Dict: 回归测试报告 {
        passed: int,
        failed: int,
        total: int,
        details: list,
        test_name: str,
    }

### `strategy_scheduler.py`

strategy_scheduler.py - 策略定时任务管理

职责：
1. APScheduler初始化和管理
2. 定时任务注册（期权诊断、缓存刷写）
3. 调度器生命周期管理

P1-05: 事件/回调统一到EventBus
==============================
APScheduler回调与EventBus事件是两套并行机制。
在关键job回调中添加EventBus事件发布（不替换原有回调，仅补充）。
后续应逐步将回调消费者迁移到EventBus订阅模式。

已添加EventBus发布的job:
- option_trading_cycle -> scheduler.trading_cycle
- position_risk_check -> scheduler.risk_check
- check_pending_orders -> scheduler.pending_order_check

#### class `StrategyScheduler`

策略调度器管理器

封装APScheduler的初始化和定时任务注册逻辑，
从StrategyCoreService中解耦出来。

职责边界：APScheduler策略定时任务调度，与infra/SchedulerService(通用定时任务)分工。
本类负责策略业务调度(交易循环/持仓风控/期权诊断/缓存刷写)，SchedulerService负责通用任务调度(超时/重试/优先级)。

- **`scheduler`** — 获取调度器实例
- **`bind_state_checker`** — 绑定策略状态检查器，用于暂停/停止时短路后台定时任务。
- **`_can_run_jobs`** — 统一判定当前是否允许执行策略相关定时任务。
- **`_publish_scheduler_event`** — 在关键job回调中发布EventBus事件，补充APScheduler回调机制。
TODO(P1-05): 后续应逐步将回调消费者迁移到EventBus订阅模式。
- **`initialize`** — 初始化调度器（APScheduler为必需依赖，带有限重试）
- **`shutdown`** — 停止调度器

✅ P0-2: 两阶段停止 - 先冻结新任务，再等待归零，最后shutdown  # R17-P2-DOC-03

Args:
    wait: True=等待运行中任务完成(可能阻塞); False=立即断开调度器,再限时join
    wait_for_zero: True=等待job归零后再shutdown
    zero_timeout: 等待job归零的超时时间（秒）
- **`pause_scheduler`** — 冻结调度器（不停止运行中任务，但拒绝新任务）

✅ P0-2: Phase 1 of two-phase stop

Returns:
    bool: 是否成功冻结
- **`add_job_with_owner`** — 统一的 job 注册方法，自动绑定 owner metadata

Args:
    func: 要执行的函数
    trigger: 触发器类型 ('interval', 'cron', 'date')
    job_id: job ID
    strategy_id: 策略ID（owner标识）
    run_id: 运行ID（用于追踪）
    owner_scope: owner范围 ('strategy', 'global', 'shared')
    **trigger_args: 触发器参数 (seconds, minutes, hour等)
- **`_try_delegate_to_scheduler_service`** — P1-08/P1-43修复: 尝试委托到SchedulerService统一调度引擎。返回True表示委托成功。
- **`remove_jobs_by_owner`** — 根据 owner 移除所有相关 job

Args:
    strategy_id: 策略ID

Returns:
    移除的 job 数量
- **`get_jobs_by_owner`** — 查询指定 owner 的所有 job

Args:
    strategy_id: 策略ID
    
Returns:
    job 信息列表
- **`verify_jobs_removed`** — 验证指定 owner 的所有 job 是否已移除

Args:
    strategy_id: 策略ID
    
Returns:
    True=已全部移除, False=仍有残留
- **`register_trading_jobs`** — 注册交易定时任务（封装 P0-3 owner metadata）

Args:
    strategy_id: 策略ID
    run_id: 运行ID
    execute_option_trading_cycle: 期权交易周期函数
    check_position_risk: 持仓风控函数
    order_service: 订单服务对象
- **`stop_strategy_jobs`** — 停止策略的所有 job（封装 P0-3 owner 移除 + 验证）

Args:
    strategy_id: 策略ID
    
Returns:
    移除的 job 数量
- **`register_option_diagnosis_task`** — 注册期权5种状态诊断定时任务

每3分钟输出一次期权5种状态的统计信息，帮助监控期权同步情况。

Args:
    t_type_service: TTypeService实例
- **`register_cache_flush_task`** — 注册缓存刷写定时任务

收市时段将 RealTimeCache 内存数据批量刷写到 DuckDB ticks_raw，
同时截断WAL文件。刷写后 DuckDB 数据可供历史查询使用。

刷写窗口：
- 中午休市时段：12:00 - 12:50
- 下午收盘后：15:30 - 20:00

Args:
    data_service: DataService实例
- **`register_14_contracts_diagnosis_task`** — 注册重点合约12环节诊断定时任务

每30秒输出一次重点监控合约的12环节诊断状态，
使用汇总输出模式避免日志被CRITICAL告警淹没。

Args:
    storage: Storage实例
    query_service: QueryService实例

### `strategy_tick_handler.py`

Tick数据处理模块 (G2b Mixin Elimination - 完成)

TickHandlerMixin 已删除，所有逻辑委托到 TickProcessingService。
此模块保留:
- TickHandlerMixin: 向后兼容别名 = TickProcessingService
- DynamicPursuitEngine: 独立类
- PyramidAddPositionEngine: 独立类
- PursuitPosition: 数据类
- check_hard_time_stop_for_position(): 独立函数
- check_hmm_dwell_anomaly(): 独立函数

#### class `PursuitPosition`

#### class `DynamicPursuitEngine`

#### class `PyramidAddPositionEngine`

金字塔加仓引擎：信号增强时逐级递减加仓

原理：每次加仓量为前次的pyramid_ratio倍（如0.5），
形成金字塔结构——底部仓位大、顶部仓位小。
ATR自适应：加仓量与当前ATR反相关，高波动时减量。

- **`func:check_hard_time_stop_for_position`** — 实盘两阶段硬时间止损检查入口，调用SafetyMetaLayer.check_position_hard_time_stop
- **`func:check_hmm_dwell_anomaly`** — R23-SM-09-FIX: HMM状态驻留时间异常检测

检测两种异常：
1. 驻留时间过长(>_hmm_state_dwell_max_sec) — 状态卡死
2. 切换频率过高(>_hmm_state_max_switches_per_window per _hmm_state_switch_window_sec) — 震荡

### `tick_aggregator_service.py`

Tick聚合处理模块 - 从TickProcessingService提取的核心处理函数

采用"模块级函数+委托"模式，将TickProcessingService中的大型方法提取为
模块级函数，通过svc参数接收self引用，避免循环import。

提取的方法：
- process_tick_core: _process_tick的核心逻辑
- process_tick_unified_path: _process_tick_unified_path的逻辑
- is_option_instrument: _is_option_instrument的逻辑（无self依赖）
- check_hard_time_stop_live: _check_hard_time_stop_live的逻辑
- dispatch_hft_tick: _dispatch_hft_tick的HFT分发逻辑
- execute_pursuit_exit: _execute_pursuit_exit的逻辑
- execute_pursuit_entry: _execute_pursuit_entry的逻辑
- execute_pursuit_add: _execute_pursuit_add的逻辑
- handle_arbitrage_signal: _handle_arbitrage_signal的逻辑
- handle_transition_signal: _handle_transition_signal的逻辑
- handle_smart_money_signal: _handle_smart_money_signal的逻辑
- handle_filtered_signal: _handle_filtered_signal的逻辑

- **`func:check_hard_time_stop_live`** — 每tick检查持仓的两阶段硬时间止损(实盘主链路入口)
- **`func:process_tick_unified_path`** — 统一实盘/回测Tick处理主路径
- **`func:process_tick_core`** — Tick数据处理核心逻辑（从TickProcessingService._process_tick提取）

### `tick_dispatch_service.py`

Tick分发服务模块 - 从TickProcessingService提取的分发+统计+缓冲函数

采用"模块级函数+委托"模式，将TickProcessingService中的分发/统计/缓冲方法
提取为模块级函数，通过svc参数接收self引用，避免循环import。

提取的方法：
- tick_dispatch_layer: _tick_dispatch_layer的分发+统计逻辑
- dispatch_tick: _dispatch_tick的正常模式tick分发逻辑
- dispatch_tick_degraded: _dispatch_tick_degraded的降级模式tick分发逻辑
- output_periodic_summary: _output_periodic_summary的周期性状态输出逻辑
- flush_tick_buffer: _flush_tick_buffer的分片缓冲刷新逻辑
- save_tick_snapshot: _save_tick_snapshot的快照保存逻辑
- atexit_flush: _atexit_flush的atexit清理逻辑

- **`func:_get_dispatch_lock`** — 获取per-instrument分发锁
- **`func:tick_preflight_check`** — Tick预处理检查（去重/时间戳/年龄/序列号/背压），返回True表示通过

### `tick_event_types.py`

Tick事件类型定义模块

从 tick_processing_service.py 拆分出的 MarketEvent 抽象层，
定义实盘 Tick 和回测 Bar 事件的统一事件类型。

P1-31修复: TickEvent 统一使用 infra/event_bus_events.py 的权威定义，
此处通过 re-export 保持向后兼容。

#### class `MarketEvent`

市场事件基类

统一实盘 Tick 和回测 Bar 的事件抽象，
使 on_market_event(event) 成为统一入口。

#### class `BarCompletedEvent`

K线完成事件（回测模式或 Tick 聚合产生）

回测模式下由 K 线合成器注入，替代原始 tick 流。

### `tick_hft_dispatch.py`

HFT分发模块 - 从tick_aggregator_service提取的HFT相关函数

采用"模块级函数+委托"模式，将HFT分发和信号处理函数提取为
模块级函数，通过svc参数接收self引用。

提取的函数：
- dispatch_hft_tick: HFT引擎tick分发
- execute_pursuit_exit: 追踪退出执行
- execute_pursuit_entry: 追踪入场执行
- execute_pursuit_add: 追踪加仓执行
- handle_arbitrage_signal: 套利信号处理
- handle_transition_signal: 状态转换信号处理
- handle_smart_money_signal: 聪明钱信号处理
- handle_filtered_signal: 过滤信号处理

### `tick_processing_service.py`

Tick处理服务模块 - 组合替代继承 (G2b Mixin Elimination Step 2)

将 TickHandlerMixin 提取为独立的 TickProcessingService 类，
通过显式依赖注入替代隐式 self 共享。

职责：
- 处理Tick数据回调（on_tick / on_market_event）
- MarketEvent 抽象：统一实盘 Tick 和回测 Bar 事件
- Tick数据统计和汇总
- 周期性状态输出
- Tick数据持久化和K线合成

设计原则：
- 组合优于继承：通过构造函数注入依赖，不依赖 Mixin 宿主的 self
- 显式依赖：state_store、callback_group 通过构造函数传入
- MarketEvent 抽象：on_market_event(event) 统一入口，on_tick(tick) 向后兼容
- 线程安全：保护共享状态

#### class `TickProcessingService`

Tick处理服务 - 独立组合类

替代 TickHandlerMixin，通过构造函数注入所有依赖，
不再隐式访问 Mixin 宿主的 self 属性。

使用方式:
    svc = TickProcessingService(
        state_store=store,
        callback_group=cb_group,
        strategy_id='strat_001',
        is_backtest=False,
        ensure_order_service_fn=provider._ensure_order_service,
        ensure_position_service_fn=provider._ensure_position_service,
        ensure_hft_engine_fn=provider._ensure_hft_engine,
    )
    svc.init_tick_handler()
    # 实盘: svc.on_tick(tick)
    # 统一: svc.on_market_event(TickEvent(instrument_id=..., tick_data=tick))

- **`__init__`** — 初始化 TickProcessingService

Args:
    state_store: StateStore 实例，用于访问共享状态
    callback_group: CallbackGroup 实例，用于注册回调
    strategy_id: 策略ID
    is_backtest: 是否回测模式
    ensure_order_service_fn: 确保 OrderService 初始化的回调
    ensure_position_service_fn: 确保 PositionService 初始化的回调
    ensure_hft_engine_fn: 确保 HFT Engine 初始化的回调
- **`init_tick_handler`** — 初始化Tick处理服务的状态

替代原 TickHandlerMixin._init_tick_handler_mixin()。
必须在构造后显式调用。
- **`stats`** — 获取统计信息（从 StateStore 读取）
- **`on_market_event`** — 统一市场事件入口

替代 on_tick(tick)，支持 TickEvent 和 BarCompletedEvent。
实盘和回测路径在事件层统一。

Args:
    event: MarketEvent 子类实例（TickEvent 或 BarCompletedEvent）
- **`_on_bar_completed`** — 处理 K 线完成事件（回测模式主路径）

将 BarCompletedEvent 转换为类 tick 对象后走统一处理流程。
- **`on_tick`** — Tick数据回调 - 核心热路径（向后兼容入口）

拆分为探针层→检查层→处理层→统计层
- **`_get_tick_field`** — P1-55修复: 委托到shared_utils.get_tick_field（全项目唯一权威实现）
- **`_normalize_tick`** — P1-55修复: 委托到shared_utils.normalize_tick（全项目唯一权威实现）
- **`_tick_probe_layer`** — 探针层：记录原始合约格式和诊断信息
- **`_check_hard_time_stop_live`** — 每tick检查持仓的两阶段硬时间止损(实盘主链路入口)
- **`_process_tick_unified_path`** — 统一实盘/回测Tick处理主路径
- **`_process_tick`** — 处理Tick数据（核心方法）- 委托到tick_aggregator_service

### `strategy_ecosystem/__init__.py`

strategy_ecosystem - 策略生态系统包（re-export门面）

将拆分后的子模块统一导出，保持与原 strategy_ecosystem.py 完全相同的公开API。

### `strategy_ecosystem/_capital_routing.py`

strategy_ecosystem._capital_routing - 资金路由Mixin

#### class `CapitalRoutingService`

资金路由相关方法

- **`_load_capital_route_params`** — R7-M-12修复: 从配置参数读取capital_route_s1~s6
- **`set_master_substrategy_pause`** — 允许在共享master槽位内独立暂停S1/S2子策略。
- **`normalize_capital_allocation`** — INV-11/INV-CON-02: 归一化资金分配，确保所有策略分配之和为1.0
- **`_precompute_capital_route_ratios`** — 预计算资金路由比例: base_capital * ratio
- **`_precompute_division`** — 预计算浮点除法并缓存

### `strategy_ecosystem/_core.py`

strategy_ecosystem._core - StrategyEcosystem主类(组合所有Mixin)

#### class `StrategyEcosystem`

策略生态系统 — Facade组合（消灭Mixin继承）

六大策略协同：
- correct_trending → 主策略(S2共振)
- incorrect_reversal → 反向策略(S2影子A)
- other → 震荡市策略(S3箱体极值)
- spring → 弹簧策略(S4弹簧)
- arbitrage → 套利策略(S5套利)
- market_making → 做市策略(S6做市)

- **`_on_degrade_frequency`** — 当LiveStrategySelector降频时，通知策略层调整决策频率
- **`notify_degrade_frequency`** — 外部接口：触发降频回调链
- **`check_strategy_bulkhead`** — 检查策略是否超过信号配额（隔板保护），返回True=允许，False=阻断
- **`record_strategy_error`** — 记录策略异常，连续错误超阈值则触发策略断路
- **`reset_strategy_error`** — 策略成功执行后重置异常计数
- **`reset_trade_id_counter_on_new_day`** — 在新交易日开始时调用，重置交易ID计数器

### `strategy_ecosystem/_factory.py`

strategy_ecosystem._factory - 工厂函数+模块级单例+性能缓存

- **`func:validate_ev_cache_time_decay`** — P1-裂缝22：验证EV缓存在市场反转后是否及时衰减
- **`func:_precompute_capital_route_ratios`** — 预计算资金路由比例: base_capital * ratio，结果缓存避免重复乘法
- **`func:_precompute_division`** — 预计算浮点除法并缓存，避免重复除法运算

### `strategy_ecosystem/_governance.py`

strategy_ecosystem._governance - 治理引擎与灰度部署Mixin

#### class `GovernanceService`

治理引擎、灰度部署、策略恢复相关方法

- **`configure_grayscale`** — P2-项17修复: 基于配置的灰度部署
- **`get_grayscale_status`** — P2-项17修复: 获取当前灰度部署状态
- **`reload_param_sets`** — P2-项18修复: 热更新参数集，支持灰度发布
- **`run_governance_checks`** — 手册14节要求: 执行所有治理检测器(E7-E13/WF6-10), 符合任意一条即触发自动降级。
- **`is_governance_degraded`** — 检查治理引擎是否已触发降级

### `strategy_ecosystem/_lifecycle.py`

strategy_ecosystem._lifecycle - 策略生命周期Mixin

#### class `StrategyLifecycleService`

策略生命周期相关方法：状态切换、退市/复活、文档绑定、谱系记录

- **`on_state_switched`** — 状态切换回调：由StateParamManager触发

当SPM检测到三态切换时，自动联动ecosystem切换活跃策略。
R5-T-05修复: 添加递归深度防护，防止回调链无限递归导致栈溢出。
- **`validate_switch_slippage_cascade`** — P1-裂缝19：验证策略切换时平仓+开仓的连续滑点放大
- **`resolve_strategy_id_from_state`** — R13-P0-API-06修复: 将市场状态映射为策略ID

### `strategy_ecosystem/_models.py`

strategy_ecosystem._models - 数据模型、枚举、常量、装饰器

#### class `SlotState`

#### class `StrategySlot`

策略槽位

- **`record_sub_strategy_trade`** — R16-P1-17修复: 记录子策略独立交易统计
- **`get_sub_strategy_ev`** — R16-P1-17修复: 获取子策略独立EV
- **`validate_state_transition`** — INV-07/INV-STA-02: 验证策略状态转换是否合法

#### class `CapitalRoute`

资金路由配置

P1-R10-02修复: 字段修改需通过update_from_dict()方法，
确保所有修改经过ecosystem锁保护。

- **`__setattr__`** — P1-R10-02修复: 冻结后禁止外部直接修改字段
- **`freeze`** — 冻结字段，阻止外部直接修改
- **`unfreeze`** — 解冻字段，仅限ecosystem内部使用
- **`update_from_dict`** — 安全更新字段（由ecosystem锁保护调用）

#### class `EcosystemTradeRecord`

生态系统交易记录

- **`func:_require_interface`** — R5-I-06修复: 运行时接口检查装饰器，替代裸duck typing

对关键方法入口检查self/参数是否实现所需属性/方法，
在调用方传入错误类型时立即抛TypeError而非运行时AttributeError。
- **`func:_require_arg_interface`** — R5-I-06修复: 参数级接口检查装饰器，检查指定参数是否实现所需属性/方法

### `strategy_ecosystem/_monitoring.py`

strategy_ecosystem._monitoring - 持仓聚合与健康监控Mixin

#### class `MonitoringService`

持仓聚合、健康状态、统计查询相关方法

- **`aggregate_position_view`** — DFG-P1-14修复: 聚合所有策略的持仓数据，解决多策略持仓聚合数据流断裂
- **`publish_aggregate_position`** — DFG-P1-14修复: 发布多策略持仓聚合数据到EventBus
- **`validate_position_consistency`** — INV-10/INV-CON-01: 验证策略持仓之和等于PositionService总持仓

### `strategy_ecosystem/_ops.py`

strategy_ecosystem._ops - 并行运行与运维Mixin

#### class `OpsService`

并行运行、回归检查、紧急降级相关方法

- **`enable_parallel_run`** — UPG-P1-03修复: 启用并行运行模式
- **`disable_parallel_run`** — UPG-P1-03修复: 禁用并行运行模式
- **`is_parallel_run_active`** — UPG-P1-03修复: 检查是否处于并行运行模式
- **`regression_check`** — UPG-P1-13修复: 回归检查关键不变量
- **`emergency_degrade`** — OPS-04修复: 紧急降级 — 减少活跃策略数量

### `strategy_ecosystem/_trading_ev.py`

strategy_ecosystem._trading_ev - 交易记录与EV/PLR Mixin

#### class `TradingEVService`

交易记录、EV计算、PLR统计相关方法

- **`_is_same_delta_direction`** — 判断两个策略的方向是否同向（Delta方向一致）

R13-P2-BIZ-02修复: 增加Delta对冲头寸感知。
- **`compute_defense_net_benefit`** — P1-裂缝31：other状态防御净收益量化（含机会成本）

## strategy_judgment

### `__init__.py`

策略评判系统 — 策略微观行为诊断基础设施 v1.5-production

### `_judgment_services.py`

#### class `DeepValidationService`

#### class `VerdictService`

#### class `ScoringHelper`

- **`chicory_eviction_score`** — 三档权重驱逐评分 - P1-09修复: 委托evaluation模块
- **`activity_weighted_score`** — 四维度活跃度加权评分 - P1-09修复: 委托evaluation模块

### `analytics_manager.py`

analytics_manager.py - AnalyticsManager
Phase 2 (CC-02+CC-04): 从_LifecycleMixin提取的分析/预热/统计Manager

职责：
- Analytics初始化（_init_analytics_services, _build_instrument_groups）
- TType预加载（_init_t_type_service_and_preload）
- Analytics任务注册（_register_analytics_jobs）
- Analytics预热（_start_analytics_warmup_async, _ensure_analytics_ready）
- 性能统计记录（record_tick, record_trade, record_signal, record_error, get_stats）

Provider接口：
- provider.storage: 存储服务
- provider._scheduler_manager: 调度器
- provider._lock: 通用锁

#### class `AnalyticsManager`

分析/预热/统计Manager

### `attribution_extensions.py`

Attribution Extensions — R19-P2 策略归因13项修复

P2-归因1:  归因频率与交易频率匹配(分钟频交易→分钟频归因)
P2-归因2:  归因结果可视化输出
P2-归因3:  归因报告格式标准化
P2-归因4:  归因历史存储
P2-归因5:  归因结论置信度评分
P2-归因6:  归因参数灵敏度分析
P2-归因7:  交互项(Interaction)建模
P2-归因8:  归因残差趋势分析
P2-归因9:  归因结果自动预警
P2-归因10: 行业/板块归因
P2-归因11: 因子暴露时序监控
P2-归因12: 策略容量归因
P2-归因13: 归因方法论文档化

#### class `AttributionReport`

标准化归因报告 [P2-归因3]

#### class `AttributionHistoryEntry`

归因历史条目 [P2-归因4]

#### class `AttributionEngine`

增强归因引擎 — 覆盖P2策略归因13项全部修复

- **`generate_report`** — 生成完整归因报告 [P2-归因3]
- **`_compute_confidence`** — 归因置信度评分 [P2-归因5]
- **`_compute_interactions`** — 交互项建模 [P2-归因7]
- **`_analyze_residual_trend`** — 残差趋势分析 [P2-归因8]
- **`_check_alerts`** — 归因自动预警 [P2-归因9]
- **`_compute_sector_breakdown`** — 行业/板块归因 [P2-归因10]
- **`_compute_factor_exposure`** — 因子暴露 [P2-归因11]
- **`_estimate_capacity`** — 策略容量估计 [P2-归因12]
- **`compute_sensitivity`** — 归因参数灵敏度分析 [P2-归因6]
- **`get_history`** — 获取归因历史 [P2-归因4]
- **`format_report_text`** — 格式化归因报告为文本 [P2-归因2/3]

### `backtest_integration_hooks.py`

回测集成钩子 (Backtest Integration Hooks) — v1.4

将策略评判系统与现有回测框架集成，
覆盖18策略(6主策略×3变体: master/shadow_a/shadow_b)完整生命周期。

集成方式：
  1. 每个tick后调用 on_tick()
  2. 信号生成后调用 on_signal_generated()
  3. 开仓后调用 on_order_opened()
  4. 平仓后调用 on_order_closed()
  5. 安全元层触发时调用 on_safety_meta_trigger()
  6. 生态系统切换时调用 on_ecosystem_switch()
  7. 弹簧状态变化时调用 on_spring_state_change()
  8. 周期共振相位变化时调用 on_phase_transition()
  9. 回测结束调用 on_backtest_finish() → 自动诊断+评判+报告

#### class `HookConfig`

#### class `BacktestIntegrationHooks`

回测集成钩子 — 一站式评判系统入口

- **`_simulate_trade_latency`** — R16-P1-PERF-19修复: 回测交易延迟模拟

模拟下单到成交的延迟，消除sharpe乐观偏差：
- 开仓延迟：市场冲击+排队时间
- 平仓延迟：滑点+执行延迟
- **`build_18_strategy_states`** — 构建18策略状态快照列表(6主策略×3变体)

Args:
    per_strategy_overrides: 可选的关键字参数，格式为 {strategy_id: {field: value}}
        例如 build_18_strategy_states(high_freq_master={"signal_strength": 0.8})

### `causal_chain_utils.py`

causal_chain_utils.py — R32 P0修复: 数据因果链追踪/隔离/清理/回滚

解决CC-01~CC-12共12个P0问题:
- CC-01: tick价格NaN/Inf全链路污染防护 (已有入口校验, 增加级联防护)
- CC-02: 全链路关联ID (correlation_id)
- CC-03: 污染检测后级联清理/回滚
- CC-04: 循环依赖检测与环路隔离
- CC-05: 价格错误K线聚合放大防护
- CC-06: 错误信号污染后续信号防护
- CC-07: 风控静默通过防护
- CC-08: 参数池错误参数隔离
- CC-09: 回测参数→生产系统因果链防护
- CC-10: 评判引擎评分错误防护
- CC-11: 下单→持仓→风控因果环隔离
- CC-12: 根因追踪能力

#### class `CausalEvent`

#### class `CausalChainTracker`

#### class `ContaminationGuard`

#### class `CyclicDependencyGuard`

#### class `ParamIsolationGuard`

#### class `TradeRollbackSnapshot`

交易前快照，用于回滚错误交易的所有后续影响

#### class `TradeRollbackManager`

[R28-C023修复] 错误交易回滚管理器

当检测到错误交易（如NaN价格导致的错误开仓）时，
回滚该交易产生的所有后续影响：
1. 撤销持仓（平仓或恢复到交易前状态）
2. 重置风控状态（连亏计数、日回撤等）
3. 标记相关信号/订单为已回滚
4. 记录回滚事件到因果链

- **`capture_pre_trade_snapshot`** — 在交易执行前捕获快照
- **`rollback_trade`** — 回滚指定交易的所有后续影响

Returns:
    True if rollback succeeded, False if snapshot not found or already rolled back
- **`get_rollback_history`** — 获取回滚历史
- **`get_pending_snapshots`** — 获取未回滚的快照ID列表

- **`func:safe_datetime_now`** — ENV-06修复: 统一时区感知的datetime.now()
- **`func:validate_tick_cascade`** — CC-01修复: tick入口级联校验（补充strategy_tick_handler已有校验）

### `jensen_alpha.py`

Jensen's Alpha — Standard Alpha calculation [R19-ATT-02 complement].

Provides industry-standard Jensen's Alpha calculation as an alternative
to the simplified (Sharpe_diff) formula currently used in
shadow_strategy_engine.compute_alpha_metrics().

Jensen's Alpha = Rp - [Rf + Beta * (Rm - Rf)]
where:
  Rp = portfolio return (annualized)
  Rf = risk-free rate
  Beta = Cov(Rp, Rm) / Var(Rm)
  Rm = market benchmark return

#### class `JensenAlphaResult`

Result of Jensen's Alpha calculation.

- **`func:compute_jensen_alpha`** — Compute Jensen's Alpha using CAPM regression.

Args:
    portfolio_returns: Period-by-period portfolio returns
    benchmark_returns: Period-by-period benchmark returns (same length)
    risk_free_rate: Annualized risk-free rate
    annualize_factor: Annualization factor (252 for daily, 60480 for minute)

Returns:
    JensenAlphaResult with alpha, beta, and regression statistics
- **`func:compute_information_ratio`** — Compute Information Ratio = Alpha / Tracking Error.

### `judgment_behavior.py`

Behavior Consistency Judgment Service

#### class `BehaviorJudger`

Behavior consistency judgment service.

- **`judge_cross_instrument_consistency`** — 跨品种一致性检验（全量扫描排序策略专用）

由于本策略使用全量扫描排序，跨品种一致性检验包含4个维度：
1. Tier分布一致性（Tier1占比<20%, Tier4占比>30%）
2. correct_up_pct分布一致性（跨品种CV<0.40）
3. Wilson降权比一致性（跨品种CV<0.30）
4. 月份权重归一化一致性（不同月份数品种间correct_up_pct可比，KS距离<0.2）

与judgment_standards.md 4.6节代码实现映射对齐。
CV定量阈值：CV < 0.40 → passed=True（介于文档0.3"一致"和0.5"过拟合"之间）

### `judgment_calibration.py`

Prediction Calibration Judgment Service

#### class `CalibrationJudger`

Prediction calibration judgment service.

### `judgment_deep_validation.py`

Deep Validation Service — extracted from strategy_judgment_facade.py

Contains _run_deep_validations logic that integrates with statistical_validation,
advanced_validation, l2_optimizer, test_design_suite, and governance_engine.

- **`func:run_deep_validations`** — Returns (warnings, blockers, conditions, validation_degraded)

### `judgment_profitability.py`

Profitability Judgment Service - extracted from strategy_judgment_engine.py

#### class `ProfitabilityJudger`

Profitability judgment service.

- **`load_shadow_jsonl`** — 读取影子策略JSONL日志文件，供评判引擎消费

### `judgment_risk.py`

Risk Compliance Judgment Service

#### class `RiskJudger`

Risk compliance judgment service.

- **`judge_realtime_risk_score`** — 第13维度：11维度实时风险评分（桥接生产风控与事后评判）

输入格式: {
    "composite_score": 0.75,          # 11维度加权综合评分
    "d1_state_strength": 0.8,         # 五态状态强度
    "d2_order_flow": 0.75,            # 订单流一致性
    "d3_life_expectancy": 1.0,        # 行情寿命置信度
    "d4_cycle_resonance": 0.8,        # 周期共振强度
    "d5_phase_quality": 1.0,          # 相位质量
    "d6_greeks_usage": 0.7,           # Greeks使用率
    "d7_consecutive_loss": 1.0,       # 连亏状态
    "d8_asymmetric_drawdown": 0.8,    # 非对称回撤
    "d9_tri_validation": 0.75,        # 三角验证
    "d10_alpha_decay": 0.7,           # Alpha衰减
    "d11_cross_correlation": 0.8,     # 跨策略相关性
}

评分逻辑：
- 有composite_score时直接使用（来自risk_service.py的compute_decision_score）
- 无composite_score时从d1-d11按RiskService.RISK_DIMENSION_DEFAULTS加权计算
- 关键维度低于阈值时触发条件/警告

### `judgment_scoring_helpers.py`

judgment_scoring_helpers — 评判评分辅助函数 (从strategy_judgment_facade.py拆分)

包含:
  - chicory_eviction_score: Chicory淘汰评分
  - activity_weighted_score: 活动加权评分
  - run_ecosystem_integrations: 生态集成(CascadeJudge/E-04~E-13/Alpha-CI/DeepValidation/Sensitivity)

#### class `ComponentFailurePolicy`

组件失败策略

- **`func:_handle_component_failure`** — 处理组件失败，返回 True=继续, False=阻断
- **`func:chicory_eviction_score`** — Chicory淘汰评分 - P1-09修复: 委托evaluation.chicory_eviction.ChicoryEvictionPolicy
- **`func:activity_weighted_score`** — 活动加权评分 - P1-09修复: 委托evaluation.activity_weighted_scorer.ActivityWeightedScorer

### `judgment_types.py`

评判数据类型 (Judgment Types) — 从 strategy_judgment_engine.py 拆分

包含:
  - JudgmentVerdict: 评判结论枚举
  - CapitalScale: 资金规模枚举
  - _JudgmentDimension: 评判维度数据类
  - JudgmentReport: 评判报告数据类
  - 工具函数: _safe_clip_score, _safe_float, _get_contract_multiplier
  - 配置常量: DEFAULT_THRESHOLDS, DEFAULT_WEIGHTS, SCORING_COEFFICIENTS, ...

#### class `JudgmentVerdict`

#### class `CapitalScale`

#### class `_JudgmentDimension`

#### class `JudgmentReport`

- **`func:_safe_clip_score`** — NP-P1-16修复: NaN安全clip，NaN/inf→lo
- **`func:_safe_float`** — NP-P1-20修复: 安全float转换，None/异常→default
- **`func:_get_contract_multiplier`** — R19-CS-01修复: 跨系统合约乘数对齐

从维护的乘数映射表获取合约乘数，与production/risk_service.py对齐。
默认1.0（安全边界：不低估风险）。
- **`func:_build_judgment_summary_lines`** — 用join替代+=拼接，减少多次字符串内存分配开销

### `market_snapshot_collector.py`

市场快照采集器 (Market Snapshot Collector) — 生产就绪版

覆盖18策略(6主策略×3变体: master/shadow_a/shadow_b)的完整方法和状态，
在以下关键时刻捕获完整市场状态快照：
  1. 所有信号发生点（开单策略触发信号时）
  2. 所有开仓、持仓检查、平仓时刻
  3. 周线/月线级别高低点前后5天
  4. 极值区域进入时
  5. 周期共振相位转换时
  6. 弹簧策略状态转换时
  7. 生态系统策略切换时
  8. 安全元层触发时

快照内容（策略池监测的所有信息）：
  - 增强Bar + 周期共振四变量 + 风险曲面调节
  - 六策略各自状态(SignalService/BoxDetector/SpringState/Ecosystem/ArbitrageDetector/MarketMakerDefense)
  - 18策略独立StrategyStateSnapshot(6主策略×master/shadow_a/shadow_b)
  - 五态分布 + 订单流指标 + Greeks
  - HMM状态/后验 + 影子策略Alpha指标(6组×3变体Sharpe)
  - 跨策略Greeks敞口聚合 + 安全元层状态
  - 组合级PnL/回撤/权益曲线

存储：DuckDB列式存储

#### class `SnapshotTrigger`

#### class `StrategyStateSnapshot`

#### class `HFTSpecificState`

#### class `ResonanceSpecificState`

#### class `BoxSpecificState`

#### class `SpringSpecificState`

#### class `ArbitrageSpecificState`

#### class `MarketMakingSpecificState`

#### class `EcosystemState`

#### class `ShadowAlphaState`

影子策略Alpha指标 — 覆盖6策略组×3变体(master/shadow_a/shadow_b)

#### class `SafetyMetaState`

#### class `CrossStrategyGreeks`

#### class `LifeExpectancySnapshot`

行情寿命快照 — 捕获当前HMM状态的寿命期望

#### class `CyclePredictionSnapshot`

周期预测快照 — 捕获多周期趋势和HMM后验

#### class `RiskDimensionScores`

11维度统一风险评分快照 — 三层架构

#### class `MarketSnapshot`

#### class `MarketSnapshotCollector`

市场快照采集器 — 覆盖六大策略完整状态

- **`_auto_fill_life_expectancy`** — 自动从寿命估计器填充行情寿命快照
- **`_auto_fill_cycle_prediction`** — 自动从cr_output推断周期预测快照
- **`_auto_fill_risk_dimensions`** — 从快照已有字段自动填充11维度风险评分
- **`set_life_estimator`** — 注入行情寿命估计器（由task_scheduler或策略引擎在回测/实盘启动时调用）

- **`func:_parse_strategy_id`** — 解析策略ID → (策略类型, 变体名)

### `parameter_pool_adapter.py`

参数池适配器 (Parameter Pool Adapter) — 连接task_scheduler回测结果与策略评判引擎

将task_scheduler.py的回测输出(单个result dict)转化为
StrategyJudgmentEngine.judge()所需的输入参数，
使参数池的参数扫描结果能被策略评判系统自动评判。

用法:
  from ali2026v3_trading.strategy_judgment.parameter_pool_adapter import judge_backtest_result

  result = run_backtest(params, bar_data)
  report = judge_backtest_result("resonance", "rb888", "2025-01~06", result)

- **`func:_extract_realtime_risk_scores`** — 从回测结果中提取11维度实时风险评分数据

回测结果可能包含:
- "realtime_risk_scores": 直接传入的维度评分字典
- "dimension_scores": compute_decision_score()的dimension_scores输出
- "decision_score": 综合评分(composite_score)

### `pnl_attribution.py`

PnL Attribution — R19-ATT-P1 fix: Four-dimensional PnL decomposition.

Provides per-strategy, per-instrument, per-direction (long/short),
per-time-segment (open/midday/close) PnL attribution, replacing the
single global aggregation in task_scheduler._compute_pnl_attribution().

Dimensions:
  X: strategy  (s1_hft, s2_resonance, s3_box, s4_spring, s5_arbitrage, s6_market_making)
  Y: instrument (product code e.g. IF, IO, etc.)
  Z: direction  (LONG, SHORT)
  W: time_segment (OPEN 09:00-09:30, MIDDAY 09:30-14:45, CLOSE 14:45-15:00)

Also provides:
  - Risk attribution by factor (market/style/idiosyncratic)  [ATT-P1-04]
  - Trading cost attribution (explicit/implicit, per-instrument)  [ATT-P1-08, ATT-P1-09]
  - Excess return vs benchmark  [ATT-P1-14, ATT-P1-15]

#### class `Direction`

#### class `TimeSegment`

#### class `TradeRecord`

Single trade record for attribution.

#### class `AttributionResult`

Result of four-dimensional PnL attribution.

#### class `CostAttribution`

Trading cost attribution [ATT-P1-08, ATT-P1-09].

#### class `RiskAttribution`

Risk factor attribution [ATT-P1-04].

#### class `PnLAttributor`

Four-dimensional PnL attribution engine.

- **`compute_attribution`** — Compute full 4D PnL attribution.
- **`compute_cost_attribution`** — Compute explicit/implicit cost attribution per instrument [ATT-P1-08, ATT-P1-09].
- **`compute_excess_return`** — Compute excess return vs benchmark [ATT-P1-14, ATT-P1-15].
- **`compute_residual`** — Compute unexplained residual PnL [ATT-P1-03].

Residual = total_pnl - sum(all attributed strategy PnL).
If attribution is perfect, residual should be 0.
Non-zero residual indicates missing attribution dimensions or data errors.
- **`compute_risk_attribution`** — Compute risk factor decomposition [ATT-P1-04].

Decomposes strategy risk into:
- market_risk_contribution: beta * market_variance
- style_risk_contribution: sum(style_beta_i * style_cov_i)
- idiosyncratic_risk: total_risk - market - style

Uses simplified single-factor model when only market_returns provided.
Uses multi-factor when style_factor_returns also provided.
- **`compute_win_loss_streaks`** — Compute max consecutive win/loss streaks [ATT-P1-13].
- **`compute_attribution_frequency_match`** — [ATT-P2-01] Check if attribution frequency matches trade frequency.
Minute-frequency trades should use minute-frequency attribution.
- **`format_attribution_report`** — [ATT-P2-03] Standardized attribution report format.
- **`compute_attribution_confidence`** — [ATT-P2-05] Attribution confidence score based on residual and coverage.
Score = 1.0 - |residual/total| penalized by low coverage.
- **`compute_interaction_effects`** — [ATT-P2-07] Compute interaction effects between strategy and direction.
Interaction = observed(strat,dir) - marginal(strat) * marginal(dir) / total.
- **`compute_residual_trend`** — [ATT-P2-08] Compute rolling residual trend for attribution diagnostics.
Returns list of rolling residuals to detect systematic attribution drift.
- **`detect_attribution_anomalies`** — [ATT-P2-09] Auto-detect anomalous attribution patterns.
- **`compute_factor_exposure_timeseries`** — [ATT-P2-11] Compute rolling factor exposure time series.
Returns rolling beta to market_returns for regime change detection.
- **`compute_risk_return_map`** — [ATT-P1-05] Map risk contribution to return contribution per factor.

For each risk factor, computes the ratio of return contribution
to risk contribution (return-per-unit-risk).
- **`compute_alpha_decay_statistics`** — [ATT-P1-07] Compute Alpha decay detection with statistical dimension.

Extends EV-only decay detection with:
- rolling mean alpha trend (linear regression slope)
- decay rate (half-life estimation)
- statistical significance (t-test on slope)
- **`compute_drawdown_duration`** — [ATT-P1-11] Compute underwater period (drawdown duration) analysis.

Measures how long equity stays below its running high-water mark.
- **`compute_return_stability`** — [ATT-P1-12] Compute return stability analysis.

Measures consistency of returns using rolling Sharpe and CV.
- **`compute_benchmark_comparison`** — [ATT-P1-14] Compute comparison against market benchmark.

Provides excess return, tracking error, information ratio.
- **`verify_attribution_additivity`** — [ATT-P1-16] Verify multi-strategy attribution additivity.

Tests whether sum(strategy_attributions) = portfolio_attribution.
Non-additivity indicates cross-strategy interaction effects.
- **`compute_portfolio_attribution`** — [ATT-P1-17/18] Compute portfolio-level attribution with cross-strategy correlation.

Decomposes portfolio variance into:
- sum of individual strategy variances
- cross-strategy covariance terms

- **`func:classify_time_segment`** — Classify a timestamp into market time segment.
- **`func:classify_direction`** — Classify trade direction from quantity.

### `resonance_turning_point_marker.py`

周期共振拐点标记 (Resonance Turning Point Marker)

结合周期共振模块(CycleResonanceModule)的四变量输出，
标识预期转折点和实际转折点，形成拐点前-中-后的完整标记链。

核心设计：
  1. 预期转折点：周期共振相位转换时（蓄力→释放、释放→衰竭等）
     标记"市场预期即将发生转折"，但价格尚未确认
  2. 实际转折点：EnhancedBar的极值区域 + 共振方向确认
     标记"价格已实际发生转折"，与预期转折点配对
  3. 转折点分类：
     - 趋势延续型转折：短周期均线走平但长周期仍延续 → 趋势中继
     - 趋势反转型转折：均线收敛+价格偏离度≥2σ → 反转前兆
     - 震荡极值型转折：混沌相位+缠绕排列 → 震荡区间边界
  4. 预期-实际配对：评估共振模块的预测能力（预期→实际的时间差和方向一致性）

#### class `TurningPointType`

#### class `_ResonancePhase`

#### class `TurningPointRecord`

#### class `_ExpectedTP`

#### class `ResonanceTurningPointMarker`

周期共振拐点标记器

在线处理流程：
  1. 每个EnhancedBar完成后调用 process_bar()
  2. 检测周期共振相位转换 → 生成预期转折点
  3. 检测极值区域+共振确认 → 生成实际转折点
  4. 尝试预期-实际配对

- **`process_bar`** — 处理一个增强Bar，检测并记录转折点

Args:
    bar: EnhancedBar（含极值区域和均线位置）
    cr_output: CycleResonanceOutput（含四变量），可选
               若为None则降级为纯价格极值检测

Returns:
    本Bar新产生的转折点列表
- **`_detect_expected_turning_point`** — 相位转换时检测预期转折点

逻辑：
  - 蓄力→释放：预期趋势爆发（延续或反转取决于bias方向）
  - 释放→衰竭：预期趋势即将结束
  - X→混沌：预期市场失序，不可交易
  - 混沌→蓄力：预期市场将进入新周期
- **`_detect_actual_turning_point`** — 极值区域+共振确认 → 实际转折点

分类逻辑：
  - 均线收敛 + 价格偏离度≥2σ + 共振强度>0.3 → 反转
  - 短周期走平 + 长周期延续 → 延续
  - 混沌相位 + 缠绕排列 → 震荡极值
- **`_try_pair_expected`** — 尝试将实际转折点与最近的未确认预期转折点配对

配对条件：
  1. 预期与实际类型方向一致（都预期反转→实际反转）
  2. 时间差在确认窗口内
  3. 方向一致（同为高点或低点）
- **`get_prediction_accuracy`** — 周期共振模块的拐点预测能力评估

Returns:
    包含命中率、平均确认时间、方向一致性等统计

### `statistical_validity_extensions.py`

Statistical Validity Extensions — R19-P2 统计有效性7项修复

P2-统计1: 市场状态结构性变化(regime change)检测
P2-统计2: 回测期间幸存者偏差检测
P2-统计3: 策略复杂度惩罚(AIC/BIC)
P2-统计4: 交易次数与统计可靠性关系
P2-统计5: 极端事件(黑天鹅)影响分析
P2-统计6: 回测结果对佣金/滑点参数的敏感性分析
P2-统计7: 不同市场状态下策略表现稳定性检验

#### class `RegimeChangeResult`

市场状态结构性变化检测结果 [P2-统计1]

#### class `SurvivorshipBiasResult`

幸存者偏差检测结果 [P2-统计2]

#### class `ComplexityPenaltyResult`

策略复杂度惩罚结果 [P2-统计3]

#### class `StatisticalReliabilityResult`

交易次数与统计可靠性 [P2-统计4]

#### class `ExtremeEventResult`

极端事件影响分析结果 [P2-统计5]

#### class `CostSensitivityResult`

佣金/滑点敏感性分析结果 [P2-统计6]

#### class `MarketStabilityResult`

市场状态下策略稳定性检验结果 [P2-统计7]

- **`func:detect_regime_changes`** — 检测收益序列中的结构性变化点 [P2-统计1]

使用滚动Sharpe变化检测regime change。
- **`func:detect_survivorship_bias`** — 检测幸存者偏差 [P2-统计2]
- **`func:apply_complexity_penalty`** — 应用AIC/BIC复杂度惩罚 [P2-统计3]
- **`func:assess_statistical_reliability`** — 评估交易次数与统计可靠性 [P2-统计4]

经验法则: Sharpe标准误差 ≈ sqrt((1 + 0.5*SR^2) / n)
需要至少30笔交易，推荐>100笔。
- **`func:analyze_extreme_events`** — 分析极端事件(黑天鹅)对回测指标的影响 [P2-统计5]
- **`func:analyze_cost_sensitivity`** — 分析回测结果对佣金/滑点参数的敏感性 [P2-统计6]
- **`func:test_market_stability`** — 检验不同市场状态下策略表现稳定性 [P2-统计7]
- **`func:_compute_sharpe_simple`** — 委托 compute_sharpe_stable 计算Sharpe

### `strategy_behavior_diagnosis.py`

策略行为诊断 (Strategy Behavior Diagnosis) — 生产就绪版 v1.1

在局部高低点处对策略进行"微观解剖"，诊断策略核心逻辑是否正确执行。

升级内容（vs v1.0-beta）：
  1. 细粒度4子项拆解：信号衰减一致性/仓位平滑度/Greeks合理性/盈亏路径质量
     评判引擎直接访问子项分数，不再依赖黑箱聚合
  2. 六大策略预设逻辑映射：high_freq/resonance/box/spring/arbitrage/market_making各有独立预期行为
  3. 行为一致性评分直接由4子项加权产生，可审计可追溯
  4. 样本量要求提升：min_samples=30
  5. 预设逻辑规则细化：区分高点/低点、区分趋势延续/反转

关键原则（不变）：
  - 诊断工具 ≠ 优化目标
  - 统计显著性检验
  - 避免事后合理化

#### class `DiagnosisSeverity`

#### class `SubItemScore`

#### class `DimensionDiagnosis`

#### class `BehaviorConsistencyScore`

#### class `DiagnosisReport`

#### class `StrategyBehaviorDiagnosis`

策略行为诊断器 — 生产就绪版 v1.2

细粒度4子项拆解 + 六大策略预设逻辑映射
所有诊断阈值可配置，不再依赖硬编码魔数

### `strategy_judgment_facade.py`

策略评判引擎 (Strategy Judgment Engine) — Facade层 v1.5

Facade模式：仅保留judge()入口方法，将_judge_*方法委托给已提取的Judger类。
子模块:
  - judgment_types: 数据类型与配置常量
  - judgment_profitability: ProfitabilityJudger
  - judgment_behavior: BehaviorJudger
  - judgment_risk: RiskJudger
  - judgment_calibration: CalibrationJudger
  - judgment_deep_validation: run_deep_validations

#### class `StrategyJudgmentEngine`

策略评判引擎 — Facade层 v1.5

12维度 + 阻塞项 + 收紧阈值 + 消除默认通过 + 边际递减 + 权重归一化
委托架构:
  _profitability_judger -> ProfitabilityJudger
  _behavior_judger -> BehaviorJudger
  _risk_judger -> RiskJudger
  _calibration_judger -> CalibrationJudger

### `turning_point_microscope.py`

拐点显微镜 (Turning Point Microscope) — 策略微观行为诊断系统的数据基础

功能：
  1. Tick → 增强Bar（VWAP/高低点时间偏移/高低点成交量/买卖价差）
  2. 在线高低点识别（仅使用历史数据，禁止未来信息泄露）
  3. 多周期均线位置计算（5/10/15/30/60期MA + 标准化偏离度 + 均线排列形态）
  4. 区分"明显高低点"与"极端行情"

关键设计决策：
  - VWAP作为Bar代表价，而非简单收盘价（无偏估计）
  - 高低点不仅记录价格，还记录时间戳和成交量分布
  - 均线计算严格使用截至当前Bar的数据（当前Bar的close只参与下一Bar的均线）
  - 极值识别使用rolling窗口+前瞻确认（N-Bar后确认，非实时最优）

#### class `ExtremeRegion`

#### class `MAAlignment`

#### class `EnhancedBar`

#### class `_TickAccumulator`

#### class `TurningPointMicroscope`

拐点显微镜：Tick→增强Bar + 在线高低点识别 + 均线位置

使用方法：
  1. 每个tick调用 process_tick()
  2. 分钟Bar完成时自动回调，产生EnhancedBar
  3. 每个EnhancedBar包含极值区域判定和均线位置信息

- **`_detect_extreme_region`** — 在线极值识别 — 仅使用当前Bar及之前的数据

算法：
  1. 取最近extreme_window个Bar的high/low
  2. 如果当前价格接近局部高点（前quantile分位）→ NEAR_HIGH
  3. 如果当前价格接近局部低点（后quantile分位）→ NEAR_LOW
  4. 额外检查：如果偏离度超过3σ → EXTREME_HIGH/EXTREME_LOW（极端行情）
- **`_classify_ma_alignment`** — 均线排列形态分类

多头排列：MA5 > MA10 > MA15 > MA30 > MA60
空头排列：MA5 < MA10 < MA15 < MA30 < MA60
收敛：短周期与长周期均线差值缩小
缠绕：均线频繁交叉
- **`flush`** — emit最后一个未完成的Bar
- **`build_from_preprocessed_df`** — 从预处理DataFrame（如preprocess_ticks.py输出）构建拐点显微镜

DataFrame需包含列：timestamp/minute, open, high, low, close, volume, vwap,
                   bid_ask_spread, open_interest, tick_count
