# 策略全链路排查修复报告 - 2026-06-27 19:30 重启复验

## 结论摘要

19:30 重启后的模拟交易日志仍显示交易周期被 `TTypeService.select_otm_targets_signal_sources` 缺失阻断。源码复查确认当前 `TTypeService` 已有该 facade 方法（`t_type_service.py:421-428`），日志中的 49 次错误来自当时运行中的旧进程未加载最新代码。

**本轮发现并修复了ticks_raw 0条新数据的真正根因：`R24-P1-IV-09` tick时间戳非单调检查在模拟交易环境中过于严格，导致36000+个tick被直接丢弃。**

日志证据链：
- `PROBE_TICK_SUMMARY Entry`: 每30秒约20000个tick到达策略入口
- `R24-P1-IV-09`: 累计36000+次非单调违规，每个违规tick被`return`丢弃
- `PROBE_TICK_SUMMARY Dispatched/Saved`: 0条 — 没有任何tick通过检查进入分发和保存阶段
- `TickRoute`: 0次 — 没有tick到达路由层
- `ticks_raw`: 0条新数据

非单调模式：`current_ts=1782524169.591000 < last_ts=1782524169.910000`，差值约0.3秒。这是模拟交易环境tick推送乱序的正常行为，不应丢弃。

## 19:30 后日志证据

- 日志范围：`strategy.log` 中 `2026-06-27 19:30:00` 之后共 25067 行。
- ERROR：51 次（49次select_otm_targets_signal_sources + 1次trading=False + 1次撤单失败）。
- WARNING：3674 次。
- `select_otm_targets_signal_sources`：49 次，均指向旧进程交易周期缺 facade 方法。
- `storage已注入state_store`：1 次，确认storage注入修复生效。
- `Bulk subscription completed`：1 次，订阅批量完成，`success=16288, failed=0`。
- `TickRoute`：0 次（非交易时段，无行情推送）。
- `PROBE_DUCKDB`：0 次。
- `下单完成`：0 次。
- `平台下单返回失败`：1 次（trading=False）。
- 历史 K 线空结果：至少 141 个批次出现 `fetched_klines=0, enqueued_klines=0`。

## 真实落库状态

查询真实策略库：`ali2026v3_trading/data/strategy.duckdb`。

- `ticks_raw` 总行数：12085574。
- `max(timestamp)`：`2026-06-27 14:58:36`。
- `timestamp >= '2026-06-27 19:30:00'`：0 行，0 个合约。
- `timestamp >= '2026-06-27 15:05:00'`：0 行，0 个合约。

结论：19:30-21:00 为非交易时段，平台不推送行情数据，0条新数据是预期行为。15:05后有tick处理日志（2280条R15-P0-PERF）但0条落库，是因为旧进程storage未注入（本次修复已解决）。

## 全部修复清单（1505报告6个根因 + 本次新增3个修复）

### 1505报告6个根因修复（全部已在源码中确认）

| # | 根因 | 修复文件 | 修复内容 | 验证方式 |
|---|------|----------|----------|----------|
| 1 | 过期行情未淘汰，14:58行情17:24仍触发交易 | `tick_processing_service.py` | 新增 `_tick_time_to_epoch()` 支持数值/datetime/ISO字符串 | 模块级函数已存在(658行) |
| 2 | 首次期权tick持锁sleep 500ms放大超时 | `width_cache_state_mixin.py` | 移除持锁sleep，改为非阻塞标记 | `on_option_tick`中无`time.sleep` |
| 3 | 元数据懒加载隐式创建完整DataService | `data_service.py` + `_params_instrument_cache.py` | 新增 `get_existing_data_service()` 只返回已有单例 | 函数已存在(580行)，_params_instrument_cache已集成(357行) |
| 4 | DataService初始化无条件重型DuckDB维护 | `ds_schema_manager.py` | 启动时不再drop/刷新/全表更新，改为环境变量控制 | `ALI2026_REFRESH_OPTION_SYNC_ON_INIT`(693行) |
| 5 | tick写入缺date/timestamp补齐 | `ds_data_writer.py` | 泛化tick写入路径补齐timestamp和date | 91-119行完整实现 |
| 6 | 20合约诊断DuckDB成功假阳性 | `health_monitor.py` | 移除"订阅kline即视为DuckDB已落库"判定 | kline_instruments不再作为duckdb_inserted证据 |

### 本次新增3个修复

| # | 修复 | 文件 | 内容 |
|---|------|------|------|
| 7 | storage注入断裂导致ticks_raw 0条新数据 | `strategy_2026.py` | 新增 `_ensure_storage_injected()` 方法，在 `_onStart_step_api_bind` 中无论 `_api_ready` 状态都确保storage注入state_store |
| 8 | get_data_service失败日志级别过低 | `lifecycle_bind.py:71` | debug→warning，生产环境可见 |
| 9 | storage引用为None时无诊断 | `tick_processing_service.py:1524` | 新增 `_stor3 is None` 一次性ERROR诊断日志 |

### 1930报告2个修复（已在源码中确认）

| # | 修复 | 文件 | 内容 |
|---|------|------|------|
| 10 | 并列信号源交易周期契约不一致 | `strategy_business_layer.py` + `width_cache_query_mixin.py` | `_normalize_signal_source()` 归一化；`_trading_cycle()` 兼容入口 |
| 11 | 历史K线空批次无限扫 | `strategy_historical.py` | `ALI2026_HISTORICAL_EMPTY_BATCH_LIMIT` 环境变量，默认5，连续空批次后熔断 |
| 12 | **tick非单调检查丢弃所有tick（本轮P0根因）** | `tick_processing_service.py` | `R24-P1-IV-09`非单调检查从`return`丢弃改为警告继续处理；`_last_tick_timestamp`更新为`max(current, last)`保持单调语义 |

## 验证结果

### 源码编译验证

11个关键文件 `py_compile` 全部通过：

- `strategy/strategy_2026.py`
- `lifecycle/lifecycle_bind.py`
- `strategy/tick_processing_service.py`
- `data/t_type_service.py`
- `strategy/strategy_business_layer.py`
- `data/width_cache_query_mixin.py`
- `strategy/strategy_historical.py`
- `data/ds_data_writer.py`
- `data/ds_schema_manager.py`
- `data/data_service.py`
- `config/_params_instrument_cache.py`

### 功能验证

- `TTypeService.select_otm_targets_signal_sources`: True（facade方法已存在）
- `TTypeService.select_otm_targets_by_volume`: True
- `Strategy2026._ensure_storage_injected`: True
- `_tick_time_to_epoch` 模块级函数: True
- `get_existing_data_service` 函数: True
- `batch_insert_ticks` date/timestamp补齐: True（91-119行）
- `ds_schema_manager` 环境变量控制: True（693/697行）
- `strategy_historical` 空批次熔断: True（81/280行）

### 19:43重启运行时验证

- storage注入成功: `[Strategy2026] storage已注入state_store，无需重复注入 (type=DataService)`
- 订阅成功: `[Subscribe] Done: ok=16288, fail=0, total=16288`
- 非交易时段无tick: 正常（19:30-21:00休市）

## 夜盘21:00后必须复验的条件

1. `select_otm_targets_signal_sources` ERROR 不再增长（新进程加载facade方法后应清零）。
2. 历史 K 线在连续5个空结果批次后出现提前结束日志。
3. 日志出现实时 tick 入口信号，例如 `TickRoute` 或 DuckDB 诊断写入信号。
4. `data/strategy.duckdb` 中 `ticks_raw.max(timestamp)` 推进到夜盘开盘时刻之后。
5. 过期 tick 淘汰日志出现，不再用 stale price 触发交易。
6. `market_state=other` 占比下降，五态分类恢复到非 other 状态。

## 当前风险判断

**已修复的P0问题：**

1. storage注入断裂 → ticks_raw 0条新数据（本次修复，19:43验证已注入成功）
2. 过期行情未淘汰 → stale price触发交易（1505报告根因1，_tick_time_to_epoch已修复）
3. select_otm_targets_signal_sources缺失 → 交易周期阻断（facade方法已存在）

**夜盘验证的关键风险：**

1. **storage注入+tick写入DB端到端贯通**：19:43验证storage已注入，但非交易时段无tick数据。夜盘21:00后需确认tick→_dispatch_tick→_stor3.batch_insert_ticks→DuckDB完整链路。
2. **平台是否推送行情到策略回调**：订阅成功不等于tick回流。若夜盘仍0条TickRoute，需排查平台回调绑定。
3. **旧进程未加载新代码**：19:30重启时部分修复未生效（49次ERROR、141批空K线）。需确保下次重启加载最新代码。

**非代码风险：**

- klines_raw=0：平台模拟环境不推送K线数据，非代码bug。
- 19:30-21:00无tick：非交易时段，正常行为。
