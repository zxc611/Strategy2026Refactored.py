# 第十一轮独立审计报告 — ali2026v3_trading 全系统全链路核查

**审计标准：**《参数池统一执行方案与使用手册_V7.0_工程落地版_20260510.md》（3180行）
**审计方法：** 全量扫描 + 逐行交叉读取，从手册出发正向核查代码
**审计重点：** 功能全链路通畅性（方法定义 → 参数传递 → 被调用消费 → 功能真正起作用）
**审计范围：** 回测-实盘差异 / 配置漂移与参数一致性 / 时间参数一致性 / 权限与隔离
**审计日期：** 2026-05-23
**审计员：** 第十一轮独立审计员（4路并行审计，去重融合）

---

## 与第十轮报告的关键差异

| 维度 | 第十轮 | 第十一轮 |
|:---|:---|:---|
| 审计视角 | 线程安全/异常传播/内存性能/逐节合规 | 回测-实盘差异/配置漂移/时间参数/权限隔离 |
| 独特焦点 | fail-unsafe链路、DCL缺陷、width_cache泄漏 | 回测时钟分裂、全局单例无隔离、参数名分裂、Bar对齐偏差 |
| P0数量 | 24 | 21 |
| P1数量 | 18 | 27 |
| P2数量 | 6 | 15 |
| **本轮独有发现** | — | **30项** |

---

## 一、P0级问题（21项）— 全链路消费断裂，功能未真正起作用

### A. 回测-实盘差异（8项）

#### P0-R11-01 | 回测L1542 vs 实盘 | 两阶段硬时间止损参数名分裂：回测`stage1_min_minutes` vs 实盘`stage1_minutes`
- **回测代码：** `task_scheduler.py:1537`使用`stage1_min_minutes`，默认90.0
- **实盘代码：** `risk_service.py:2132`使用`stage1_minutes`
- **后果：** 回测和实盘使用不同的参数名，同一个配置文件中`stage1_min_minutes=90`只在回测中生效，实盘使用默认值，两阶段止损阈值在回测和实盘中不一致

#### P0-R11-02 | 回测L1082-1122 vs 实盘width_cache | 五态判定逻辑不等价
- **回测代码：** `task_scheduler.py:1082-1122`使用简化的本地判断（对比correct/incorrect比例）
- **实盘代码：** `width_cache._classify_status()`实现完整的五态分类
- **后果：** 回测验证的策略状态判定与实盘实际状态判定逻辑不同，回测通过但实盘表现迥异

#### P0-R11-03 | 回测无 vs 实盘ProductionQuantSystem | 回测完全缺失HMM状态诊断
- **回测代码：** 无HMM推断逻辑
- **实盘代码：** `ProductionQuantSystem.py`使用`AdaptiveHMM`进行状态诊断
- **后果：** 回测中的策略状态完全不考虑HMM分类，回测与实盘状态机完全不同

#### P0-R11-04 | 回测无 vs 实盘signal_service | 回测缺失`decision_interval_minutes`跳帧机制
- **回测代码：** 所有Bar都执行信号生成和决策，无跳帧
- **实盘代码：** `signal_service.py`中`decision_interval_minutes`控制低频决策
- **后果：** S3/S4在回测中每Bar产生决策（高频噪声），实盘中按间隔跳帧，两者信号频率完全不同

#### P0-R11-05 | 回测L1439 vs 实盘L307 | 回测中仅2/11开仓路径经过`check_before_trade()`
- **回测代码：** `_try_open()`中调用（P0-9修复后），但`box_extreme`（L1998）和`box_spring`（L2159）开仓路径直接构造仓位
- **实盘代码：** `StrategyCoreService.execute_option_trading_cycle()`中调用
- **后果：** 回测中箱体系列策略的Greeks硬约束检查缺失，回测结果比实盘乐观

#### P0-R11-06 | 回测无 vs 实盘L12.1节 | 回测中无健康检查机制
- **回测代码：** 不检查组件健康状态
- **实盘代码：** `get_health_status()`检测CRITICAL时应暂停新开仓
- **后果：** 回测无法模拟组件故障场景

#### P0-R11-07 | 回测vs实盘 | 回测影子策略参数使用同一对象引用
- **回测代码：** `STRATEGY_SHADOW_DEFAULTS["hft"]["shadow_a"]`引用`PARAM_DEFAULTS_SHADOW_A`同一对象
- **实盘代码：** 影子策略从YAML动态加载
- **后果：** 回测中修改影子参数影响全局，实盘中影子参数从YAML独立加载，两者行为不同

#### P0-R11-08 | 回测vs实盘 | 回测Alpha计算为点值无CI，实盘应使用CI宽度差异化处理
- **回测代码：** `alpha = master_sharpe - max(shadow_a_sharpe, shadow_b_sharpe)`直接点值
- **手册要求：** `compute_alpha_confidence_interval()`给出95% CI，按CI宽度差异化处理
- **后果：** 回测中Alpha伪精确，未修正置信区间宽度

---

### B. 配置漂移（4项）

#### P0-R11-09 | 参数`circuit_breaker_trigger_sigma` | 三处分歧：`params_service.py:106`=3.0 vs `task_scheduler.py:4198`=3.0 vs `risk_service.py:1638`硬编码2.5
- **问题：** 参数定义为3.0，但运行时使用硬编码2.5，金发姑娘测试扫描的sigma值全部无效

#### P0-R11-10 | 参数`max_net_delta_pct` | 四处分歧：`risk_service.py`=0.30 vs `config_params.py`=0.30 vs `parameter_attribute_matrix.yaml`=1.0 vs 手册0.10-0.60
- **问题：** YAML中range上限1.0（300%净Delta敞口）远超手册定义的60%上限

#### P0-R11-11 | 参数`max_net_gamma_pct` | 压力测试覆盖至0.50（默认值0.08的6.25倍）
- **问题：** 压力测试允许Gamma敞口远超安全范围

#### P0-R11-12 | 参数`p0_gate_check()` | `optuna_multiobjective_search.py`版本缺少`oos_retention`检查且阈值硬编码
- **问题：** 两套P0铁律检查不一致，若`P0_IRON_RULES`更新optuna版本不同步

---

### C. 时间参数（5项）

#### P0-R11-13 | 时间相关 | `_aggregate_ticks_to_bars()`使用`dt.floor("min")`向下取整对齐
- **代码位置：** `preprocess_ticks.py:416`
- **问题：** 中国期货市场分钟Bar标准对齐为下一分钟起点（00:01:00~00:01:59归入00:02:00 Bar），向下取整导致Bar OHLC与交易所标准偏差一个tick周期

#### P0-R11-14 | 时间相关 | 回测中`_check_safety_meta_layer()`使用`time.time()`而非Bar时间
- **代码位置：** `risk_service.py:1852`
- **问题：** 硬时间止损的`elapsed_min = (time.time() - open_time) / 60.0`中`time.time()`返回系统真实时间，与回测Bar时间混用，硬时间止损判断完全错误

#### P0-R11-15 | 时间相关 | 回测到期日计算使用`pd.Timestamp.now()`而非Bar时间
- **代码位置：** `task_scheduler.py:1185`
- **问题：** 回测中`days_to_expiry = (expiry_dt - pd.Timestamp.now()).days`返回当前系统真实时间，导致2024年合约`days_to_expiry`为负数，到期日滑点倍增模型完全失效

#### P0-R11-16 | 时间相关 | EOD平仓仅检查日盘14:55，未覆盖夜盘品种
- **代码位置：** `task_scheduler.py:1665-1669`
- **问题：** 夜盘品种（原油等）收盘时间02:30，硬编码14:55平仓导致夜盘品种回测中永不平仓

#### P0-R11-17 | 时间相关 | Theta计算除以365日历日，默认不使用252交易日
- **代码位置：** `greeks_calculator.py:314`，`calendar.py:481-482`默认`use_trading_days=False`
- **后果：** Theta在回测中被低估（非交易日被计入衰减但实际无交易）

---

### D. 权限与隔离（4项）

#### P0-R11-18 | 策略间配置隔离 | 全局单例`_param_table_cache`无任何策略标识
- **代码位置：** `config_params.py:32-34, 260-276`
- **问题：** S1-S6共享同一个参数缓存，策略A修改参数后策略B立即生效

#### P0-R11-19 | 安全元层隔离 | `SafetyMetaLayer`全局单例，所有策略共享断路器/回撤监控
- **代码位置：** `risk_service.py:2150-2160, 443-464`
- **问题：** 任一策略触发断路器或日回撤硬停止，所有策略同时被暂停；日回撤计数器不区分策略来源

#### P0-R11-20 | 数据库隔离 | DuckDB全局单例+无并发写保护
- **代码位置：** `storage_core.py`
- **问题：** 并行回测多个worker同时写入同一个DuckDB文件，可能导致数据库损坏

#### P0-R11-21 | 沙箱隔离 | `RestrictedExecLoader`沙箱形同虚设
- **代码位置：** `v7_meta_audit_v2.py:1622-1637`
- **问题：** AST只检查import语句但可绕过；`exec(code, {'__builtins__': __builtins__}, local_ns)`传入完整内置函数，用户代码可执行任意系统命令

---

## 二、P1级问题（27项）— 定义存在但消费路径不完整

### A. 回测-实盘差异（6项）
| 编号 | 回测代码 | 实盘代码 | 问题描述 |
|:---|:---|:---|:---|
| P1-R11-01 | L1447 | ProductionQuantSystem | 滑点模型差异：回测`_compute_dynamic_slippage_bps()`的spread从哪来vs实盘盘口数据 |
| P1-R11-02 | L1185 | ProductionQuantSystem | 到期日滑点倍增反向差异：回测用`pd.Timestamp.now()`实盘用实际到期日 |
| P1-R11-03 | L67 | order_service | 手续费差异：回测3维vs实盘可能使用5维模型 |
| P1-R11-04 | L1570 | position_service | 平仓频率差异：回测每Bar末尾执行vs实盘由事件驱动 |
| P1-R11-05 | 无 | ProductionQuantSystem | 订单执行差异：回测订单立即成交无延迟vs实盘有网络延迟 |
| P1-R11-06 | L5319 | shadow_strategy_engine | Alpha计算差异：回测点值vs实盘应有CI宽度处理 |

### B. 时间参数（8项）
| 编号 | 代码位置 | 问题描述 |
|:---|:---|:---|
| P1-R11-07 | task_scheduler.py:1537-1548 | 两阶段止损`stage1_min_minutes`默认90分钟，不同Bar周期下行为不一致 |
| P1-R11-08 | task_scheduler.py:1559-1563 | 斜率计算除以Bar数量而非时间窗口 |
| P1-R11-09 | risk_service.py:1685 | 日回撤使用日历日00:00~23:59定义，未考虑夜盘跨日 |
| P1-R11-10 | risk_service.py:2073 | `confirm_daily_resume()`无时间判断，可被同一天内无限次调用 |
| P1-R11-11 | preprocess_ticks.py:661 | `_minute_safe_chunk_indices()`不检测跨交易日分割 |
| P1-R11-12 | strategy_tick_handler.py:241 | 实盘Bar生成使用`datetime.now()`无时区校验 |
| P1-R11-13 | greeks_calculator.py:488 | 节假日未自动获取，需手动添加到holidays集合 |
| P1-R11-14 | task_scheduler.py:305 | HFT策略`state_confirm_bars=5`（5分钟）与`hft_hard_time_stop_ms=1000`（1秒）时间尺度冲突 |

### C. 权限与隔离（7项）
| 编号 | 代码位置 | 问题描述 |
|:---|:---|:---|
| P1-R11-15 | shadow_strategy_engine.py:262 | 影子策略参数从YAML加载但所有影子实例共享同一YAML，无策略组标识 |
| P1-R11-16 | shadow_strategy_engine.py:721 | 影子策略日志无策略组标识，不同策略组日志混写 |
| P1-R11-17 | structured_jsonl_logger.py | 策略标识字段依赖调用方传入，漏传则无法区分 |
| P1-R11-18 | config_params.py:260 | `update_cached_params()`无鉴权，任何模块可修改全局参数 |
| P1-R11-19 | shadow_strategy_engine.py:338 | `relock_params()`无调用方限制 |
| P1-R11-20 | mode_engine.py:998 | `reload_tvf_params()`无调用方限制 |
| P1-R11-21 | width_cache.py:42 | `width_cache`全局共享，多策略同时写入竞态 |

### D. 配置漂移（6项）
| 编号 | 参数 | 问题描述 |
|:---|:---|:---|
| P1-R11-22 | `hard_time_stop_minutes`废弃残留 | 旧参数名在部分代码中仍被引用，新参数名未完全覆盖 |
| P1-R11-23 | `tvf_params.yaml` vs `ModeConfig` | YAML键名与ModeConfig字段名映射关系待确认 |
| P1-R11-24 | `cascade_config.yaml` vs `DEFAULT_THRESHOLDS` | YAML阈值与代码硬编码阈值是否一致 |
| P1-R11-25 | `_worker_task()` params默认值 | task['params']缺少的键的默认值是否与`PARAM_DEFAULTS`一致 |
| P1-R11-26 | `PARAM_GRID_ROUND1` | `task_scheduler.py`和`enhanced_phase_scan.py`各自定义，是否一致 |
| P1-R11-27 | `PULLBACK_DEFAULTS` | `task_scheduler.py`中定义，回测中是否使用相同定义 |

---

## 三、P2级问题（15项）

| 编号 | 类别 | 问题描述 |
|:---|:---|:---|
| P2-R11-01 | 隔离 | `state_param_sets.yaml`状态参数全局共享，不同策略对"状态"定义不同但共享同一判断 |
| P2-R11-02 | 隔离 | 策略降级后参数仍保留在全局缓存中 |
| P2-R11-03 | 隔离 | 主策略与影子策略日志在同一目录，无子目录隔离 |
| P2-R11-04 | 隔离 | 回测checkpoint按参数哈希命名，无运行ID隔离 |
| P2-R11-05 | 隔离 | `tick_data`表未按品种+日期分区 |
| P2-R11-06 | 隔离 | `get_health_status()` CRITICAL状态不区分是哪个子组件触发 |
| P2-R11-07 | 隔离 | `delay_time_sharpe_3d.py`独立DuckDB与主系统共享目录 |
| P2-R11-08 | 隔离 | `v7_meta_audit_v2.py` monkey-patch非线程安全 |
| P2-R11-09 | 隔离 | `_safety_meta_layer`冗余属性声明造成隔离假象 |
| P2-R11-10 | 隔离 | 参数缓存修改无审计日志 |
| P2-R11-11 | 隔离 | 影子参数从YAML加载而非独立定义，与主策略耦合 |
| P2-R11-12 | 隔离 | 多进程DuckDB写入无事务隔离 |
| P2-R11-13 | 时间 | `ConfigService`冷却时间使用`time.monotonic()`但`_last_param_update`使用`time.time()` |
| P2-R11-14 | 时间 | EV评估缓存TTL=5秒，HFT tick频率>5Hz时最多25个tick用相同EV |
| P2-R11-15 | 时间 | `_option_greeks`缓存无时间过期机制，仅LRU淘汰 |

---

## 四、最严重问题Top 5

| 排名 | 编号 | 严重性说明 |
|:---|:---|:---|
| **1** | P0-R11-01 | 两阶段硬时间止损参数名分裂——**回测和实盘使用完全不同的参数名**，回测验证的阈值在实盘中不生效 |
| **2** | P0-R11-02/03 | 回测五态判定与实盘不等价+**回测完全缺失HMM状态诊断**——回测和实盘状态机根本不同 |
| **3** | P0-R11-14/15 | 回测中**`time.time()`和`pd.Timestamp.now()`与Bar时间混用**——硬时间止损和到期日判断在回测中完全失效 |
| **4** | P0-R11-18/19 | 全局单例`_param_table_cache`和`SafetyMetaLayer`无策略隔离——**一个策略的参数修改影响所有策略** |
| **5** | P0-R11-16 | EOD平仓**未覆盖夜盘品种**——夜盘品种回测中永不平仓 |

---

## 五、与前十轮报告的差异与互补

### 第十轮未覆盖、第十一轮独有发现（30项核心）

| 类别 | 第十一轮独有发现 | 说明 |
|:---|:---|:---|
| **回测-实盘差异** | P0-R11-01 参数名分裂（stage1_min_minutes vs stage1_minutes） | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-02 五态判定逻辑不等价 | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-03 回测缺失HMM状态诊断 | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-04 回测缺失决策跳帧 | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-05 回测仅2/11路径经过风控 | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-06 回测无健康检查 | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-07 影子参数同一对象引用 | 回测-实盘路径 |
| **回测-实盘差异** | P0-R11-08 Alpha无CI | 回测-实盘路径 |
| **时间参数** | P0-R11-13 Bar对齐dt.floor向下取整偏差 | 时间参数 |
| **时间参数** | P0-R11-14 硬时间止损time.time()混用 | 时间参数 |
| **时间参数** | P0-R11-15 到期日计算pd.Timestamp.now() | 时间参数 |
| **时间参数** | P0-R11-16 EOD未覆盖夜盘 | 时间参数 |
| **时间参数** | P0-R11-17 Theta除以365默认非交易日 | 时间参数 |
| **配置漂移** | P0-R11-09 circuit_breaker_trigger_sigma三分歧 | 配置漂移 |
| **配置漂移** | P0-R11-10 max_net_delta_pct四处分歧 | 配置漂移 |
| **配置漂移** | P0-R11-11 max_net_gamma_pct压力测试覆盖过大 | 配置漂移 |
| **配置漂移** | P0-R11-12 p0_gate_check两版本不一致 | 配置漂移 |
| **权限隔离** | P0-R11-18 _param_table_cache全局无隔离 | 权限隔离 |
| **权限隔离** | P0-R11-19 SafetyMetaLayer全局无隔离 | 权限隔离 |
| **权限隔离** | P0-R11-20 DuckDB并发写无保护 | 权限隔离 |
| **权限隔离** | P0-R11-21 沙箱形同虚设 | 权限隔离 |

---

## 六、十一轮累计统计

| 轮次 | P0 | P1 | P2 | 独有发现 | 审计角度 |
|:---|:---|:---|:---|:---|:---|
| R1 | ~25 | ~35 | ~18 | 78 | 代码存在性 |
| R2 | ~14 | ~22 | ~12 | 17 | 方法名/调用链 |
| R3 | 12 | 24 | 15 | 19 | 数据流正向追踪 |
| R4 | 10 | 18 | 14 | 18 | 边界/并发/配置 |
| R5 | 10 | 20 | 12 | 16 | 逻辑一致性 |
| R6 | 14 | 33 | 17 | 25 | 金融算法/安全 |
| R7 | 10 | 23 | 16 | 19 | 手册对照/隐藏功能 |
| R8 | 16 | 20 | 10 | 20 | 参数硬编码/死角 |
| R9 | 22 | 32 | 2 | 28 | 数据流/状态机/接口 |
| R10 | 24 | 18 | 6 | 32 | 线程安全/异常/内存 |
| R11 | 21 | 27 | 15 | 30 | 回测-实盘差异/配置漂移/时间/隔离 |
| **十一轮累计** | **~156** | **~240** | **~135** | — | — |

**十一轮累计总问题：~531项（~156 P0 / ~240 P1 / ~135 P2）**

---

## 七、审计结论

第十一轮审计采用4路并行、4个**前十轮完全未触及的新角度**（回测-实盘差异/配置漂移/时间参数/权限隔离），发现**63项问题**（21 P0 / 27 P1 / 15 P2），其中**30项为前十轮未明确列出的独有发现**。

**核心发现——回测冠军实战崩盘的根本原因之一：**

第十一轮发现的回测-实盘差异问题，极可能是"回测冠军实战崩盘"的**根本原因**。具体而言：
1. **参数名分裂**（P0-R11-01）：回测验证的止损参数在实盘中根本不生效
2. **状态机不等价**（P0-R11-02/03）：回测和实盘的状态判定逻辑完全不同
3. **时间混用**（P0-R11-14/15）：回测中使用系统真实时间计算持仓时间/到期日，导致时间相关逻辑完全失效
4. **跳帧缺失**（P0-R11-04）：回测中所有Bar都决策（高频噪声），实盘中低频决策
5. **风控覆盖率**（P0-R11-05）：回测中仅2/11开仓路径经过风控，箱体系列完全跳过

这些差异导致回测结果"看起来完美"但实盘中行为完全不同——这正是回测冠军实战崩盘的典型模式。

**最紧急修复项（建议立即处理）：**
1. P0-R11-01（参数名分裂）— 回测实盘使用不同参数名
2. P0-R11-02/03（五态/HMM差异）— 回测与实盘状态机根本不同
3. P0-R11-14/15（时间混用）— 回测中time.time()/Timestamp.now()与Bar时间混用
4. P0-R11-18/19（全局单例无隔离）— 策略间互相影响
5. P0-R11-16（夜盘EOD缺失）— 夜盘品种永不平仓