# ali2026v3_trading 4.1-4.9 & 5.1-5.5 问题核查报告

**核查日期**: 2026-05-04
**核查依据**: 27模块专业评判报告_20260504.md
**核查方法**: 逐项对比报告问题与当前代码，验证问题是否仍存在
**核查范围**: 4.1-4.9 (HIGH) + 5.1-5.5 (MEDIUM) = 14个模块共55个问题

---

## 核查总览

| 状态 | 数量 | 占比 |
|------|------|------|
| 已修复 | 28 | 50.9% |
| 部分修复/缓解 | 13 | 23.6% |
| 仍存在 | 14 | 25.5% |
| **合计** | **55** | 100% |

---

## 一、HIGH模块 (4.1-4.9)

### 4.1 diagnosis_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | _shard_enqueue_counts类变量Dict无锁并发读写 | **已修复** | L1306-1307 添加 `_shard_enqueue_lock`，L1331-1332 写操作在锁内 |
| 2 | DiagnosisReport.add_log中self.logs列表操作未加锁 | **已修复** | L77 添加 `_logs_lock`，L134-135 操作在锁内 |
| 3 | is_monitored_contract每次调用都导入config_service | **部分修复** | L922-924 添加1秒TTL缓存缓解，但import仍在函数体内(L939) |
| 4 | MONITORED_CONTRACTS硬编码26个合约 | **部分修复** | 硬编码仍作为fallback(L870-898)，已增加环境变量加载机制(L900-913)；合约数从26减至20 |
| 5 | 6处`except Exception:`无日志吞异常 | **已修复** | 所有except块均已添加logging.debug或错误捕获 |

### 4.2 config_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | ensure_products_with_retry(200+行)含DB写入/SQL断言/重试 | **部分修复** | config_service中已改为3行委托包装器(L404-407)，实际逻辑迁移至product_initializer.py，但product_initializer仍混合DB写入/SQL断言/重试 |
| 2 | 硬编码路径+API密钥硬编码 | **部分修复** | L576 `os.getenv('ALI2026_DATA_DIR', 'D:\\ali2026_data')` 回退值仍硬编码；API密钥字段存在但值为空字符串(L1136-1139) |
| 3 | DEFAULT_PARAM_TABLE模块级可变全局字典 | **仍存在** | L1130 定义，L1543-1554 `merge_option_params_to_default()` 通过global直接修改，无保护 |
| 4 | ConfigService._config_data类变量无锁保护 | **仍存在** | L645 类变量无锁，虽然当前未被主动使用但仍是隐患 |
| 5 | ServiceContainer依赖业务层 | **仍存在** | L1983-2013 导入event_bus/SignalService/OrderService等L2业务层 |

### 4.3 width_cache.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | _update_sort_bucket/_remove_from_sort_bucket未独立持锁 | **已修复** | L1190/L1278 均已加 `with self._lock:` |
| 2 | 3处`except Exception: pass` | **已修复** | 全部改为 `except Exception as e: logging.xxx(...)` |
| 3 | _recalc_all_state_for_underlying锁内遍历全部期权重算 | **已修复** | L837 重构为三阶段：锁内快照→锁外重算→锁内替换 |
| 4 | 对get_params_service()全局单例强依赖 | **仍存在** | 19处调用，未通过注入方式解耦 |
| 5 | on_option_tick内datetime.now()判断交易日 | **部分修复** | L731-733 提取为 `_get_current_trading_date` 可mock方法，但本质仍用datetime.now() |

### 4.4 subscription_manager.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | on_tick无锁保护 | **仍存在** | L1202-1342 整体无锁，仅局部计数器有锁 |
| 2 | _check_alert共享计数器无锁 | **已修复** | L677 已加 `with self._lock:` |
| 3 | on_tick中`except Exception: pass` | **已修复** | L1218 改为 `except Exception:` + logger.debug |
| 4 | _handshake空实现直接返回True | **仍存在** | L1042-1046 仍为空实现+return True，已加TODO标记 |
| 5 | 后台线程在构造函数中自动启动 | **仍存在** | L208 `__init__` 中直接调用 `_start_background_threads()` |

### 4.5 storage_query.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | register_instrument全程无锁保护 | **已修复** | L996 已加 `with self._lock:` |
| 2 | _get_instrument_info/_get_info_by_id无锁 | **已修复** | L332/L359 均已加 `with self._lock:` |
| 3 | infer_exchange_from_id硬编码品种-交易所映射 | **仍存在** | L96-110 `_EXCHANGE_MAP` 硬编码字典 |
| 4 | _fetch_historical_kline_data尝试7种API签名 | **仍存在** | L537-544 kline_call_specs 6种 + L555-563 get_kline 2种 |
| 5 | _TempParams内部类 | **已修复** | 已移除，搜索无匹配 |

### 4.6 position_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | 穿透RiskService封装直接操作_position_limits字典 | **部分修复** | 写操作已改用公共API，但读操作仍穿透(L828/L863 `getattr(..., '_position_limits', {})`) |
| 2 | check_position_limit默认允许违反fail-safe | **部分修复** | RiskService不可用时已改为阻断(L417-419)，但异常分支仍返回True(L421-423) |
| 3 | 获取对手价`except Exception: pass`静默吞错 | **已修复** | L647-648 改为 `logging.debug(...)`，不再静默（但日志级别偏低） |
| 4 | 重试逻辑含time.sleep阻塞 | **仍存在** | L696-724 最大阻塞0.7秒未消除 |
| 5 | 止盈止损比率硬编码默认值无告警 | **已修复** | L514-517 已加 `logging.warning` |

### 4.7 strategy_core_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | _start_executed/_stop_executed无锁保护 | **已修复** | L314 添加 `_lifecycle_lock`，onStart/onStop/internal_resume_strategy 均在锁内操作 |
| 2 | onStart包含5个try-except半初始化状态 | **已修复** | L465-583 实现 `_init_steps` 追踪 + `_CRITICAL_STEPS` + DEGRADED降级 |
| 3 | __init__中写入日志文件I/O | **已修复** | 已移除 `open('auto_logs/...')` 操作，替换为 `logging.info(...)` |
| 4 | 动态创建匿名类type('Params'...) | **已修复** | L257-278 替换为 `StrategyParams` 具名类 |
| 5 | 导入14个模块耦合汇聚 | **已修复** | 顶层导入从14个减至8个（4标准库+4Mixin必须），5个改为延迟导入，6个未使用已移除 |

### 4.8 strategy_tick_handler.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | _degraded_tick_count/_snapshot_tick_count无锁递增 | **部分修复** | 递增已加锁(L360/L616)，但_degraded_tick_count初始化在锁外(L358-359) |
| 2 | flush失败回写在shard_lock外 | **已修复** | L637-640 已移入 `with shard_lock:` 内 |
| 3 | on_tick热路径超100行 | **仍存在** | L139-L269 约131行 |
| 4 | L174/L307 `except Exception: pass` | **部分修复** | 原位置已修复，但L368-369仍有新的 `except Exception: pass` |
| 5 | _dispatch_tick中storage异常可能导致tick丢失 | **部分修复** | 单条tick有保护，外层异常仍可丢批次 |

### 4.9 t_type_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | _ensure_preload_started中6个if分支锁保护不完整 | **已修复** | L107 简化为单一 `with self._data_lock:` 块 |
| 2 | _preload_from_db部分失败仍标记complete | **部分修复** | L431-435 添加完整性验证告警，但仍标记complete不重试 |
| 3 | 大量方法为WidthStrengthCache薄代理 | **仍存在** | 9个薄代理方法未合并 |
| 4 | _preload_from_db直接执行SQL | **已修复** | L226-271 封装为 `_query_latest_futures()`/`_query_latest_options()` 独立方法 |
| 5 | on_future_tick首次tick触发预加载 | **仍存在** | L458-460 热路径仍有条件检查和潜在锁竞争 |

---

## 二、MEDIUM模块 (5.1-5.5)

### 5.1 query_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | QueryService直接访问storage._params_service私有属性(5+处) | **已修复** | L296-302 新增 `_get_params_service()` 方法，通过公共单例获取 |
| 2 | load_and_preregister_instruments(150行)违反SRP | **仍存在** | L495-L651 约157行，包含加载/补齐/预注册/通知4个职责 |
| 3 | StorageMaintenanceService放在query_service中违反SRP | **已修复** | 已迁移至独立文件 maintenance_service.py，query_service仅保留导入 |
| 4 | _internal_id_to_instrument_idx缓存无锁 | **已修复** | L294 新增 `_idx_lock`，L308-321 索引构建在锁内 |

### 5.2 params_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | get_product_cache无锁(L337) | **已修复** | L337-338 `with self._lock: return self._product_cache.get(product)` |
| 2 | clear_instrument_cache清空窗口期 | **已修复** | L340-360 原子替换模式：先快照→重建→失败恢复旧缓存 |
| 3 | 单例初始化失败使用不完整实例(L1152) | **已修复** | L1192-1194 抛出 `RuntimeError`，全局变量仍为None可重试 |
| 4 | load_instrument_list失败返回None非抛异常 | **已修复** | L531-533 抛 `ValueError`，L537-541 抛 `RuntimeError` |

### 5.3 greeks_calculator.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | _should_update TOCTOU竞态 | **已修复** | L713-767 改为参数由调用方在锁内传入，消除TOCTOU |
| 2 | L394 `except Exception: pass`节假日解析 | **已修复** | L622-625 改为 `logging.warning(...)` |
| 3 | TradingCalendar延迟导入scheduler_service | **已修复** | L443 TradingCalendar直接定义在文件内，无延迟导入 |
| 4 | 971行仍偏长 | **仍存在** | 文件约1218行，含5个类，比原报告更长 |

### 5.4 strategy_historical.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | L824-826无锁快速路径 | **已修复** | L831 所有状态读取在 `with self._historical_loader_lock:` 内 |
| 2 | L862 _background_threads.append无锁 | **已修复** | L307 添加 `_background_threads_lock`，L638-639/L872-873 在锁内 |
| 3 | _resolve_historical_provider 6级降级链路复杂 | **仍存在** | L397-455 6级降级链路未简化 |
| 4 | 与storage深度耦合 | **仍存在** | 直接访问storage私有属性19处 |
| 5 | L692 `except Exception: pass` | **已修复** | L696-697 改为 `logging.warning(...)` |

### 5.5 scheduler_service.py

| # | 问题描述 | 状态 | 证据 |
|---|---------|------|------|
| 1 | L157 _running无锁设置 | **已修复** | L122 改为 `threading.Event`，L146/L159 在锁内操作 |
| 2 | L431 _active_threads无锁写入 | **已修复** | L434 在 `with self._lock:` 内写入 |
| 3 | MarketTimeService应独立 | **仍存在** | L544 仍定义在scheduler_service.py中 |
| 4 | 超时线程可能成孤儿线程 | **部分修复** | L432 设 `daemon=True`，但超时后无主动取消机制 |

---

## 三、仍存在问题汇总（14项）

| # | 模块 | 问题 | 严重程度 | 建议 |
|---|------|------|---------|------|
| 1 | config_service.py | DEFAULT_PARAM_TABLE模块级可变全局字典 | HIGH | 加锁或改不可变 |
| 2 | config_service.py | ConfigService._config_data类变量无锁保护 | MEDIUM | 加锁或移除 |
| 3 | config_service.py | ServiceContainer依赖业务层 | MEDIUM | 迁移至独立模块 |
| 4 | width_cache.py | 对get_params_service()全局单例强依赖(19处) | MEDIUM | 构造函数注入 |
| 5 | subscription_manager.py | on_tick无锁保护 | HIGH | 加锁或原子引用 |
| 6 | subscription_manager.py | _handshake空实现直接返回True | LOW | 实现或标记NotImplementedError |
| 7 | subscription_manager.py | 后台线程在构造函数中自动启动 | MEDIUM | 改为显式start()调用 |
| 8 | storage_query.py | infer_exchange_from_id硬编码品种-交易所映射 | MEDIUM | 提取为配置 |
| 9 | storage_query.py | _fetch_historical_kline_data尝试6+2种API签名 | MEDIUM | 统一API签名 |
| 10 | position_service.py | 重试逻辑含time.sleep阻塞(0.7秒) | MEDIUM | 改为异步重试 |
| 11 | strategy_tick_handler.py | on_tick热路径超100行(131行) | MEDIUM | 拆分为检查层→分发层→统计层 |
| 12 | t_type_service.py | 大量方法为WidthStrengthCache薄代理(9个) | LOW | 评估合并或让调用方直接使用 |
| 13 | t_type_service.py | on_future_tick首次tick触发预加载 | MEDIUM | 改为显式初始化 |
| 14 | greeks_calculator.py | 文件1218行偏长，含5个类 | LOW | 拆分IVCalculator/TradingCalendar |

---

## 四、部分修复问题汇总（13项）

| # | 模块 | 问题 | 修复进展 | 遗留风险 |
|---|------|------|---------|---------|
| 1 | diagnosis_service.py | is_monitored_contract每次导入config_service | 已加1秒TTL缓存 | import仍在函数体内 |
| 2 | diagnosis_service.py | MONITORED_CONTRACTS硬编码 | 已加环境变量加载 | 硬编码仍作fallback |
| 3 | config_service.py | ensure_products_with_retry违反SRP | 已迁移至product_initializer | product_initializer仍混合职责 |
| 4 | config_service.py | 硬编码路径+API密钥 | 路径优先读环境变量，密钥值为空 | 回退默认值仍硬编码 |
| 5 | width_cache.py | datetime.now()判断交易日 | 提取为可mock方法 | 本质仍用datetime.now() |
| 6 | position_service.py | 穿透RiskService封装 | 写操作已改用公共API | 读操作仍穿透 |
| 7 | position_service.py | check_position_limit默认允许 | RiskService不可用已阻断 | 异常分支仍返回True |
| 8 | strategy_tick_handler.py | 计数器无锁递增 | 递增已加锁 | 初始化在锁外 |
| 9 | strategy_tick_handler.py | except Exception: pass | 原位置已修复 | L368-369有新同类问题 |
| 10 | strategy_tick_handler.py | storage异常导致tick丢失 | 单条tick有保护 | 外层异常仍可丢批次 |
| 11 | t_type_service.py | 部分失败仍标记complete | 有完整性告警 | 仍标记完成不重试 |
| 12 | strategy_historical.py | _resolve_historical_provider复杂 | 未简化 | 6级降级链路 |
| 13 | scheduler_service.py | 超时线程成孤儿 | 设daemon=True | 无主动取消机制 |

---

## 五、已修复问题汇总（28项）

| # | 模块 | 问题 |
|---|------|------|
| 1 | diagnosis_service.py | _shard_enqueue_counts无锁 → 已加锁 |
| 2 | diagnosis_service.py | DiagnosisReport.add_log无锁 → 已加锁 |
| 3 | diagnosis_service.py | 6处except Exception:无日志 → 全部添加日志 |
| 4 | width_cache.py | _update_sort_bucket未持锁 → 已加锁 |
| 5 | width_cache.py | 3处except Exception:pass → 全部改为logging |
| 6 | width_cache.py | _recalc_all_state锁内重算 → 改为快照+锁外重算+锁内替换 |
| 7 | subscription_manager.py | _check_alert共享计数器无锁 → 已加锁 |
| 8 | subscription_manager.py | on_tick中except Exception:pass → 已加日志 |
| 9 | storage_query.py | register_instrument无锁 → 已加锁 |
| 10 | storage_query.py | _get_instrument_info无锁 → 已加锁 |
| 11 | storage_query.py | _TempParams内部类 → 已移除 |
| 12 | position_service.py | 获取对手价静默吞错 → 已加日志 |
| 13 | position_service.py | 止盈止损比率无告警 → 已加warning |
| 14 | strategy_core_service.py | _start_executed/_stop_executed无锁 → 已加_lifecycle_lock |
| 15 | strategy_core_service.py | onStart半初始化无追踪 → 已实现_init_steps追踪+DEGRADED |
| 16 | strategy_core_service.py | __init__中写日志文件 → 已移除，改为logging.info |
| 17 | strategy_core_service.py | type('Params'...)匿名类 → 已替换为StrategyParams |
| 18 | strategy_core_service.py | 导入14个模块耦合 → 已拆分，顶层8个+5个延迟导入 |
| 19 | strategy_tick_handler.py | flush失败回写在锁外 → 已移入shard_lock内 |
| 20 | t_type_service.py | _ensure_preload_started锁保护不完整 → 简化为单一锁块 |
| 21 | t_type_service.py | _preload_from_db直接执行SQL → 封装为独立查询方法 |
| 22 | query_service.py | 直接访问storage._params_service → 改用公共单例 |
| 23 | query_service.py | StorageMaintenanceService放错位置 → 迁移至maintenance_service.py |
| 24 | query_service.py | _internal_id_to_instrument_idx无锁 → 已加锁 |
| 25 | params_service.py | get_product_cache无锁 → 已加锁 |
| 26 | params_service.py | clear_instrument_cache窗口期 → 原子替换+失败恢复 |
| 27 | params_service.py | 单例初始化失败用不完整实例 → 改为抛RuntimeError |
| 28 | params_service.py | load_instrument_list返回None → 改为抛ValueError/RuntimeError |

---

**核查人**: AI Assistant
**核查完成时间**: 2026-05-04
