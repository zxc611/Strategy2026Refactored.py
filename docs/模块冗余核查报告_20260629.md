# ali2026v3_trading 模块冗余核查报告

**核查日期**: 2026-06-29  
**核查方法**: 逐文件对比功能、导入关系、代码重复  
**核查范围**: 2026-06-16至2026-06-29新增的34个文件

---

## 一、冗余问题汇总

### ❌ P0级冗余（功能完全重复，需立即清理）

| 问题 | 文件1 | 文件2 | 影响 | 建议 |
|------|-------|-------|------|------|
| **HFT分发函数重复** | strategy/tick_hft.py (897行) | strategy/tick_hft_dispatch.py (471行) | tick_hft.py已合并tick_hft_dispatch.py的所有函数，但tick_processing_service.py等3个文件仍导入tick_hft_dispatch | **删除tick_hft_dispatch.py**，统一使用tick_hft.py |
| **CheckpointService重复** | strategy/persistence_service.py (480行) | strategy/checkpoint_service.py (234行) | persistence_service.py已合并checkpoint_service.py，但service_factory.py等4个文件仍导入checkpoint_service | **删除checkpoint_service.py**，统一使用persistence_service.py |

### ⚠️ P1级冗余（功能部分重复，需评估）

| 问题 | 文件 | 说明 | 建议 |
|------|------|------|------|
| **singleton_registry重复** | infra/singleton_registry.py (180行) | registry_service.py已合并，但26个文件仍导入singleton_registry | **删除singleton_registry.py**，统一使用registry_service.py |
| **callback_registry重复** | infra/callback_registry.py (172行) | registry_service.py已合并，但2个文件仍导入callback_registry | **删除callback_registry.py**，统一使用registry_service.py |
| **risk_rules重复** | infra/risk_rules.py (76行) | security_service.py已合并，但仍存在独立文件 | **删除risk_rules.py**，统一使用security_service.py |
| **security部分重复** | infra/security.py (418行) | security_service.py已合并，但保留security.py作为基础模块 | 评估是否需要保留security.py |

**总计冗余**：933行代码（471 + 234 + 180 + 172 + 76）

---

## 二、详细分析

### 2.1 HFT分发函数重复（P0）

**现状**：
- `tick_hft.py`（897行）：包含`dispatch_hft_tick`等8个HFT函数 + `DynamicPursuitEngine`等类
- `tick_hft_dispatch.py`（471行）：只包含`dispatch_hft_tick`等8个HFT函数

**代码证据**：

tick_hft.py头部注释：
```python
"""HFT引擎与追仓/金字塔策略 - 合并自tick_hft_dispatch.py和strategy_tick_handler.py (2026-06-12)"""
```

tick_hft.py的`__all__`：
```python
__all__ = [
    # from tick_hft_dispatch
    'dispatch_hft_tick',
    'execute_pursuit_exit',
    'execute_pursuit_entry',
    'execute_pursuit_add',
    'handle_arbitrage_signal',
    'handle_transition_signal',
    'handle_smart_money_signal',
    'handle_filtered_signal',
    # from strategy_tick_handler
    'TickHandlerMixin',
    'DynamicPursuitEngine',
    ...
]
```

**导入关系**：
- `tick_processing_service.py`导入`tick_hft_dispatch`（错误）
- `tests/run_e2e12_signal_strength.py`导入`tick_hft_dispatch`（错误）
- `tests/test_coverage_boost_final9.py`导入`tick_hft_dispatch`（错误）

**修复方案**：
1. 删除`strategy/tick_hft_dispatch.py`
2. 修改所有导入：
   ```python
   # 错误
   from ali2026v3_trading.strategy.tick_hft_dispatch import dispatch_hft_tick
   
   # 正确
   from ali2026v3_trading.strategy.tick_hft import dispatch_hft_tick
   ```

---

### 2.2 CheckpointService重复（P0）

**现状**：
- `persistence_service.py`（480行）：包含`CheckpointService` + `RecoveryService`
- `checkpoint_service.py`（234行）：只包含`CheckpointService`

**代码证据**：

persistence_service.py头部注释：
```python
"""persistence_service.py - 检查点与灾备恢复服务合并
合并自 checkpoint_service.py + recovery_service.py (2026-06-12)
```

persistence_service.py第32行：
```python
class CheckpointService:
    """独立检查点/状态同步服务 — 通过注入回调访问策略状态，不依赖Mixin继承"""
```

**导入关系**：
- `service_factory.py`导入`checkpoint_service.CheckpointService`（错误）
- `strategy_core_service.py`导入`checkpoint_service.CheckpointService`（错误）
- `tests/test_dual_backtest_fence.py`导入`checkpoint_service.CheckpointService`（错误）
- `tests/test_strategy_low_cov.py`导入`persistence_service.CheckpointService`（正确）

**修复方案**：
1. 删除`strategy/checkpoint_service.py`
2. 修改所有导入：
   ```python
   # 错误
   from ali2026v3_trading.strategy.checkpoint_service import CheckpointService
   
   # 正确
   from ali2026v3_trading.strategy.persistence_service import CheckpointService
   ```

---

### 2.3 singleton_registry重复（P1）

**现状**：
- `registry_service.py`（1153行）：已合并`callback_registry.py` + `singleton_registry.py` + `state_store.py`
- `singleton_registry.py`（180行）：仍独立存在

**代码证据**：

registry_service.py头部注释：
```python
"""registry_service.py - 注册表服务合并

合并并发callback_registry.py + singleton_registry.py + state_store.py (2026-06-12)
```

registry_service.py的`__all__`：
```python
__all__ = [
    # Section 1: CallbackRegistry
    'CallbackRegistry', 'CallbackGroup', 'get_callback_group', 'reset_callback_group',
    # Section 2: SingletonRegistry
    'SingletonRegistry',
    # Section 3: StateStore
    'StateSnapshot', 'StateStore', 'get_state_store', 'reset_state_store',
]
```

**导入关系**：
- 26个文件导入`singleton_registry.SingletonRegistry`（错误）
- 包括：config_service.py, tvf_param_loader.py, governance_engine.py, performance_monitor.py等

**修复方案**：
1. 删除`infra/singleton_registry.py`
2. 修改所有导入：
   ```python
   # 错误
   from ali2026v3_trading.infra.singleton_registry import SingletonRegistry
   
   # 正确
   from ali2026v3_trading.infra.registry_service import SingletonRegistry
   ```

---

### 2.4 callback_registry重复（P1）

**现状**：
- `registry_service.py`（1153行）：已合并`callback_registry.py`
- `callback_registry.py`（172行）：仍独立存在

**导入关系**：
- `strategy_core_service.py`导入`callback_registry`（错误）
- `tests/test_integrated_modules.py`导入`callback_registry`（错误）

**修复方案**：
1. 删除`infra/callback_registry.py`
2. 修改所有导入：
   ```python
   # 错误
   from ali2026v3_trading.infra.callback_registry import get_callback_group
   
   # 正确
   from ali2026v3_trading.infra.registry_service import get_callback_group
   ```

---

### 2.5 risk_rules重复（P1）

**现状**：
- `security_service.py`（1526行）：已合并`security.py` + `risk_audit_utils.py` + `risk_rules.py`
- `risk_rules.py`（76行）：仍独立存在

**代码证据**：

security_service.py头部注释：
```python
"""security_service.py - 安全服务合并

合并并发security.py + risk_audit_utils.py + risk_rules.py (2026-06-12)
```

**修复方案**：
1. 删除`infra/risk_rules.py`
2. 修改所有导入：
   ```python
   # 错误
   from ali2026v3_trading.infra.risk_rules import check_daily_drawdown_hard_stop
   
   # 正确
   from ali2026v3_trading.infra.security_service import check_daily_drawdown_hard_stop
   ```

---

### 2.6 security部分重复（P1）

**现状**：
- `security_service.py`（1526行）：已合并`security.py`
- `security.py`（418行）：仍独立存在

**代码证据**：

security_service.py导入security.py：
```python
# 五唯一性修复：从 security.py 统一导入 SecurityProfile 与 ENV_SECURITY_PROFILES
from ali2026v3_trading.infra.security import (
    SecurityProfile, ENV_SECURITY_PROFILES,
)
```

**说明**：
- `security.py`作为基础模块被`security_service.py`导入
- 这是合理的设计，不是冗余
- 但需要确认是否有其他文件直接导入`security.py`

**建议**：保留`security.py`作为基础模块

---

## 三、修复优先级

### P0-立即修复（功能完全重复）

1. **删除`strategy/tick_hft_dispatch.py`**
   - 影响文件：3个
   - 修复时间：5分钟

2. **删除`strategy/checkpoint_service.py`**
   - 影响文件：4个
   - 修复时间：5分钟

### P1-评估后修复（功能部分重复）

3. **删除`infra/singleton_registry.py`**
   - 影响文件：26个
   - 修复时间：15分钟

4. **删除`infra/callback_registry.py`**
   - 影响文件：2个
   - 修复时间：5分钟

5. **删除`infra/risk_rules.py`**
   - 影响文件：需检查
   - 修复时间：5分钟

---

## 四、修复脚本

### 修复P0-1：删除tick_hft_dispatch.py

```python
# 1. 删除文件
rm ali2026v3_trading/strategy/tick_hft_dispatch.py

# 2. 修改导入（3个文件）
# tick_processing_service.py:58
# tests/run_e2e12_signal_strength.py:24
# tests/test_coverage_boost_final9.py:177-209

# 修改前
from ali2026v3_trading.strategy.tick_hft_dispatch import dispatch_hft_tick

# 修改后
from ali2026v3_trading.strategy.tick_hft import dispatch_hft_tick
```

### 修复P0-2：删除checkpoint_service.py

```python
# 1. 删除文件
rm ali2026v3_trading/strategy/checkpoint_service.py

# 2. 修改导入（4个文件）
# strategy/service_factory.py:16
# strategy/strategy_core_service.py:14
# tests/test_dual_backtest_fence.py:185,378

# 修改前
from ali2026v3_trading.strategy.checkpoint_service import CheckpointService

# 修改后
from ali2026v3_trading.strategy.persistence_service import CheckpointService
```

### 修复P1-1：删除singleton_registry.py

```python
# 1. 删除文件
rm ali2026v3_trading/infra/singleton_registry.py

# 2. 修改导入（26个文件）
# config/config_service.py:1562
# config/tvf_param_loader.py:128,148,166
# governance/governance_engine.py:660
# infra/performance_monitor.py:65,264
# param_pool/optimization/sensitivity.py:461,479
# ProductionQuantSystem.py:38,49
# risk/risk_service.py:1427,1654
# signal/plr_calculator.py:261
# strategy_judgment/causal_chain_utils.py:63,126,203,259,362
# tests/conftest.py:74,81
# tests/test_r12_f1_p1_40_di_unify.py:21,57
# tests/test_tvf_param_loader.py:21
# tests/_audit_r7.py:65

# 修改前
from ali2026v3_trading.infra.singleton_registry import SingletonRegistry

# 修改后
from ali2026v3_trading.infra.registry_service import SingletonRegistry
```

### 修复P1-2：删除callback_registry.py

```python
# 1. 删除文件
rm ali2026v3_trading/infra/callback_registry.py

# 2. 修改导入（2个文件）
# strategy/strategy_core_service.py:164
# tests/test_integrated_modules.py:836

# 修改前
from ali2026v3_trading.infra.callback_registry import get_callback_group

# 修改后
from ali2026v3_trading.infra.registry_service import get_callback_group
```

### 修复P1-3：删除risk_rules.py

```python
# 1. 删除文件
rm ali2026v3_trading/infra/risk_rules.py

# 2. 修改导入（需检查所有导入risk_rules的文件）

# 修改前
from ali2026v3_trading.infra.risk_rules import check_daily_drawdown_hard_stop

# 修改后
from ali2026v3_trading.infra.security_service import check_daily_drawdown_hard_stop
```

---

## 五、核查结论

### 发现问题

1. ❌ **P0级冗余**：2个文件完全重复（tick_hft_dispatch.py, checkpoint_service.py）
2. ⚠️ **P1级冗余**：5个文件部分重复（singleton_registry.py, callback_registry.py, risk_rules.py, risk_audit_utils.py, cross_system.py）
3. ⚠️ **C类薄重导出层**：3个文件仅做重导出（_deprecated_reexports.py, deviation_classifier.py, subscription_manager.py）

### 影响评估

- **代码冗余**：1,331行（471 + 255 + 180 + 172 + 76 + 160 + 22 + 5 + 18 + 18）
- **维护成本**：需要同时维护多个相同功能的文件
- **导入混乱**：不同文件导入不同模块，增加理解成本

### ✅ 已执行修复（2026-06-29）

#### 第一批：7个冗余文件彻底删除

| 已删除文件 | 目标模块 | 调用方修改数 | 验证结果 |
|------------|----------|-------------|----------|
| strategy/checkpoint_service.py | persistence_service | 3处 | ✅ CheckpointService在目标中存在 |
| strategy/tick_hft_dispatch.py | tick_hft | 2处 | ✅ 8个函数在目标中存在 |
| infra/callback_registry.py | registry_service | 1处 | ✅ 4个符号在目标中存在 |
| infra/risk_rules.py | security_service | 4处 | ✅ 2个函数在目标中存在 |
| infra/singleton_registry.py | registry_service | 26处 | ✅ SingletonRegistry在目标中存在 |
| infra/risk_audit_utils.py | security_service | 8处 | ✅ 34个符号在目标中存在 |
| infra/cross_system.py | trading_utils | 0处 | ✅ 6个符号在目标中存在 |

- 27个调用方文件导入路径已重定向
- 5个目标模块语法检查全部通过
- 27个调用方文件语法检查全部通过
- 7个`__init__.py`映射全部正确
- AlertLevel定义已从risk_audit_utils移入security_service（解决循环依赖）

#### 第二批：3个C类薄重导出层删除

| 已删除文件 | 目标模块 | 调用方修改数 |
|------------|----------|-------------|
| infra/_deprecated_reexports.py | trading_utils/commission_utils等 | 2处 |
| infra/deviation_classifier.py | trading_utils | 0处 |
| infra/subscription_manager.py | subscription_service | 0处 |

#### 28个未跟踪文件审查结果

| 分类 | 数量 | 说明 |
|------|------|------|
| A-必要 | 13个 | 有活跃调用方，功能独立，需提交git |
| C-薄重导出层 | 3个 | 已删除 |
| D-内部辅助 | 12个 | 被_开头模块内部引用，需提交git |

**A类保留文件(13)**：final_three_layer_config, commission_utils, event_publisher, exception_layers, market_time_service, security, service_contracts, box_detector, recovery_service, service_factory, strategy_core_service_builder, strategy_tick_handler, judgment_three_layer

**D类保留文件(12)**：_backup_restore, _disk_monitor, _helpers, _ops_automation, _ops_framework, _ops_knowledge_metrics, _storage, _box_spring_types, _shadow_internals, _shadow_strategy_pnl, _shadow_strategy_pnl_metrics, shadow_strategy_types

### 建议

1. **提交git**：25个A/D类文件应提交到git，消除untracked状态
2. **建立规范**：新增模块前先检查是否已存在类似功能
3. **定期清理**：每月执行一次冗余检查

---

**报告完成时间**: 2026-06-29  
**核查结论**: ✅ 10个冗余文件已删除（7个P0/P1 + 3个C类），25个必要/内部辅助文件保留待提交git