# ali2026v3_trading 交易系统 — 交易所规则适配专项审计报告

**审计基准**: `参数池统一执行方案与使用手册_V7.0_工程落地版_20260510.md`
**审计范围**: `ali2026v3_trading/` 目录下所有 .py 文件（生产系统、测试系统、评判系统）
**审计方法**: 全量扫描 + 逐行读取交叉验证 + 全链路追踪（方法定义→传递→消费→功能真正实现）
**审计日期**: 2026-05-23
**审计状态**: 仅列出问题清单，不修改代码

---

## 审计维度1：涨跌停板处理

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 1-1 | `order_service.py` | 全文 | **无任何涨跌停价格检查逻辑**。`send_order()` 方法直接发送委托，未检查委托价格是否触及涨跌停板。`_send_order()` 直接调用 `self.engine.send_order()`，无任何涨跌停拦截。 | **P0** | 生产 |
| 1-2 | `order_service.py` | 全文 | **无涨跌停行情标记机制**。`_on_tick()` 回调处理行情数据，但未对 `upper_limit`/`lower_limit` 字段进行任何检测或标记。涨跌停行情可能被当作正常行情用于信号计算。 | **P0** | 生产/测试 |
| 1-3 | `order_service.py` | L375-383 | `send_order()` 方法中价格修正逻辑（`_correct_price()`）仅做 ±tick_size 偏移，不检查修正后价格是否越涨跌停板。在涨停板时追价可能导致无效委托。 | **P0** | 生产 |
| 1-4 | `position_service.py` | L940-1005 | `_trigger_close_position()` 紧急平仓时直接使用对手价或最新价±tick_size下单，未检查目标价格是否在涨跌停板范围内。极端行情下可能发出无效平仓委托。 | **P0** | 生产 |
| 1-5 | `risk_service.py` | 全文 | **风控服务中无涨跌停相关风控规则**。`RiskService` 包含资金、持仓、时间、熔断等风控，但无任何涨跌停板风控逻辑（如涨跌停禁止开仓、涨跌停加速平仓等）。 | **P1** | 生产 |
| 1-6 | `params_service.py` | L784-791 | `load_caches_from_db()` 从数据库加载合约元数据时，未加载/存储涨跌停价格相关信息（数据库查询SQL中无 `upper_limit`/`lower_limit` 字段）。 | **P1** | 生产/测试/评判 |
| 1-7 | `product_initializer.py` | 全文 | 品种规格字典仅包含 `tick_size` 和 `contract_size`，无涨跌停幅度配置。 | **P2** | 生产/测试/评判 |
| 1-8 | `scheduler_service.py` | L550-624 | `MarketTimeService` 的交易时段判断不考虑涨跌停导致的提前休市或暂停交易。 | **P2** | 生产 |

### 全链路追踪结论
- **定义**: 全系统未定义涨跌停检测函数
- **传递**: Tick 数据中的涨跌停字段（如有）未在 `data_service.py` 的 `_on_tick()` 中提取或传递
- **消费**: 无消费方使用涨跌停数据
- **结论**: ⚠️ **全链路缺失** — 涨跌停板处理在系统中完全不存在

---

## 审计维度2：交割日/到期日管理

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 2-1 | `order_service.py` | 全文 | **生产交易系统无到期日检测**。`send_order()` 方法不检查合约是否临近到期日。期权/期货合约在到期日前可能未被正确平仓或移仓。 | **P0** | 生产 |
| 2-2 | `position_service.py` | 全文 | **持仓服务无到期日管理逻辑**。无期权到期日检查、无期货交割日移仓逻辑。持仓可能在到期日被强制交割。 | **P0** | 生产 |
| 2-3 | `risk_service.py` | 全文 | **风控服务无到期日风控**。无到期日临近预警、无交割日前强制平仓规则。 | **P0** | 生产 |
| 2-4 | `scheduler_service.py` | L550-624 | `MarketTimeService` 无交割日计算逻辑（如CFFEX股指期货交割日为第3个星期五等规则未实现）。 | **P1** | 生产 |
| 2-5 | `params_service.py` | L718-779 | `load_caches_from_db()` 加载合约时从 `futures_instruments` 和 `option_instruments` 表读取数据，但未读取 `expiry_date`/`delivery_date` 等到期日字段。SQL 查询中无相关字段。 | **P1** | 生产/测试/评判 |
| 2-6 | `product_initializer.py` | L23-65 | 品种规格字典无到期日规则配置（如 `expiry_rule: "3rd_friday"` 等）。 | **P1** | 测试/评判 |
| 2-7 | `参数池/task_scheduler.py` | L1164-1200 | 回测系统中有 `_get_expiry_slippage_multiplier()` 到期日滑点倍增逻辑，但这是回测代码，不影响生产交易。且到期日判断依赖 `bar.get('expiry_date')` 字段，该字段在实际tick数据中可能不存在。 | **P1** | 评判(回测) |
| 2-8 | `position_service.py` | 全文 | 无移仓换仓（rollover）逻辑。期货合约到期前需要手动移仓，系统无自动移仓功能。 | **P2** | 生产 |

### 全链路追踪结论
- **定义**: 生产系统中未定义到期日管理函数
- **传递**: 合约元数据中未包含到期日信息
- **消费**: 无消费方使用到期日数据
- **结论**: ⚠️ **全链路缺失** — 生产系统到期日管理完全不存在，仅回测系统有简单的到期日滑点倍增

---

## 审计维度3：最小变动价位(tick size)

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 3-1 | `order_service.py` | L408-418 | `_correct_price()` 方法仅做 ±tick_size 偏移，**未将价格对齐到 tick_size 的整数倍**。正确做法应为 `price = round(price / tick_size) * tick_size`。当前偏移可能产生非 tick_size 整数倍的价格，导致交易所拒单。 | **P0** | 生产 |
| 3-2 | `order_service.py` | L577-607 | `_get_tick_size()` 方法回退到硬编码默认值（如 `IF/IC/IH/IM → 0.2`）。如果品种不在默认列表中，返回 0.2 可能不正确（如债券期货 tick_size=0.005，黄金 tick_size=0.02）。 | **P1** | 生产 |
| 3-3 | `order_service.py` | L610-612 | `_calculate_chase_price()` 追价逻辑使用 `tick_size * chase_ticks` 计算偏移，但未对最终价格做 tick_size 对齐验证。追价后价格可能不对齐。 | **P1** | 生产 |
| 3-4 | `position_service.py` | L972 | `get_params_service().get_float('tick_size', 1.0)` 使用全局参数 `tick_size`，但该参数是全局统一的，不按品种区分。不同品种有不同的 tick_size（如 IF=0.2, AU=0.02），使用统一值会导致价格计算错误。 | **P1** | 生产 |
| 3-5 | `order_service.py` | L1256-1263 | `estimate_slippage()` 中 `tick_size` 使用 `_get_tick_size()` 获取，但滑点计算 `slip_value = min_price_change * slip_ticks * multiplier` 中的 `min_price_change` 与 `tick_size` 概念混用，可能导致计算偏差。 | **P2** | 生产/评判 |
| 3-6 | `product_initializer.py` | L68-69 | `_DEFAULT_FUTURE_SPEC` 和 `_DEFAULT_OPTION_SPEC` 硬编码默认 tick_size=0.2 和 1.0，与交易所实际值可能有偏差。新上市品种如果没有配置将使用错误默认值。 | **P2** | 生产/测试/评判 |
| 3-7 | `params_service.py` | L784-791 | 从数据库加载 `tick_size` 但未在合约缓存中使用。`load_caches_from_db()` 将 `tick_size` 加载到 `temp_product_cache`，但合约信息查询未关联使用。 | **P2** | 生产 |

### 全链路追踪结论
- **定义**: `_get_tick_size()` 定义在 `order_service.py:L577`
- **传递**: tick_size 通过 ParamsService 产品缓存传递，但仅部分代码路径使用
- **消费**: `_correct_price()` 消费了 tick_size 但未做整数倍对齐（P0问题）
- **结论**: ⚠️ **tick_size 对齐逻辑错误** — 存在价格未对齐到 tick_size 整数倍的风险

---

## 审计维度4：保证金比例动态变化

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 4-1 | `risk_service.py` | L348-383 | `check_margin_usage()` 使用 `margin_ratio` 参数检查保证金使用率，但 `margin_ratio` 由外部传入，系统本身**不查询交易所实时保证金比例**。交易所调整保证金时系统无法自动响应。 | **P0** | 生产 |
| 4-2 | `risk_service.py` | L170-211 | `_calculate_position_margin()` 使用 `params.get('margin_ratio', 0.15)` 固定默认值 15%。交易所保证金比例调整（如长假前提高保证金）时，系统无法自动适配。 | **P0** | 生产 |
| 4-3 | 全文 | - | **无 SPAN 保证金计算逻辑**。CFFEX 期权使用 SPAN 保证金系统，但全系统无任何 SPAN 计算代码。 | **P1** | 生产/评判 |
| 4-4 | `params_service.py` | L784-791 | `load_caches_from_db()` 从 `future_products` 表加载数据，但未加载 `margin_ratio` 字段。数据库中有保证金比例信息但未被系统使用。 | **P1** | 生产 |
| 4-5 | `risk_service.py` | L170-211 | `_calculate_position_margin()` 中 `margin = notional * margin_ratio` 是简单线性计算，不考虑期权卖方组合保证金优惠、跨期套利保证金优惠等交易所实际规则。 | **P1** | 评判 |
| 4-6 | 全文 | - | **无保证金追缴(Margin Call)处理逻辑**。交易所追加保证金时无自动减仓或风控响应机制。 | **P0** | 生产 |

### 全链路追踪结论
- **定义**: 保证金计算使用固定比率，无动态获取交易所保证金的函数
- **传递**: `margin_ratio` 通过参数传入，无实时查询通道
- **消费**: 风控服务使用固定比例计算，不符合交易所动态调整规则
- **结论**: ⚠️ **保证金计算基于固定比率，无法响应交易所动态调整**

---

## 审计维度5：交易时间规则

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 5-1 | `scheduler_service.py` | L552-558 | `_sessions` 交易时段配置中，**CFFEX 配置为 9:30-11:30, 13:00-15:00**，实际 CFFEX 股指期货交易时间为 9:30-11:30, 13:00-15:00（正确），但 CFFEX 国债期货为 9:15-11:30, 13:00-15:15，配置不准确。 | **P1** | 生产 |
| 5-2 | `scheduler_service.py` | L560-566 | `_night_sessions` **夜盘时段配置不完整**。仅配置了 21:00-23:00/23:30 的夜盘，但部分品种夜盘到凌晨 1:00（如黄金 AU 到 2:30，白银 AG 到 2:30）。跨日处理仅处理了跨午夜，未处理跨凌晨的情况。 | **P1** | 生产 |
| 5-3 | `scheduler_service.py` | L568 | `self.holidays: set = set()` **节假日集合默认为空**，需要外部手动配置。系统启动时未自动加载中国期货交易所节假日安排。休市期间可能发出无效委托。 | **P1** | 生产 |
| 5-4 | `scheduler_service.py` | 全文 | **无集合竞价时间处理**。中国期货市场集合竞价时间为 8:55-9:00（日盘）和 20:55-21:00（夜盘），系统无集合竞价时段识别逻辑。集合竞价期间下单可能导致异常。 | **P1** | 生产 |
| 5-5 | `order_service.py` | L116-118 | `_on_bar()` 方法中 `if not self.is_trading: return` 使用简单的 `is_trading` 布尔标志，该标志由 `start_trading()`/`stop_trading()` 手动控制，而非基于交易时间自动判断。 | **P1** | 生产 |
| 5-6 | `scheduler_service.py` | L615-623 | 夜盘跨午夜处理逻辑 `now_time >= start_time or now_time <= end_time` 仅处理了同日内跨午夜的情况，如果夜盘延续到次日凌晨（如 21:00-02:30），`end_time=2:30` 会被判断为跨午夜，但日期边界处理不正确。 | **P1** | 生产 |
| 5-7 | 全文 | - | **无节假日自动获取机制**。系统未接入交易所节假日公告API或配置文件，需要人工维护节假日集合。 | **P2** | 生产 |

### 全链路追踪结论
- **定义**: `MarketTimeService` 定义了基础交易时段，但配置不完整
- **传递**: 交易时间判断通过 `is_market_open()` 传递，但部分代码路径使用手动 `is_trading` 标志
- **消费**: `order_service.py` 使用 `is_trading` 而非自动时间判断
- **结论**: ⚠️ **交易时间配置不完整，夜盘跨日处理有缺陷，节假日需手动配置**

---

## 审计维度6：持仓限额

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 6-1 | `risk_service.py` | 全文 | **无交易所持仓限额检查**。中国各期货交易所对品种有持仓限额规定（如CFFEX股指期货单边持仓限额、DCE/SHFE品种持仓限额等），系统未实现任何持仓限额检测。 | **P0** | 生产 |
| 6-2 | `risk_service.py` | L310-346 | `check_position_concentration()` 仅检查单一品种持仓占总权益的比例，不检查交易所绝对持仓数量限额。交易所限额是按手数计算的绝对值，而非权益占比。 | **P1** | 生产 |
| 6-3 | `position_service.py` | L1103 | `check_position_limits()` 方法存在但实现为空（仅有 `pass`）。持仓限额检查接口定义但未实现。 | **P0** | 生产 |
| 6-4 | `order_service.py` | L159-170 | `send_order()` 方法发送委托前未检查当前持仓是否已达交易所限额。 | **P0** | 生产 |
| 6-5 | `position_service.py` | 全文 | **无大户报告制度处理**。交易所规定持仓达到一定数量需要大户报告，系统无相关检测和上报机制。 | **P2** | 生产 |

### 全链路追踪结论
- **定义**: `check_position_limits()` 定义为空方法（pass）
- **传递**: 无持仓限额数据传递
- **消费**: 无消费方使用持仓限额数据
- **结论**: ⚠️ **全链路缺失** — 持仓限额检查完全未实现

---

## 审计维度7：手续费计算

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 7-1 | `order_service.py` | 全文 | **生产交易系统无手续费计算**。`send_order()` 发送委托但不计算或记录手续费。手续费仅在回测系统（`参数池/task_scheduler.py`）中计算。生产系统无法准确计算实际交易成本。 | **P1** | 生产 |
| 7-2 | `shadow_strategy_engine.py` | L1037-1079 | `execute_trade()` 中手续费通过参数 `commission` 传入，默认值为 0.0。影子策略模拟交易的手续费依赖外部传入，系统本身无计算逻辑。 | **P1** | 测试(影子) |
| 7-3 | `risk_service.py` | 全文 | 风控服务无手续费成本计入风控计算。每日盈亏计算未扣除手续费，可能导致风控阈值判断不准确。 | **P1** | 生产 |
| 7-4 | `参数池/task_scheduler.py` | L53-133 | 回测系统有三维手续费模型（品种-方向-持仓时间），但**平今仓手续费差异**（如CFFEX股指期货平今仓手续费是开仓的数倍）在 `FEE_STRUCTURE` 中仅部分覆盖。如 IF/IC/IH/IH 平今仓高手续费有配置，但 SHFE/DCE/CZCE 品种平今仓差异未完全配置。 | **P1** | 评判(回测) |
| 7-5 | `参数池/task_scheduler.py` | L64 | `COMMISSION_PER_LOT = 1.5` 作为回退默认值，该值不符合任何交易所的实际手续费标准。品种未匹配时将使用错误的手续费。 | **P2** | 评判(回测) |
| 7-6 | `shadow_strategy_engine.py` | L950, L974 | `simulate_open_position()` 和 `simulate_close_position()` 中 `commission=0.0` 硬编码为零，影子策略模拟交易不计手续费，导致模拟盈亏偏高。 | **P2** | 测试(影子) |

### 全链路追踪结论
- **定义**: 手续费计算仅在回测系统中定义（`_compute_commission()`）
- **传递**: 生产系统无手续费数据传递
- **消费**: 影子策略使用外部传入的手续费，默认为0
- **结论**: ⚠️ **生产系统无手续费计算，影子策略手续费默认值为0**

---

## 审计维度8：滑点模型

| # | 文件路径 | 行号范围 | 问题描述 | 严重等级 | 影响系统 |
|---|---------|---------|---------|---------|---------|
| 8-1 | `order_service.py` | 全文 | **生产交易系统无滑点模型**。实际交易中滑点是不可避免的，但系统无滑点预估、滑点监控或滑点自适应逻辑。无法判断实际成交价格是否偏离预期过多。 | **P1** | 生产 |
| 8-2 | `order_service.py` | L1243-1283 | `estimate_slippage()` 方法存在但使用简化的线性模型 `slip_value = min_price_change * slip_ticks * multiplier`，不考虑市场深度、订单簿厚度、波动率等影响滑点的实际因素。 | **P1** | 生产/评判 |
| 8-3 | `order_service.py` | L1251 | `_get_tick_size()` 获取的 tick_size 用于滑点估算，但 tick_size 是合约属性而非市场流动性指标，用 tick_size 估算滑点不够准确。 | **P2** | 生产 |
| 8-4 | `参数池/task_scheduler.py` | L1130-1161 | 回测系统有 `_compute_dynamic_slippage_bps()` 动态滑点计算（考虑 bid-ask spread、到期日倍增），但**该滑点模型仅用于回测**，不影响实际生产交易的委托价格。 | **P2** | 评判(回测) |
| 8-5 | `order_service.py` | L408-418 | `_correct_price()` 追价偏移使用固定 tick 数量（`+tick_size` 或 `-tick_size`），未根据市场深度或波动率动态调整。高波动行情下单 tick 偏移可能不足以成交，低波动行情下可能产生不必要滑点。 | **P2** | 生产 |
| 8-6 | 全文 | - | **无滑点监控告警机制**。实际成交价格与委托价格的偏差未被记录和分析，无法发现流动性异常或滑点恶化趋势。 | **P1** | 生产 |

### 全链路追踪结论
- **定义**: `estimate_slippage()` 定义在 `order_service.py:L1243`，使用简化模型
- **传递**: 滑点估算值未被下游消费方使用（仅估算，不参与委托决策）
- **消费**: 无滑点自适应消费方
- **结论**: ⚠️ **生产系统滑点模型过于简化，且估算结果未被实际使用**

---

## 审计汇总统计

### 按严重等级汇总

| 严重等级 | 问题数量 | 说明 |
|---------|---------|------|
| **P0** | **14** | 会导致实战崩盘或资金损失的严重问题 |
| **P1** | **22** | 功能异常或数据错误 |
| **P2** | **10** | 潜在风险或规范问题 |
| **合计** | **46** | - |

### 按影响系统汇总

| 系统 | P0 | P1 | P2 | 合计 |
|------|----|----|----|-----|
| **生产** | 14 | 16 | 6 | 36 |
| **测试(影子)** | 0 | 2 | 1 | 3 |
| **评判(回测)** | 0 | 4 | 3 | 7 |

### 按审计维度汇总

| 维度 | P0 | P1 | P2 | 核心结论 |
|------|----|----|----|---------|
| 1. 涨跌停板处理 | 4 | 2 | 2 | ⚠️ 全链路缺失 |
| 2. 交割日/到期日管理 | 3 | 3 | 2 | ⚠️ 全链路缺失（仅回测有简单逻辑） |
| 3. 最小变动价位 | 1 | 3 | 3 | ⚠️ 价格未对齐到tick_size整数倍 |
| 4. 保证金比例 | 3 | 2 | 1 | ⚠️ 固定比率，无法响应动态调整 |
| 5. 交易时间规则 | 0 | 6 | 1 | ⚠️ 配置不完整，夜盘跨日有缺陷 |
| 6. 持仓限额 | 2 | 1 | 1 | ⚠️ 全链路缺失 |
| 7. 手续费计算 | 0 | 3 | 2 | ⚠️ 生产系统无手续费计算 |
| 8. 滑点模型 | 0 | 3 | 2 | ⚠️ 模型简化且未被实际使用 |

---

## P0问题详细列表（需优先修复）

| # | 维度 | 文件 | 行号 | 简述 |
|---|------|------|------|------|
| P0-1 | 涨跌停 | `order_service.py` | 全文 | 无任何涨跌停价格检查 |
| P0-2 | 涨跌停 | `order_service.py` | 全文 | 无涨跌停行情标记机制 |
| P0-3 | 涨跌停 | `order_service.py` | L375-383 | 价格修正可能越涨跌停板 |
| P0-4 | 涨跌停 | `position_service.py` | L940-1005 | 紧急平仓未检查涨跌停板 |
| P0-5 | 到期日 | `order_service.py` | 全文 | 无到期日检测 |
| P0-6 | 到期日 | `position_service.py` | 全文 | 无到期日管理逻辑 |
| P0-7 | 到期日 | `risk_service.py` | 全文 | 无到期日风控 |
| P0-8 | tick_size | `order_service.py` | L408-418 | 价格未对齐到tick_size整数倍 |
| P0-9 | 保证金 | `risk_service.py` | L348-383 | 不查询交易所实时保证金比例 |
| P0-10 | 保证金 | `risk_service.py` | L170-211 | 使用固定保证金比例默认值 |
| P0-11 | 保证金 | 全文 | - | 无保证金追缴处理逻辑 |
| P0-12 | 持仓限额 | `risk_service.py` | 全文 | 无交易所持仓限额检查 |
| P0-13 | 持仓限额 | `position_service.py` | L1103 | `check_position_limits()` 为空方法 |
| P0-14 | 持仓限额 | `order_service.py` | L159-170 | 下单前未检查持仓限额 |

---

## 与V7.0手册的对标差距

| V7.0手册要求 | 当前系统状态 | 差距 |
|-------------|-------------|------|
| 参数池统一执行、版本控制、回滚 | 参数池系统已实现 | ✅ 已覆盖 |
| 策略分层管理(L1-L5) | 已实现分层架构 | ✅ 已覆盖 |
| 风控参数动态调整 | 仅部分实现，缺少交易所规则适配 | ⚠️ 部分覆盖 |
| 交易所规则适配 | **完全缺失** | ❌ 未覆盖 |
| 涨跌停板处理 | 无 | ❌ 缺失 |
| 交割日/到期日管理 | 无（仅回测有） | ❌ 缺失 |
| tick_size价格对齐 | 有逻辑但有bug | ⚠️ 有缺陷 |
| 保证金动态变化 | 无 | ❌ 缺失 |
| 交易时间规则 | 有基础但不完整 | ⚠️ 不完整 |
| 持仓限额 | 无 | ❌ 缺失 |
| 手续费精确计算 | 仅回测有，生产无 | ⚠️ 部分覆盖 |
| 滑点模型 | 有但简化且未使用 | ⚠️ 不完整 |

---

*审计报告结束*
*生成时间: 2026-05-23*
*审计方法: 全量扫描(47个.py文件) + 逐行读取交叉验证 + 全链路追踪*
